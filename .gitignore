# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# MCP config (contient des clés API — jamais sur Git)
mcp.json
.mcp.json

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# Playwright MCP (outil local, pas de code source)
.playwright-mcp/

# Playwright E2E (rapports + artefacts générés)
/test-results/
/playwright-report/
/playwright/.cache/

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# Fichiers temporaires d'éditeur (un `.h-9.swp` traînait à la racine)
*.swp
*.swo
*~

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel

# Document de transmission client — JAMAIS sur GitHub
ACCES-CLIENT.md
code google cloud.json

# Ruflo runtime (MPP v4.0) — ne jamais committer
# ⚠️ Ancrés à la racine (/) : sinon `data/` ignore aussi le code source `src/data/`
.claude-flow/
/data/
/logs/
/sessions/
/_backups/

# Dumps de base — JAMAIS sur GitHub (le pattern racine /_backups/ ne couvre
# pas les sous-dossiers : celui-ci attrape supabase/_backups/ et consorts)
**/_backups/
filou/veo-out/

# Documents de test pour l import d un ancien planning (25 Mo, jetables)
_plannings-test/
