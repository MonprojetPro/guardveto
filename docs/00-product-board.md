# Product Board — GuardVeto

> Noyau et memoire du projet. Une decision qui n'est pas ici n'existe pas.
> Tenu par OTTO. Le PRD (`01-prd.md`) porte la vision de fond ; ce board porte la vie du projet.
> **Regle d'or : un identifiant est immortel. On change son statut, jamais son existence.**
> Detail complet des entrees condensees ci-dessous → `docs/00-product-board-archive.md`.

| | |
|---|---|
| Projet | GuardVeto |
| Proprietaire | **MonProjetPro** (produit propre, meme modele que `foxeo-one` : MPP possede la base technique). Le Cabinet veterinaire du Val d'Allier (Anne-Sophie) est un **client abonne**, pas proprietaire — decide par MiKL le 24/08, tranche apres une question ouverte a tort par OTTO. Repo `MonprojetPro/guardveto` confirme au bon endroit. |
| Phase | Build → bascule en cours vers la mise en service reelle chez le premier client abonne, Val d'Allier |
| Derniere mise a jour | 2026-09-03 par OTTO (2e archivage : board 110 617 → 76 244 octets · **0 identifiant perdu sur 121** et **aucun sorti du board**, verifie par `comm` · detail → archive 23) |
| Prochain jalon | Mise en service Val d'Allier (rollout, section 3) — pas de date cible fixee |

**Perimetre par defaut : `A qualifier`.** Jamais `Devis` par defaut.

---

## 1. Vision en 5 lignes

GuardVeto genere et publie le planning de gardes d'un cabinet veterinaire (7 veterinaires chez
Val d'Allier, dont un « dernier recours ») a partir de regles configurables — equite,
frequences, enchainements, absences — resolues par un moteur de contraintes (deux gardiens
independants : moteur + validateur). Filou est l'assistant IA porte-parole de ces regles
(jamais decisionnaire). Reussi si : le cabinet publie un planning sans intervention manuelle
ni violation de regle dure, et si Anne-Sophie mene la mise en service sans secours de MiKL.

---

## 2. INBOX — idees brutes non triees

| ID | Idee | Origine | Date | A qualifier avec |
|---|---|---|---|---|
| B-111 | **🔒 CADENAS SUR LES CASES — l'admin fixe des dates a la main, le moteur compose AUTOUR.** Demande de MiKL le 04/09. Deux moments : ① **AVANT toute generation**, l'admin pre-remplit certaines dates (week-end ou soir de semaine) sur une periode encore vide — ces cases sont cadenassees d'office, et ne s'ouvrent que s'il retire le cadenas ou supprime la case ; ② **SUR UN BROUILLON deja genere**, il pose un cadenas sur les cases qu'il veut garder, puis relance une generation qui ne rebat QUE le reste. Dans les deux cas le moteur doit **tenir compte du fige** (equite, enchainements, repos) au lieu de l'ignorer. Et si le choix de l'admin enfreint une regle : **on l'indique, on ne bloque pas** — c'est sa decision. ⚠️ **Etat des lieux mesure le 04/09** : la colonne `gardes.verrouille` EXISTE et l'ecriture la respecte deja (`ecrirePlanningV1` ne supprime ni ne recouvre une case verrouillee) — mais son propre commentaire l'ecrit noir sur blanc : *« le solver regenere toute la periode sans les connaitre »*. Le moteur compose donc comme si les cases figees etaient libres, et on jette ses propositions dessus apres coup : l'equite et les regles de rythme sont calculees sur un planning qui n'est pas celui qui sera affiche. Manquent aussi : la **pose d'une garde sur une date sans ligne** (`appliquerChangementGarde` ne sait que MODIFIER une garde existante — une periode brouillon non generee n'a aucune ligne) et le **geste de (de)verrouillage** cote ecran hors mode Correction. Deja en place et reutilisable : `avertissements-regles.ts` (dire sans bloquer) et `relecture/reconstruire.ts` (refaire une portion en gelant le reste). **ARBITRE PAR MiKL le 04/09** : ① le cadenas se pose **PLACE PAR PLACE** (1er seul, 2nd seul, ou les deux — independamment) ; ② une case **incomplete est autorisee** (l'admin pose le 1er, le moteur trouve le 2nd) ; ③ une case posee **alimente le compteur immediatement** ; ④ le cadenas **TOMBE a la publication** (aucun interet a le garder) ; ⑤ **perimetre = Devis** (compris dedans). ⚠️ **DEUX MESURES DU 04/09 QUI CHANGENT LA CONCEPTION** : (a) `gardes.verrouille` est **DEJA PRIS** par un autre concept — le cron `api/cron/lock-gardes` la passe a `true` automatiquement sur les gardes passees/publiees, et l'import d'historique aussi (commentaire SQL d'origine : « Garde passee ou publiee — bloquee en modification automatique »). Reutiliser cette colonne rendrait la decision ④ impossible (on retirerait un verrou de protection legitime) et ferait apparaitre un cadenas cliquable sur des gardes d'historique. **Il faut donc un marquage DEDIE, distinct du verrou systeme.** (b) La decision ③ est **deja acquise mecaniquement** : `compteurs_gardes` est une VUE, sans filtre de statut — une garde ecrite en brouillon compte des l'ecriture (verifie sur la migration `20260826120000`). Dette adjacente reperee au passage, **hors perimetre** : cette vue ne compte que `premier_id`/`second_id`, donc les places 3 et 4 d'un cabinet regle a 4 ne sont comptees nulle part. | MiKL | 2026-09-04 | **Devis** — cadre, pret a decouper |
| B-112 | **⚠️ LA RELECTURE DE FILOU IGNORE LES CADENAS — elle peut proposer de deplacer ce que l'admin vient de fixer.** Trouve le 04/09 en ecrivant la decision de couverture Filou du lot 2 de B-111, **pas par un test qui aurait echoue**. Filou relit un planning genere et propose des mouvements (B-062) ; son dossier ne porte AUCUNE information sur les places cadenassees, et rien dans `arbitrer.ts` ne les protege. Il peut donc proposer — avec le meme aplomb que ses autres propositions — de retirer la personne que l'admin a justement figee, et le moteur validerait le mouvement puisqu'il est legal. ⚠️ **Le risque n'est pas theorique** : c'est exactement le motif pour lequel B-111 existe (« quand le moteur regenere il doit tenir compte de ce qui a ete deja fixe »). La relecture est un CHEMIN DE PLUS qui choisit des personnes, et la lecon du 26/08 s'applique mot pour mot : « tout nouveau code qui CHOISIT une personne rouvre le trou sans faire rougir un seul test ». **A traiter AVANT de rouvrir le chantier Filou**, qui est en pause a la demande de MiKL depuis le 04/09. | Auto-detecte 04/09 (couverture Filou, B-111 lot 2) | 2026-09-04 | Filou + moteur — a traiter avant reprise de la relecture |
| B-110 | **⚠️ DEUX FICHES « Victor » actives en base, avec chacune leurs propres regles.** Trouve le 03/09 en diagnostiquant l'ecart de nuits de Victor (B-105) : `veterinaires` contient `00000000-0000-0000-0000-000000000007` (id de seed/placeholder) ET `3bd643a9-5442-4224-8de7-517c1684fb3c`, tous deux `prenom = 'Victor'` et `actif = true`, tous deux porteurs des MEMES regles (lundi/mardi interdits, repos conditionnel). Seul le second recoit des gardes reelles (verifie sur `gardes` et `compteurs_gardes`) — le premier ne fait aucune garde et le premier n'a pas fait doublonner l'ecran Compteurs (7 lignes, pas 8), ce qui laisse croire qu'il est inerte. **Non verifie** : si `contexte.vets` (moteur) ou le dossier de Filou incluent les deux, un raisonnement d'equite pourrait etre discretement fausse par un 8e vetos fantome. Pas touche — a qualifier avant toute suppression, une fiche vetos porte potentiellement d'autres references (historique, comptes). | Auto-detecte 03/09 (diagnostic B-105) | 2026-09-03 | Donnees — a qualifier avant toute suppression |
| B-105 | **🎯 LES COMPTEURS SONT EQUILIBRES ET LE VECU NE L'EST PAS — Victor fait 10 mercredis sur 13 nuits.** Intuition de MiKL le 02/09 (*« sauf peut-etre les nuits pour Victor »*), verifiee en base et **juste, mais pas sur le volume**. Volume : Victor 13 nuits, 3e MOINS charge, et le 2e meilleur rythme (ecart moyen 4,6 j contre 3,8 pour Antoine). **Repartition par jour : lundi 3 · mardi 0 · mercredi 10 · jeudi 1.** 77 % de ses nuits tombent le meme jour. Cause mecanique et non un defaut : son mardi est interdit (regle dure), son lundi evite (souple), son repos conditionnel mord le jeudi — il ne reste que le mercredi. **Rien dans le produit ne mesure la VARIETE des jours** : ni les 6 dimensions d'`equilibrer`, ni les criteres de Filou. Meme famille que « le moteur compte les totaux, jamais le rythme » (20/08) : ici il compte les totaux, jamais la MONOTONIE. ⚠️ Un planning peut etre parfaitement equitable au compteur et invivable au quotidien — « je suis de garde tous les mercredis » est une plainte que rien ne verrait venir. **A ARBITRER** : nouvelle dimension d'equite (variete des jours), ou simple constat de Filou ? ⚠️ **RECONFIRME le 03/09, meme famille sur Anne-Sophie** : `sem_total` = 9 pour elle (contre 17-18 pour Antoine/Fanny), verifie en base. Cause identique — regles personnelles tres restrictives (`interdire_creneau` sur jeudi+vendredi les semaines impaires, ET sur lundi+mardi+mercredi les semaines paires), confirmee par le texte de Filou lui-meme (« les regles personnelles d'Anne-Sophie... la rendent indisponible beaucoup de soirs »). Deux personnes sur sept montrent maintenant le meme symptome — ce n'est plus un cas isole. | MiKL (recette Hiver P2) | 2026-09-02 | Moteur — a qualifier |
| B-106 | **Coquille dans un message affiche a l'admin : « 2 cases resteraont a pourvoir ».** Vue le 02/09 dans les etapes tracees de la generation (`generations_trace`). Meme famille que « regleesur » et « larendre » (B-077) : **un texte abime decredibilise ce qu'il explique**, et celui-ci s'affiche au moment ou l'admin apprend que son planning est incomplet. | Traceur B-104 | 2026-09-02 | Interne |
| B-104 | **🔴 OUVERT — LA GENERATION AVORTE : la fenetre se ferme au bout de quelques secondes, puis « une generation est deja en cours » alors que rien n'aboutit.** Signale par MiKL le 02/09, **recurrent** (« ce n'est pas la premiere fois »). ⛔ **AUCUN FIX POSE — la cause n'est pas prouvee.** **CE QUI EST ETABLI** : ① aucun chemin d'erreur du client ne FERME la fenetre (tous affichent un message) — ce que MiKL voit n'est donc aucun d'eux ; ② la fenetre s'est fermee **ET aucune trace ne s'est ouverte** (mesure du 02/09 22h56) → **la fermeture est probablement la CAUSE** : l'ecran disparait, le `fetch` est annule avec lui, le serveur ne commence jamais ; ③ le « deja en cours » est une CONSEQUENCE (verrou perime a 3 min) ; ④ une generation NORMALE consomme **42 s sur les 60** de la fonction, dont **30,5 s dans la seule etape « Reprise 1 »** qui atteint son plafond. ⚠️ **Piste Vercel ECARTEE** par MiKL. ✅ **TRACEUR POSE** (`68536d3`) puis **ses deux angles morts corriges** (`518e85a`) — il etait aveugle exactement la ou ca casse. **Le prochain incident laissera forcement une ligne** : `abandon_client` (l'ecran a disparu, rien n'a demarre) · `refusee` (verrou) · ou une ligne restee OUVERTE (la fonction est morte). Les trois designent des causes differentes. **PROCHAINE ETAPE : MiKL regenere jusqu'a l'incident, je lis la trace.** Historique complet du diagnostic → archive 23. | MiKL (recette) | 2026-09-02 | Moteur — a diagnostiquer |
| B-103 | **🔴 REGRESSION SUR B-046 — le DERNIER RECOURS entre dans le planning par la reconstruction (B-101), livree ce matin.** Recit integral → archive 23. | MiKL (recette Hiver P2) | 2026-09-02 | Moteur — regression B-046 |
| B-099 | **🔴 TRANCHÉ PAR LA MESURE — le levier MANQUAIT pour Antoine, ce n'est pas Filou.** Recit integral → archive 23. | Mesure automatique (colonne `dossier`) | 2026-09-02 | Moteur — arbitrage MiKL |
| B-101 | **✅ DECIDE PAR MiKL le 02/09 — Filou pourra demander au moteur de REFAIRE une portion de planning.** Recit integral → archive 23. | MiKL (arbitrage) | 2026-09-02 | Moteur — feu vert, a decouper |
| B-100 | **🔴 LES RELECTURES SUCCESSIVES DEGRADENT L'EQUITE DES WEEK-ENDS — mesure en base.** Recit integral → archive 23. | Auto-detecte 02/09 (lecture de la trace) | 2026-09-02 | Filou — **risque assume par MiKL**, pas de correctif |
| B-098 | **💣 Filou confond « changer de rôle » et « ne plus être de garde » — et le dit a l'admin.** Recette du 02/09 apres B-096, mesure en base. Quatre de ses cinq changements sont motives par « le lundi de repos fixe de Victor est mordu ». Resultat net : **Victor est de garde 4 lundis avant, 4 lundis apres** (19/10, 09/11, 30/11, 14/12 — verifie par requete sur `garde_placements`). Detail : ① lundi 09/11 et lundi 14/12 → **inversions de role sur le MEME soir** : Victor passe de 1er a 2nd et **reste de garde**, alors que le motif annonce « allege encore le lundi de Victor » et « continue a soulager Victor ». ② lundi 02/11 → Victor reellement retire… mais le meme mouvement **le pose le lundi 30/11**, un lundi de plus. Un echange lundi contre lundi. ⚠️ **Le probleme n'est pas la mecanique — ces cinq mouvements sont legaux, le moteur les a valides.** C'est que le MOTIF est faux, et qu'il est affiche a l'administratrice avec l'aplomb des motifs justes. Meme famille que le constat invente du 02/09 (« Antoine reprend des le lundi 26 »). **Piste** : le dossier ne dit nulle part qu'un mouvement RETIRE ou non quelqu'un d'une journee — Filou le deduit, et il deduit mal. On sait deja le calculer : les jours couverts avant/apres sont dans `planningApres`. | MiKL (recette B-096) | 2026-09-02 | Filou — a corriger avant la mise en service |
| B-097 | **💣 La revue affichee decrit le planning d'AVANT les changements que Filou vient d'appliquer.** Trouve le 02/09 en recettant B-096. Son constat dit : *« Victor est de garde le lundi... a 4 reprises : lundis 19 octobre, 2 novembre, 9 novembre et 14 decembre »* — or il vient de retirer Victor du **2 novembre** et de le poser le **30 novembre**, qu'il ne cite pas. La liste est donc fausse des deux cotes, sur un ecran qui affiche les changements appliques JUSTE AU-DESSUS. **Cause structurelle** : une seule passe. Filou lit le dossier initial, propose ses changements ET redige sa revue dans la meme reponse ; l'arbitrage applique ensuite. Rien ne relit le planning apres coup. **Ce n'est pas une erreur du modele** — on ne lui a jamais montre le resultat. Deux issues possibles : ① une seconde passe de revue sur le planning arbitre (coute un appel de plus) ; ② n'afficher la revue que sur les criteres qu'aucun changement n'a touches, et le dire. **A trancher par MiKL.** ⚠️ Tant que ce n'est pas regle, l'ecran de fin de generation **affiche des faits perimes comme des faits actuels** — exactement ce que ce projet combat depuis le debut. | Auto-detecte 02/09 (recette B-096) | 2026-09-02 | Filou — a trancher, puis corriger |
| B-096 | **« Filou il a fait quoi ? Rien. » — la relecture avait les chiffres sous les yeux et n'a rien dit.** Recit integral → archive 21. | MiKL (recette Hiver P2) | 2026-09-02 | Filou relecture — lie a B-095, B-062, B-093 |
| B-095 | **Antoine fait un week-end sur deux pendant six semaines, et 5 WE contre 3 a Fanny — vu par MiKL en recettant Hiver P2 le 02/09.** ⚠️ **RECONFIRME le 03/09** : sur les 6 regenerations de la journee, Filou signale la meme surcharge d'Antoine a chaque fois (« il ne va pas tenir la periode sans souffler »), avec la note « il ne voit pas de correction automatique » — c'est le moteur (poids d'equite) qui doit reduire l'ecart AVANT generation, pas une retouche a posteriori de Filou. MiKL : « c'est lui qui doit faire en sorte qu'il n'y ait pas autant d'ecart ». Recit integral → archive 23. | MiKL (recette Hiver P2) | 2026-09-02 | Moteur — equite week-end · lie a B-092 et B-092a |
| B-088 | **Deux ecrans traduisent les roles avec une table AMPUTEE — « troisieme » s'affiche en brut.** Recit integral → archive 23. | Auto-detecte 27/08 (agent AGENDA) | 2026-08-27 | Coherence d'affichage — a faire |
| B-087 | **Le role s'ecrit « premier » / « second » en toutes lettres dans l'agenda — MiKL veut « 1er » / « 2nd ».** Recit integral → archive 21. | MiKL (recette sur l'agenda reel) | 2026-08-27 | Libelle agenda — **fait, apercu en cours de cablage** |
| B-086 | **DEUX encarts Google Agenda sur le meme ecran, et un bouton qui deborde.** Recit integral → archive 21. | MiKL (recette) | 2026-08-27 | UI Reglages + fiche veto — **fait, a recetter** |
| B-085 | **🔴 SECURITE — 8 fonctions `SECURITY DEFINER` sont EXECUTABLES par `anon`, dont `prendre_snapshot`.** Recit integral → archive 23. | Auto-detecte 27/08 (MAX, en verifiant les migrations) | 2026-08-27 | Securite — a qualifier avec CERBERE, ne rien revoquer sans analyse |
| B-083 | **🔴 BOMBE A RETARDEMENT SUR L'ETE — la synchro d'une periode pleine frole les 60 s.** Recit integral → archive 23. | Auto-detecte 27/08 (agent AGENDA) | 2026-08-27 | Arbitrage MiKL — mesurer d'abord, optimiser ensuite |
| B-084 | **Un test « golden » fige desormais un chemin qui n'existe plus.** Recit integral → archive 23. | Auto-detecte 27/08 (agent AGENDA) | 2026-08-27 | Dette — a reprendre apres la recette |
| B-082 | **Les evenements d'agenda n'ont plus de description.** Recit integral → archive 23. | Auto-detecte 27/08 (agent AGENDA) | 2026-08-27 | Decision produit — a trancher par MiKL |
| B-080 | **Des listes de colonnes figees en SQL rendent MUETTE toute colonne future.** Recit integral → archive 23. | Auto-detecte 27/08 (agent SOCLE) | 2026-08-27 | Dette de conception — inventaire FAIT, reste a decider le garde-fou |
| B-037 | **Le planning est equitable en VOLUME et intenable en RYTHME.** Recit integral → archive 23. | MiKL | 2026-08-26 | Moteur — a trancher avec Anne-Sophie |
| B-035 | **Quand il ne reste qu'UN remplacant possible, l'ecran ne le dit pas.** Recit integral → archive 23. | MiKL | 2026-08-26 | Ecran de reparation |
| B-049 | **Le rapport d'impasse est un dump technique, pas une explication.** Recit integral → archive 23. | MiKL | 2026-08-26 | Refonte de l'ecran d'impasse |
| B-051 | **MESURE FAITE — l'impasse est REELLE, et elle tient a DEUX gardes.** Recit integral → archive 23. | MiKL | 2026-08-26 | Decision produit — mesure faite, en attente d'arbitrage |
| B-052 | **Le chargement du moteur ne filtre PAS par cabinet** — decouvert le 26/08 en mesurant B-051. `chargerInputDepuisSupabase` (`engine/loader.ts:268`) lit `veterinaires` avec `.eq('actif', true)` **et rien d'autre** ; idem pour… Recit integral → archive 23. | Auto-detecte 26/08 | 2026-08-26 | Securite — a qualifier avec CERBERE |
| B-053 | **🔴 BLOQUANT PROD — une generation ne doit JAMAIS rendre l'admin bredouille.** Recit integral → archive 23. | Feature (correctif de fond) | MiKL | A qualifier | **Livre — 4 lots, a recetter** | 2026-08-26 |
| B-054 | **Le rapport des cases vides repete 5 fois la MEME objection, et c'est une CONSEQUENCE, pas une cause.** Recit integral → archive 21. | MiKL | 2026-08-26 | Ecran + moteur |
| B-055 | **Dans une garde a moitie remplie, TOUT s'affiche en 1re position — on ne sait pas quelle place reste.** Recit integral → archive 23. | MiKL | 2026-08-26 | Ecran planning + modale de garde |
| B-056 | **Le pre-vol a dit « Rien ne coince » avant une generation qui a laisse 5 cases vides.** Recit integral → archive 23. | MiKL | 2026-08-26 | Ecran de pre-vol |
| B-057 | **L'encart « dernier recours » est desequilibre.** Recit integral → archive 21. | MiKL | 2026-08-26 | Retouche visuelle |
| B-058 | **MiKL n'a pas pu recetter l'Assistance : le cas ne s'est pas presente.** Recit integral → archive 23. | MiKL | 2026-08-26 | Recette |
| B-059 | **« Pourquoi Victor ne fait pas le week-end du 25 septembre ? Il a 0 WE au compteur et n'est pas absent. »** Recit integral → archive 21. | MiKL | 2026-08-26 | Moteur — cause racine identifiee, correctif a valider |
| B-060 | **UNE PASSE DE RATTRAPAGE APRES LE REMPLISSAGE — proposition de MiKL le 26/08 (« un dossier tres serieux »).** Recit integral → archive 21. | MiKL | 2026-08-26 | Moteur + ecran — EN COURS |
| B-061 | **Fanny fait 2 week-ends, les DEUX en 2nde — et le compteur « 1er WE » se desequilibre.** Recit integral → archive 21. | MiKL | 2026-08-26 | Moteur — cause racine prouvee, correctif a valider |
| B-006 | V3 — etendre le produit aux journees de travail (aujourd'hui : soirs + week-ends seulement). Exclu volontairement de la V2. | MiKL | 2026-08-24 | V3, ne pas commencer |
| B-015 | **Le selecteur de couleur deseequilibre la fiche veto.** MiKL, 24/08, apres recette : « ca marche, par contre fais quelque chose au niveau de l'affichage parce que la fenetre est asymetrique et chargee ». Constat sur capture : le selecteur est deplie en PERMANENCE dans le formulaire, colle a gauche sur un tiers de largeur, la moitie droite vide — alors que la reference fournie par MiKL etait un panneau compact. S'y ajoutent 18 pastilles de suggestion sur deux rangees et trois blocs de texte d'aide. La fiche fait desormais deux ecrans de haut pour changer un prenom. | MiKL | 2026-08-24 | Retouche visuelle, en cours |

**B-003 ≠ B-004** (arbitrage MiKL 24/08) : B-004 est une regle **deja active** dans le moteur
(voir section 4) ; B-003 reste **a construire** (voir section 3). Detail → archive 2bis.

---

## 3. Backlog qualifie

> Statuts : Qualifie → Pret → En cours → En revue → Livre (ou Gele / Ecarte).
> Perimetre : GuardVeto est un produit MPP (abonnement) — la majorite du dev est **Interne**
> (invisible du client, inclus dans l'abonnement). `A qualifier*` marque un item ne du besoin
> SPECIFIQUE de Val d'Allier, dont on ne sait pas encore s'il reste inclus ou devient une
> prestation a part — decision MiKL du 24/08, nuance demandee par team-lead, a trancher au cas
> par cas (items marques * ci-dessous).

| ID | Titre | Type | Origine | Perimetre | Statut | MAJ |
|---|---|---|---|---|---|---|
| B-109 | **🔴 FILOU N'A JAMAIS FAIT CE POUR QUOI IL EST LA — 0 changement propose alors que 108 mouvements legaux existaient.** Question de MiKL le 03/09 : « quand est-ce que Filou va reussir a s'apercevoir qu'Antoine fait des week-ends tous les 2 semaines et proposer un autre modele, ce qu'il avait deja reussi a faire ? ». Verifie en base sur la relecture Hiver P2 du jour : Filou a marque `corrigeable: true` sur le rythme d'Antoine — donc a annonce vouloir corriger — et n'a ajoute AUCUN changement, ni pour ce critere ni pour aucun autre, alors que le dossier listait explicitement Antoine parmi les personnes qu'un mouvement peut soulager (2 `refaire_semaine`, 5 `remplacement_weekend`, section dediee qui interdit textuellement d'ecrire « pas de correction automatique » dans ce cas precis). **Cause structurelle trouvee** : rien dans le schema ne relie `revue[].corrigeable` a `changements[]` — Filou peut promettre sans agir, et rien ne le contredit a l'ecran. Corrige : un controle (`promesseNonTenue`) compare les deux et affiche a l'admin « Filou a dit pouvoir corriger ce point, mais n'a propose aucun changement » au lieu du silence actuel. **Ce correctif rend le probleme VISIBLE, il ne le RESOUD pas** — pourquoi Filou n'utilise aucun des 108 leviers reste ouvert, piste principale : la relecture tourne a l'effort `low` par defaut (`EFFORT_PAR_DEFAUT`), jamais valide sur un dossier de cette taille (12 semaines, 16 preferences enfreintes). **A trancher par MiKL** : tester a l'effort `medium` (plus lent, `~130s` mesure par le banc) avant de changer le defaut. | Correctif + diagnostic | MiKL (recette Hiver P2) | Interne | **Livre (le controle) — le vrai probleme (0 changement propose) reste ouvert, a trancher** | 2026-09-03 |
| B-108 | **💣 FILOU ADDITIONNE LE TOTAL ET LES WEEK-ENDS — « Antoine fait 27 gardes » alors que la base en donne 22.** Trouve le 03/09 en verifiant un delta signale par MiKL entre le rapport de Filou et l'ecran Compteurs. Preuve en base : `compteurs_gardes` donne Antoine 22 (17 semaine + 5 WE) et Anne-Sophie 13 (9 + 4) pour Hiver P2. Les **6 relectures** de la journee (`relectures_planning`, de 2026-09-02 19h05 a 2026-09-03 14h05, sur des plannings regeneres a chaque fois) ecrivent TOUTES **exactement** « Antoine 27 » et « Anne-Sophie 17 » = total + week-ends, le meme calcul errone repete de facon identique — pas un hasard de generation, une erreur de lecture systematique. Cause : le dossier disait « X gardes, DONT Y week-ends » — grammaticalement Y est inclus dans X, mais rien n'empechait de les additionner, et Filou le faisait a chaque fois. Corrige en nommant le total EXPLICITEMENT « au total » avec la clause « deja compris dans les X, ne pas les rajouter ». | Correctif (fiabilite Filou) | MiKL (recette Hiver P2) | Interne | **Livre** — a recetter (relancer une relecture pour verifier que « 27 » ne revient plus) | 2026-09-03 |
| B-107 | **L'ECRAN DE FIN DE GENERATION EST ILLISIBLE.** Sortie de l'inbox le 03/09 : MiKL a regenere Hiver P2, le rapport refondu le 02/09 restait « encore imbuvable » sur trois points precis (deux captures a l'appui). Trois pistes validees par MiKL et livrees le meme jour : objections du moteur humanisees a l'affichage (`humaniserObjection`, sans toucher `Violation.detail` partage avec CartesViolations/GardienFilou), liste des mouvements en tableau quand le format s'y prete (`parserGeste(s)`), motif de Filou visuellement separe des places touchees. Recit integral → archive 23. | Retouche visuelle | MiKL (recette) | Interne | **En cours** — livre, MiKL recette sur ecran reel (rendu jamais vu, voir Convergence) | 2026-09-03 |
| B-093 | **Filou relevait SEPT problemes et n'en corrigeait AUCUN — on lui donnait les remplacements, jamais les echanges.** Recit integral → archive 21. | Correctif | MiKL | Interne | **Livre** — a recetter | 2026-09-01 |
| B-094 | **Le meme week-end a TROIS definitions differentes dans le produit.** Recit integral → archive 23. | Feature | MiKL | A qualifier | **Pret** | 2026-09-01 |
| B-092 | **Le moteur comptait un week-end comme un point, et rangeait l'equite derriere toutes les preferences.** Recit integral → archive 21. | Correctif | MiKL | Interne | **Livre** — a recetter | 2026-09-01 |
| B-092a | **Exposer les seuils de desequilibre critique sur l'ecran Regles.** Reste-a-faire assume de B-092. Le moteur sait deja lire un reglage par cabinet (`EquityWeights.seuilsCritiques`, threade partout comme les poids et les cohortes) ; ce qui manque est l'ecran qui l'expose, dans le meme vocabulaire a quatre crans que les autres regles d'equite. D'ici la, les valeurs de depart s'appliquent a tous les cabinets et **aucun cabinet ne peut les changer** — contraire au principe « toutes les regles reglables ». Valeurs en place : 2 pour les week-ends, le 1er du week-end et les feries ; 3 pour 1er/2nd/renfort de semaine ; 0 (desactive) pour les grands week-ends, compteur derive qui suit mecaniquement celui des week-ends. | Feature | Reste-a-faire B-092 | Interne | **Pret** | 2026-09-01 |
| B-091 | **La copie du cabinet vers le bac a sable avait laisse 13 regles muettes — et personne ne l'a su pendant des semaines.** Recit integral → archive 21. | Correctif | MiKL | Interne | **Livre** — a recetter | 2026-08-31 |
| B-090 | **Une marque « bac a sable » sur tous les ecrans du compte de demonstration.** Recit integral → archive 21. | Feature | MiKL | Interne | **Livre** — a recetter | 2026-08-31 |
| B-089 | **La relecture de Filou est lente, et personne n'avait choisi son niveau d'application.** Recit integral → archive 21. | Correctif + Feature | MiKL | Interne | **En cours** — lots 1 et 2 livres (banc + Sonnet 5 + 3 correctifs), MiKL recette | 2026-08-31 |
| B-048 | **⚠️ « L'equipe GuardVeto a recu ta situation » s'affichait SANS AUCUNE PREUVE D'ENVOI.** `signalerLimite` faisait `await sendBrevoEmail(...)` dans un try/catch — or cette fonction ne LEVE jamais, elle RETOURNE `{ error }`. La valeur n'etait pas lue : « C'est parti » s'affichait meme quand rien ne partait, et rien n'etait journalise (aucune ligne `email_log` le 26/08 a 18h49, verifie en base). **Corrige par SUPPRESSION** du chemin, pas par rafistolage : le signalement passe desormais par l'onglet Assistance, qui lui journalise (`demandes_support.email_envoye` / `email_erreur`). | Correctif | MiKL | Interne | **Livre** — a recetter | 2026-08-26 |
| B-050 | **« Prevenir l'equipe » envoyait un message vide.** Le champ libre etait facultatif. Le bloc ORIENTE maintenant vers l'Assistance : « prends une capture d'ecran avant de quitter », « decris la situation », et un lien vers `/support` avec titre + description pre-remplis (contexte technique inclus, relisible et modifiable). Rappel de la capture affiche a l'arrivee, quand on peut encore la joindre. | Correctif | MiKL | Interne | **Livre** — a recetter | 2026-08-26 |
| B-046 | **Le dernier recours n'entre PLUS JAMAIS dans une generation.** Demande de MiKL le 26/08. Avant : il n'etait pas exclu, seulement REPOUSSE (score 1 000 000) — donc mobilise des que le moteur n'avait plus personne, sans que l'admin l'ait decide. Desormais il sort de l'effectif AVANT le moteur (`engine/effectif.ts`, drapeau `pourGeneration` sur les 3 portes : generation, replay, pre-vol) ; equite et pre-vol ne le comptent plus non plus. **Consequence assumee et mesuree : une periode que seul lui pouvait boucler echoue maintenant** — et l'ecran NOMME le reglage au lieu de laisser chercher parmi les regles. Il reste propose pour les retouches a la main, la reparation d'absence et les appels aux volontaires. Note explicative ajoutee sous la case dans Equipe ; 4 textes de Filou devenus faux corriges ; garde-fou `tests/lib/generation-exclut-dernier-recours.test.ts` (mutation testee). | Feature | MiKL | A qualifier | **Livre** — a recetter | 2026-08-26 |
| B-047 | **Conges affiches SOUS les gardes** dans la case du calendrier (`PlanningV2.tsx`) : la garde est l'information principale, elle se lit en premier. | Retouche visuelle | MiKL | Interne | **Livre** — a recetter | 2026-08-26 |
| B-033 | **⚠️ FUITE ENTRE COMPTES — la conversation Filou d'une admin restait affichee a un veterinaire.** Cle `sessionStorage` FIXE, sans identifiant de personne : changer de compte dans le meme onglet gardait le fil du precedent. Corrige sur la conversation ET sur la reponse du tableau. Detail integral → archive 12. | Correctif | MiKL (recette) | Interne | **Livre** | 2026-08-26 |
| B-032 | **Filou est muet pour les veterinaires — decision produit a prendre.** Decouvert avec B-033. `FilouChat` conditionne le champ de saisie a `estAdmin` ; la branche veterinaire rendait `null`. Un veto voyait donc la tablette, le renard et le mot d'accueil, **sans rien pour ecrire ni aucune explication** — la coquille vide que ce projet refuse partout ailleurs. ⚠️ **Le serveur, lui, EST pret** : le catalogue ouvre une vingtaine d'outils a un veterinaire (`outilsPour`, mesure de l'audit B-007 : 20 pour un veto, 52 pour une admin). La capacite existe, l'ecran ne la donne pas. **Correctif immediat pose le 26/08 : une phrase honnete a la place du vide** (« Filou repond aux questions de l'administratrice ; ton planning et tes conges se consultent a l'ecran »). **Reste a trancher par MiKL : ouvre-t-on le champ aux veterinaires ?** Ce n'est pas un correctif — ca change ce que six personnes peuvent faire, et le perimetre par droits est deja code cote serveur. | Decision produit | Auto-detecte 26/08 | A qualifier | Qualifie — a trancher | 2026-08-26 |
| B-029 | **⚠️ L'effectif de nuit n'est plus reglable a l'ecran — et un test rassurait sur un ecran MORT.** Trouve le 26/08 en supprimant le code mort. `tests/lib/controle-impact-couverture.test.ts` verifiait que « les QUATRE ecrans » ouvrent le gardien d'impact ; le quatrieme, `components/admin/EffectifPeriodeSelect.tsx`, n'etait atteint par aucune page. **Le test rendait donc un vert sur du vide.** En V2 l'effectif de nuit est AFFICHE (Epicentre, parcours de generation) mais ne se regle plus sur un ecran dedie : il vient de la periode type (places par creneau). L'action `setEffectifPeriode`, avec son controle d'impact, **n'est plus appelee que par un outil de Filou**. Le test dit desormais « trois ecrans » et explique pourquoi. **A trancher : le controle d'impact « demander 2 vetos par nuit la ou l'equipe n'en fournit qu'un » doit-il etre re-pose sur le nouveau chemin (places par creneau) ?** Aujourd'hui cette porte n'est gardee que si l'on passe par Filou. | Dette technique | Auto-detecte 26/08 | Interne | Qualifie — a trancher | 2026-08-26 |
| B-030 | **`validerConfigBrique` ne valide rien en production** — il n'est appele que par son propre test. Decouvert le 26/08 : supprime par erreur en le prenant pour un simple ré-export, il DEFINIT en realite cette fonction, et son test s'est casse. Restaure et declare dans le garde-fou anti-code-mort, mais sans complaisance : un validateur de schema que la production n'appelle jamais ne garde rien. **A trancher : le brancher la ou une config de brique entre dans le systeme, ou l'assumer comme une aide de test.** | Dette technique | Auto-detecte 26/08 | Interne | Qualifie — a trancher | 2026-08-26 |
| B-031 | **L'appel aux volontaires n'a JAMAIS tourne — on ne peut donc pas dire qu'il fonctionne.** Question de MiKL le 26/08 : « est-ce qu'on est sur que la fonction appel aux volontaires fonctionne bien ? ». Reponse honnete : **non, faute de preuve.** `email_log` ne contient AUCUNE ligne de type `appel_volontaires` — la fonction n'a jamais ete declenchee en reel. Ce qui EST verifie : le type est bien autorise par la contrainte `email_log_type_check`, le code journalise succes ET echec, et les garde-fous de la route sont en place (admin, cabinet, creneau encore impacte, candidats legaux recalcules). Ce qui ne l'est PAS : l'envoi effectif, le lien recu, et le parcours du veto qui clique. ⚠️ **Obstacle a la recette : aucun veterinaire du cabinet bac a sable n'a d'adresse e-mail** (`email` a NULL sur les 7) — un appel aux volontaires n'y enverrait rien. Il faut soit des adresses de test (alias de MiKL), soit un test automatise de la chaine. **Contexte utile : sur les 29 e-mails jamais journalises, 9 sont partis et 20 ont echoue** — tous explicables (adresses `@guardveto.local` fictives sans MX, et expediteur non valide chez Brevo), et plus rien depuis le 21/08. | Verification | MiKL | Interne | Qualifie — a prouver | 2026-08-26 |
| B-022a | **⚠️ B-022 avait ete livre DANS DU CODE MORT** — la `key` etait dans `MonthView.tsx`, que rien n'importe ; l'ecran reel est `PlanningV2.tsx`. Le controle de convergence verifiait que la ligne EXISTAIT, pas qu'elle etait ATTEIGNABLE. **Un grep prouve qu'un code est ecrit, jamais qu'il est execute.**  Detail integral → archive 12. | Correctif | Auto-detecte 26/08 | Interne | **Livre** | 2026-08-26 |
| B-026 | **Fenetre « Reparer le planning » portee sur le systeme de design.**  Detail integral → archive 12. | Retouche visuelle | MiKL | Interne | **Livre** | 2026-08-26 |
| B-027 | **Code mort de la V1 — recense PAR MESURE puis nettoye.** 294 fichiers, 274 atteints ; **14 supprimes** (~1500 lignes), 4 faux positifs documentes dont `src/proxy.ts`, qui est le middleware d'authentification (Next 16 le charge lui-meme). Garde-fou pose : `tests/lib/aucun-code-inatteignable.test.ts`. Detail integral → archive 12. | Dette technique | Auto-detecte 26/08 | Interne | **Livre** — 14 fichiers supprimes | 2026-08-26 |
| B-028 | **LIVRE — le test qui refuse le code inatteignable.** Feu vert de MiKL le 26/08.  Detail integral → archive 12. | Process | MAX | Interne | **Livre** | 2026-08-26 |
| B-022 | **Le formulaire d'absence etait en retard d'un CLIC** — on pouvait declarer absent le mauvais veterinaire, sur un planning publie.  Detail integral → archive 12. | Correctif | MiKL (recette) | Interne | **Livre** | 2026-08-26 |
| B-023 | **Une regle affichait l'IDENTIFIANT TECHNIQUE d'un veto retire de l'equipe.** Six replis divergents, alors que la phrase juste au-dessus savait deja le dire en francais. Source unique + test. Piege : un cycle d'imports aurait rendu `undefined`. Detail → archive 10. | Correctif | MiKL (recette) | Interne | **Livre** | 2026-08-26 |
| B-024 | **Le bac a sable etait inexploitable** : le clone n'avait pas remappe les references, les 11 regles du Demo pointaient vers les vetos de Val d'Allier. Remappe et verifie personne par personne. ⚠️ Le CLONAGE lui-meme n'est pas corrige. Detail → archive 10. | Correctif (donnees) | Recette 26/08 | Interne | Partiellement livre | 2026-08-26 |
| B-025 | **L'amorcage pose des plannings `verrouille` avec des dates FUTURES**, ce qui fait disparaitre le lien « Absent·e » sans explication. ⚠️ Val d'Allier est dans le meme cas. Detail → archive 10. | Dette technique | Recette 26/08 | Interne | Qualifie | 2026-08-26 |
| B-026 | **Fenetre « Reparer le planning » portee sur le systeme de design.** Cause reelle : elle n'avait jamais ete portee sur la V2 (5 couleurs Tailwind par defaut). L'avertissement passe sur `.gf-card.souple` comme les 4 autres ecrans, le sortant redevient lisible, le pave vert passe sur `--ok-soft`. Piege attrape : 2 des 3 ecrans qui l'ouvrent n'importaient pas le CSS. Detail → archive 10. | Retouche visuelle | MiKL | Interne | Qualifie | 2026-08-26 |
| B-003 | Compteur de jours a rattraper (formation pendant conges). Design MiKL 24/08 : compteur par veto, pose en joker valide par l'admin, jour bloque en dur. Bloque sur 6 questions a Anne-Sophie (→ archive 3bis / 8bis). | Feature | Anne-Sophie/MiKL | A qualifier* | Qualifie — bloque | 2026-08-24 |
| B-019 | **REGLE PERMANENTE : Filou suit le produit, et c'est un REFUS qui l'impose.**  Detail integral → archive 12. | Process | MiKL | Interne | Livre | 2026-08-25 |
| B-018 | **Filou mis a jour des nouveautes du 25/08** — 4 outils secretariat, prompt systeme, orientation vers l'onglet Assistance. Le risque n'etait pas « il ne sait pas faire » mais une reponse incomplete presentee comme complete. Detail integral → archive 9. | Correctif | MiKL | Interne | Livre | 2026-08-25 |
| B-007 | **Audit de couverture de Filou — VOLET 2 RENDU.** Les 6 trous n'etaient pas oublies, ils etaient INVISIBLES : le test ne lisait que les `actions.ts`, les 18 routes API y echappaient.  Detail integral → archive 12. | Audit | MiKL | Interne | **Livre** | 2026-08-26 |
| B-021 | **Les 6 outils Filou manquants, nommes par B-007 volet 2.** Par valeur decroissante : ① **se porter volontaire** (geste veto le plus courant, aujourd'hui Filou explique puis dit « va sur l'ecran ») ; ② **modifier une garde** (retouche libre admin) ; ③ export PDF ; ④ lancer une generation ; ⑤ relancer la synchro agenda ; ⑥ bilan de fin de periode. ⚠️ **① et ② ecrivent sur les chemins les mieux gardes du produit** — `volontaire` emploie le SERVICE ROLE derriere 5 verrous, `PATCH gardes` porte regles dures + perimetre jour/bloc + 409 + audit. L'outil devra passer par une fonction partagee extraite de la route, **jamais reimplementer ces controles** (meme geste que `lib/crise/changements.ts` pour T-006). A recetter, donc a ne pas empiler sur une autre livraison. | Feature | Audit B-007 | Interne | Qualifie — Should | 2026-08-26 |
| B-009+B-013+B-014 | **LIVRE `360794a`** — les trois correctifs commites et pousses, verifies par execution reelle (tsc propre, 1253 tests verts, rejoues deux fois independamment). Detail integral → archive 9. | Correctif | Audit B-007 | Interne | Livre | 2026-08-24 |
| B-010 | **Clos par la MESURE, et le verdict de dette etait perime** : les vues sont bien en `security_invoker` (verifie en base), 0 erreur au linter, et les deux documents accuses disaient vrai. Ce qui manquait etait la trace de l'episode. Detail → archive 10. | Dette technique | Audit B-007 | Interne | **Livre** | 2026-08-26 |
| B-011+B-012 | **LIVRE `5657b30`** — 22 fichiers. Filou distingue enfin « la base a repondu, il n'y a rien » de « la base n'a pas repondu », et les sources consultees s'affichent. Detail integral → archive 9. | Correctif | Audit B-007 | Interne | Livre | 2026-08-24 |
| B-011+B-012-ancien | Remplace par la ligne ci-dessus — etat intermediaire ou le build etait rouge, conserve pour la lecon (ne pas livrer sur une verification portant sur un etat anterieur). Detail integral → archive 9. | Correctif | Audit B-007 | Interne | Remplace | 2026-08-24 |
| B-015 | **LIVRE `f227030`** — selecteur compacte en panneau flottant, tactile intact, pipette conditionnelle.  Detail integral → archive 12. | Retouche visuelle | MiKL | Interne | Livre | 2026-08-24 |
| B-011 | **Solde** — le gros etait livre le 24/08 (`5657b30`), il restait UN reliquat (`equipe.ts:352`) : une base muette faisait proposer « Inviter X » a quelqu'un ayant deja un compte. Detail → archive 10. | Correctif | Audit B-007 | Interne | **Livre** | 2026-08-26 |
| B-012 | **Rien ne veille sur ce que Filou REPOND.** Le second gardien (`agentFilou.ts:567-645`, appel isole + `tool_choice`) controle l'omission d'une action, pas l'invention d'une reponse. Deux chemins laissent passer un texte non fonde : `agentFilou.ts:291-304` (si le modele ne demande aucun outil au premier tour, son texte libre part sur le tableau) et `afficher.ts:20-41` (titre, introduction et lignes en texte libre, affiches tels quels). **Le materiau du controle existe deja et n'est branche nulle part** : `outilsAppeles` est constitue a chaque tour (`agentFilou.ts:332`) puis jamais teste, ni meme transmis au client. Deux options a trancher par MiKL : refuser d'afficher une reponse portant sur le cabinet quand aucune lecture n'a eu lieu, ou remonter les sources a l'ecran. | Decision produit | Audit B-007 | Interne | A trancher par MiKL | 2026-08-24 |
| B-013 | **LIVRE `360794a`** — le jumeau orthographique desactivait des regles en silence (etiquette « seniors » ecrite par Filou la ou l'equipe utilise « senior » : le geste reussit, et les regles cessent de s'appliquer). Detail integral → archive 9. | Correctif | Audit B-007 | Interne | Livre | 2026-08-24 |
| B-014 | **LIVRE `360794a`** — l'expediteur des e-mails n'etait pas revalide cote serveur ; une adresse invalide faisait tomber les sept chemins d'envoi, silencieusement. Detail integral → archive 9. | Correctif | Audit B-007 | Interne | Livre | 2026-08-24 |
| T-006 | **L'ecran de crise consulte enfin le gardien des regles.** Traduction partagee sortie dans `lib/crise/changements.ts`, validation separee de l'ecriture, 409 + modale + trace audit. Piege evite : `onClick={handleAppliquer}` aurait confirme d'office. Detail → archive 10. | Dette technique | Audit B-007 | Interne | **Livre** | 2026-08-26 |
| T-007 | **LIVRE `b82eccb`** — les trois appels IA suivent enfin le meme reglage. Contient la correction d'une affirmation fausse (« Filou a tourne un mois sur Opus » etait FAUX). Detail → archive 11. | Cout | Audit B-007 | Interne | Livre | 2026-08-24 |
| B-008 | **REQUALIFIE le 26/08 : n'est PLUS bloquant, et ne l'etait deja plus quand B-017 est parti en prod.** Verification avant tout code : le booleen `estAdmin` et le filtre `!adminSeulement \|\| estAdmin` existent bien, tels que decrits. **Mais la faille redoutee est fermee un cran plus haut** — `filou/actions.ts` refuse EXPLICITEMENT le secretariat (lot B-017 du 25/08), avec le commentaire qui dit pourquoi le refus ne doit pas rester un effet de bord. Une secretaire n'atteint jamais le catalogue. **Ce qui reste est de la dette latente, pas une faille** : le jour ou un 3e role doit parler a Filou, le booleen ne suffira pas. ⚠️ **La ligne de board a affiche « bloquant avant le role secretaire » pendant que le role secretaire partait en production** — le board decrivait un etat que le code avait deja depasse, comme B-010 et B-011 le meme jour. | Dette technique | Audit B-007 | Interne | Qualifie — Could, sans urgence | 2026-08-26 |
| — | Mise en service Val d'Allier : Anne-Sophie relit les regles, valide les souhaits, genere le brouillon, verifie, invite l'equipe, publication a deux avec MiKL | Feature (rollout) | Anne-Sophie | A qualifier* | En cours | 2026-08-24 |
| T-001 | **Le repli d'agenda Google devient NOMINATIF.** Il etait accorde a tout cabinet sans agenda : au 2e client, c'est ecrire dans l'agenda d'un autre. Preuve trouvee en base (valeur bidon posee a la main pour se neutraliser). Detail → archive 10. | Dette technique | Audit interne | Interne | **Livre** | 2026-08-26 |
| T-002 | **Re-validation continue aveugle aux exceptions par jour.** Verifie le 26/08 : `revaliderPlanning` reconstruit le planning depuis la table `gardes`, alors que la vue `planning_semaine` applique les exceptions (migration `20260820151000`). Un jour remplace a titre exceptionnel est donc juge sur son titulaire d'origine — la re-validation peut crier une violation qui n'existe plus, ou taire une violation reelle. ⚠️ **NON CORRIGE VOLONTAIREMENT : ce n'est pas un oubli, c'est un arbitrage produit non tranche.** Le projet a deja decide qu'une exception d'un jour ne compte PAS dans le rythme (`avertissementsReglesDuresJour` : « un seul creneau ne forme ni paire ni serie »). Appliquer betement les exceptions au validateur ferait donc apparaitre des violations fantomes — le defaut « une regle souple noyee par un faux positif » deja paye ici. **Question a trancher : que doit dire le bandeau sur un jour exceptionnellement remplace ?** | Dette technique | Audit interne | Interne | Qualifie — bloque sur arbitrage | 2026-08-26 |
| T-003 | **Les compteurs montrent les vetos a zero garde.** Le dernier recours disparaissait quand tout allait bien, et la moyenne d'equite etait calculee sans lui. Mesure avant/apres : 14 lignes contre 14, 0 divergence. Migration appliquee. Detail → archive 10. | Dette technique | Audit interne | Interne | **Livre** | 2026-08-26 |
| T-004 | Suivi des migrations non fiable (appliquees mais absentes de la liste officielle) | Dette technique | Audit interne | Interne | Qualifie | 2026-08-24 |
| T-005 | Feries en semaine absorbes par le creneau ordinaire. **Non traite le 26/08** : contrairement a T-001/T-003, ce n'est pas un defaut technique a corriger mais une regle metier a etablir (un ferie tombant un mardi doit-il etre couvert comme un dimanche, comme un soir de semaine, ou selon un reglage du cabinet ?). Aucune correction possible sans cette reponse — et c'est une question pour Anne-Sophie. | Dette technique | Audit interne | Interne | Qualifie — bloque sur arbitrage metier | 2026-08-26 |
| T-008 | **Kit realtime reporte** : les 5 abonnements signalent desormais leur echec au lieu de se taire. Restent 3 protections du kit a reporter, non urgentes. Detail → archive 10. | Dette technique | FORGE 25/08 | Interne | Partiellement livre | 2026-08-26 |

**Etat de verification de l'audit B-007** — le pave de controle du 24/08 (protocole `git log`
puis `git show HEAD:...` fichier par fichier) est **archive** : ses trois verdicts ouverts ont
tous ete rejoues et tranches le 26/08 (B-011 solde, B-012 toujours a trancher par MiKL, B-008
requalifie non bloquant). Detail integral → archive 3ter.

> Limite volontaire : pas plus de 3 items « En cours » — respectee (2/3 : B-002, rollout).

---

## 4bis. Convergence — controle de sortie avant commit

> **Rempli par OTTO avant CHAQUE commit qui contient du code.** Une ligne par element annonce.
> C'est la seule section que le harnais lit pour autoriser un commit : sans entree fraiche
> pour l'item concerne, `gate-commit` refuse. Ce n'est pas de la paperasse — c'est la reponse
> a la seule question qui compte : *a-t-on livre ce qui etait demande, en ENTIER ?*

| ID | Ce qui etait annonce | Ce qui est reellement livre | Verdict |
|---|---|---|---|
| B-111 | **LOTS 3 ET 4** — le cadenas se pose sur la GRILLE, d'un geste (choix B de MiKL) | `CadenasPlaces.tsx` : `BoutonCadenas` (une place tenue : fixer / liberer), `FixerUneGarde` (pre-remplissage d'un jour vide), `BandeauCadenas` (ce que le choix enfreint). Branches dans `PlanningV2` · CSS dans `v2-planning.css`. Le cadenas OUVERT ne se montre qu'au survol — le dessiner partout noierait la seule information qui compte, « celle-ci est fixee ». `@media (hover: none)` le garde atteignable sur tablette (lecon du 07 : une commande visible au seul survol n'existe pas sur tactile) | OK |
| B-111 | 💣 **L'ECRAN AURAIT CADENASSE LA MAUVAISE PERSONNE UN JOUR SUR TROIS** | Sur la ligne du vendredi, la vue inverse les roles : le « 1er » affiche est le 2nd de la garde. L'ecran aurait pu refaire l'inversion — **il ne la fait PAS**. Il envoie la GARDE et la PERSONNE ; le serveur cherche la place qu'elle occupe. L'inversion depend de la config du cabinet, que la vue connait et que l'ecran ignore : la recopier aurait tenu jusqu'au premier cabinet qui decouple son vendredi, puis se serait trompee en silence. C'est aussi ce que l'admin a en tete — « ce week-end-la, c'est Fanny », jamais « la place 2 du creneau » | OK |
| B-111 | Le pre-remplissage d'une periode VIDE, qui etait la moitie de la demande | Sans lui, une periode non generee n'a aucune ligne `gardes` — donc aucun endroit ou cliquer, et le cas ① de la demande restait inatteignable. `creneauPosableDuJour` (nouveau) derive le creneau du jour ; le vendredi et le dimanche n'offrent RIEN et disent pourquoi (portes par la garde du samedi), plutot qu'un bouton qui creerait une ligne que rien ne lit et qui collisionnerait sur `UNIQUE(date, type)` | OK |
| B-111 | Garde-fou anti-divergence pose sur le champ | `creneauPosableDuJour` doit rester la RECIPROQUE de `mapTypeGardeEnDb` (l'une part d'un jour, l'autre d'un creneau — infusionnables, mais meme decoupage). Un test compare les deux sur 4 cas dont le ferie. Les laisser diverger ferait creer a la main des gardes que la generation ecraserait au tour suivant, **sans erreur ni message** — l'admin constatant seulement que « ca n'a pas tenu » | OK |
| B-111 | Le style dit « fixe » SANS ressembler a « regle enfreinte » | Les deux tombent sur la MEME ligne quand l'admin fixe quelqu'un contre une regle — cas explicitement prevu par la demande. L'orange plein reste au reproche (`.vet-row.warned`), le cadenas parle par un lisere sobre a gauche. Jetons du projet uniquement, aucune couleur inventee | OK |
| B-111 | **LOT 4** — la generation DIT ce qu'elle a garde | `/api/generate` annonce « je garde N places que tu as fixees et je compose autour » pendant le parcours, et renvoie `placesFigees` + `cadenasInoperants`. `NoteCadenas.tsx` les affiche sur les TROIS issues (complet, partiel, echec). Sans ce chiffre, une regeneration contrainte a 3 cases sur 40 se lirait comme un moteur qui n'a rien trouve — l'admin chercherait le defaut du mauvais cote | OK |
| B-111 | 💣 **LES CADENAS QUI NE PROTEGENT RIEN SONT DITS** — et j'avais cree ce silence moi-meme | `figeesSansPlace` et `placesFigeesSansTitulaire` existaient depuis les lots 1 et 2, **transportes jusqu'a la route et affiches nulle part**. Une place cadenassee puis videe, un creneau disparu du catalogue, une date hors bornes : l'ecran affiche encore un cadenas et le moteur vient de rebattre la case. L'ecart ne se decouvre qu'en comparant deux plannings — donc jamais. C'est exactement la famille de defaut que ce produit combat : pas « il ne sait pas faire », mais « il a fait autre chose que ce que l'ecran raconte » | OK |
| B-111 | Preuves d'execution (lots 3 et 4) | `npx tsc --noEmit` → 0 erreur · `npx vitest run` → **1711 passed**, 1 skipped, 0 echec · `npm run build` → « Compiled successfully in 48s » · `npx eslint` sur les 7 fichiers touches → 0 | OK |
| B-111 | ⚠️ CE QUI N'EST PAS VERIFIE, et c'est la recette de MiKL | **Le rendu n'a jamais ete vu a l'ecran** — je n'ai pas de navigateur. La logique, les types, les tests et le build sont prouves ; l'apparence du cadenas, sa lisibilite au survol, la taille du petit panneau « Fixer » et le comportement sur tablette ne le sont pas. Points les plus susceptibles d'ajustement : l'opacite du cadenas ouvert (0 hors survol) et la largeur du panneau de selection dans une case de calendrier deja dense | ⚠️ **a recetter** |
| B-111 | **LOT 2** — le cadenas EXISTE en base, distinct du verrou systeme | Colonne `gardes.places_figees TEXT[]` (labels des places cadenassees), **verifiee en base** par `information_schema` apres application. Migrations au depot : `20260904075149` et `20260904075251`. Deux booleens `fige_premier`/`fige_second` auraient eu l'air de suffire — ils seraient faux des qu'un cabinet regle son effectif de nuit a 3 ou 4, ce que le produit autorise deja | OK |
| B-111 | 💣 **LA VUE INVERSE LES ROLES UN JOUR SUR TROIS — le cadenas aurait designe la mauvaise personne** | `planning_semaine` materialise le week-end sur 3 jours et INVERSE 1er/2nd sur la ligne du VENDREDI. Exposer `g.places_figees` brut aurait dessine le cadenas sur l'autre place ce jour-la. Les labels suivent donc l'inversion, comme `premier_id`/`second_id`. **MESURE sur la base reelle** : cadenas sur `premier` d'un week-end Fanny (1re) / Antoine (2nd) → la vue rend `{premier}` samedi et dimanche, `{second}` vendredi — ou elle affiche justement Fanny en 2nde. Le cadenas designe la meme personne les trois jours | OK |
| B-111 | 💣 **LES CADENAS AURAIENT DISPARU A CHAQUE REGENERATION** | `ecrirePlanningV1` supprime puis reinsere les gardes non `verrouille` — et une garde cadenassee ne l'est pas. Symptome qui aurait ete le plus trompeur possible : **le planning serait reste JUSTE** (le moteur a bien compose autour), mais les cadenas auraient disparu de l'ecran, et la generation suivante n'aurait plus rien de fixe. L'admin, qui vient de voir son choix respecte, n'aurait eu aucune raison de re-verifier. Les cadenas sont lus AVANT le DELETE et reinjectes a l'insert — 3 tests dedies (`cadenas-survivent-a-la-regeneration.test.ts`) | OK |
| B-111 | Le geste de l'admin : poser / liberer / vider | `POST /api/planning/places-figees`. **`liberer` et `vider` sont deux gestes distincts** — enlever le cadenas d'une garde qu'on garde telle quelle est courant ; perdre la personne au passage ne serait compris par personne. La route CREE la garde si elle n'existe pas (une periode non generee n'a aucune ligne), ce qui manquait au produit. Refuse explicitement les places au-dela de la 2e plutot que d'ecrire dans le vide | OK |
| B-111 | On INFORME, on ne bloque pas — comme demande | Le controle est le MEME juge que la publication (`avertissementsReglesDures` → `validerPlanning`), aucune regle reecrite. Il tourne **apres** l'ecriture et ne renvoie que le delta : l'admin a decide, le produit lui dit ce que sa decision implique, il ne lui demande pas la permission d'enregistrer | OK |
| B-111 | Le cadenas tombe a la publication (arbitrage ④) | `POST /api/publish`, apres la publication reussie, en best-effort assume : un echec ne defait pas une publication. ⚠️ On ne touche PAS a `verrouille` — les confondre aurait retire, au moment meme de la publication, la protection qui doit justement s'installer | OK |
| B-111 | Le cadenas remonte jusqu'au moteur, sans silence possible | `chargerPlacesFigees` (conversion type base → type moteur, avec le piege 'ferie' → 'semaine_soir' documente) · loader · `resoudreContexte` (propagation EXPLICITE : l'objet est reconstruit champ par champ). Une erreur de lecture **interrompt la generation** au lieu d'etre avalee en « aucun cadenas » : regenerer sans eux ecraserait des choix faits a la main. `figeesSansPlace` recense les cadenas qui ne retombent sur aucune place | OK |
| B-111 | Le garde-fou `security_invoker` a REFUSE ma migration, et il avait raison de ne pas se fier au reste | `CREATE ... WITH (security_invoker = true)` fonctionne (verifie : `reloptions` = `security_invoker=true`), mais le test exige la forme explicite `ALTER VIEW ... SET`. **Le controle n'a PAS ete assoupli** pour reconnaitre l'autre ecriture : c'est un `CREATE OR REPLACE` sans l'option qui a ouvert les vues a `anon` le 22/08. Une ligne ajoutee coute moins cher qu'un garde-fou elargi | OK |
| B-111 | Filou : la decision est ecrite, et elle a revele autre chose | `couverture-produit.ts` → `manque` (il ne sait ni poser ni LIRE les cadenas). En l'ecrivant : **B-112 cree** — sa relecture peut proposer de deplacer une place cadenassee, avec l'aplomb de ses autres propositions. A traiter avant de rouvrir le chantier Filou | OK |
| B-111 | Preuves d'execution (lot 2) | `npx tsc --noEmit` → 0 erreur · `npx vitest run` → **1705 passed**, 1 skipped, 0 echec · `npm run build` → « Compiled successfully in 24.2s » · colonne et vue **verifiees en base**, pas sur le `success` de la migration | OK |
| B-111 | ⚠️ CE QUI N'EST TOUJOURS PAS VISIBLE | **Aucun ecran ne montre ni ne pose de cadenas.** Tout est branche de la base au moteur, et le geste existe cote serveur — mais il n'a aucun bouton. **MiKL ne peut toujours pas recetter** : c'est le lot 3 | ⚠️ **lot suivant** |
| B-111 | **LOT 1** — le moteur compose AUTOUR des places cadenassees, au lieu de les ignorer | `src/engine/figees.ts` (nouveau) : `PlaceFigee`, `indexerFigees`, `stepsHorsFigees`, `attributionsDesFigees`, `reposerFigees`, `prioriserCasesFigees`. Champ `placesFigees` sur `SolverInput`. Les figees sont POSEES avant que la recherche commence et retirees de ce qu'il reste a pourvoir — donc tout ce que le moteur pose ensuite les VOIT | OK |
| B-111 | Les places figees comptent dans l'equite et dans les regles de rythme | Prouve par deux tests distincts, pas par lecture du code : ① `espacement_min` de 2 jours + garde figee le lundi → le moteur ne pose PAS la personne le mardi (avant, rien ne l'en empechait) ; ② trois week-ends figes sur la meme personne → le moteur la freine au lieu de la servir comme si elle n'avait rien fait | OK |
| B-111 | Les CINQ chemins qui defont des attributions respectent les cadenas | Seed (`genererSeedGreedy`), LNS (`supprimerSemaine` + `reposerFigees`), remplissage au mieux (`remplirAuMieux`), reprise des cases vides (`essayerDePourvoir`), reequilibrage des roles (`blocFige` — un echange 1er/2nd deplacerait la place cadenassee). Un test par chemin | OK |
| B-111 | 💣 **CE QUE LA MESURE A REVELE — cadenasser un WEEK-END rendait TOUTE generation impossible** | Trouve par sonde, pas par raisonnement : **R9** (« le vendredi soir doit avoir les memes veterinaires que le week-end ») juge un candidat contre l'equipe du creneau lie **des qu'elle existe, sans verifier qu'elle est complete**. Un week-end a moitie cadenasse est une equipe d'UNE personne : le vendredi n'avait plus qu'un candidat, que l'inversion des roles interdisait a son tour. Meme famille que la lecon du 02/09 (« une place a null existe encore pour R9 »). ⚠️ **R9 n'a PAS ete assouplie** — lui faire ignorer les equipes incompletes aurait laisse le vendredi se poser avec des gens qui ne seront jamais du week-end, soit exactement l'incoherence qu'elle existe pour empecher. Correction par l'ORDRE : les places restantes d'une case a moitie cadenassee se decident en premier (`prioriserCasesFigees`), appliquee au seed, aux blocs du remplissage et a la reprise | OK |
| B-111 | La forme rendue reste celle qu'attend l'ecriture en base | `ecrirePlanningV1` lit les places par POSITION (`placements[0]` → `premier_id`). Une attribution qui n'aurait contenu que la place figee aurait ecrit le 2nd dans la colonne du 1er, **sans qu'aucun test d'egalite de contenu ne s'en apercoive**. Les attributions figees sont donc construites comme le solver les construit (`attributionVide` + `avecVet`), et un test verifie l'ordre canonique quand SEUL le 2nd est cadenasse | OK |
| B-111 | Aucune regression quand rien n'est cadenasse | `npx vitest run` → **1702 passed**, 1 skipped, 0 echec (dont les golden tests du moteur, qui exigent le byte-identique) · `npx tsc --noEmit` → 0 erreur · un test compare explicitement le planning rendu avec `placesFigees` absent et vide | OK |
| B-111 | ⚠️ CE QUI N'EST PAS FAIT DANS CE LOT, et se voit | **Rien n'est visible a l'ecran et rien n'est branche en base** : `placesFigees` n'est alimente par aucun appelant, la route `/generate` ne le passe pas encore, et aucun cadenas ne se pose. Le moteur SAIT faire, personne ne le lui demande. C'est l'objet des lots 2 a 4 — **MiKL ne peut donc rien recetter a ce stade** | ⚠️ **lot suivant** |
| B-102 | 2e archivage, demande par MiKL : ramener le board sous le seuil | **110 617 → 76 244 octets** (-33 %), mesure par `wc -c` avant et apres, jamais estimee. 17 recits condenses, 17 convergences consommees deplacees, B-104 resserre | OK |
| B-102 | 💣 **Le critere de la 1re passe ne suffisait plus, et s'y fier aurait range le dossier EN COURS** | Il disait « un recit part des lors que l'item a une convergence ». Or **B-104 a six convergences et reste le bug ACTIF**, cause inconnue. Nouveau critere en DEUX conditions : une convergence **ET** un probleme qui n'existe plus. Le 1er se mesure, le 2e se juge — d'ou une liste d'items **nommee et assumee**, plutot qu'un calcul qui aurait eu l'air objectif en se trompant | OK |
| B-102 | Les items OUVERTS condenses, **jamais deplaces** — la nuance est nette | Identifiant, perimetre et premiere phrase restent au board, donc dans le digest injecte a chaque message ; seul le recit long part a l'archive, avec renvoi. « Un item ouvert dans une archive est un item perdu » (1re passe) reste vrai : ce n'est pas ce qui a ete fait. Verifie : **0 identifiant sorti du board** | OK |
| B-102 | Ne rien perdre | `comm` entre l'inventaire d'avant et celui d'apres : **121 identifiants des deux cotes, liste des perdus VIDE**. Backlog : **50 lignes avant, 50 apres** — verifie apres avoir constate une variation du profil de colonnes | OK |
| B-102 | Une variation de structure EXPLIQUEE, pas ignoree | Le compte des lignes a 7 colonnes passait de 51 a 50. Cause trouvee : certains recits contenaient des `\|` qui **cassaient le tableau markdown** (9 colonnes au lieu de 5). En les condensant, le tableau redevient correct — c'est un gain, pas une perte | OK |
| B-102 | ⚠️ Ce qui n'est PAS fait | Le board **repassera le seuil** : la marge est de 22 Ko sous la limite d'injection, mais chaque journee de recette produit ~10 Ko. **Aucun mecanisme n'empeche la derive** — l'archivage reste un geste manuel que MiKL doit demander. A automatiser si ca devient hebdomadaire | ⚠️ assume |
| B-104 | TROU 1 — la trace s'ouvre desormais AVANT le verrou | `route.ts` : `ouvrirTrace` remonte au-dessus de `acquerir_verrou_generation`. Un refus 409 ferme la ligne avec `issue = 'refusee'` au lieu de ne rien ecrire. **C'est ce qui dira si le verrou refuse sur une generation REELLEMENT en cours, ou sur un verrou fantome laisse par une fonction morte** — deux causes opposees, jusqu'ici indiscernables | OK |
| B-104 | TROU 2 — le navigateur parle meme sans identifiant de trace | Le temoin commencait par `if (!rapport.traceId) return`, au motif que « le silence est alors exact ». **Il ne l'etait pas** : la fenetre de MiKL s'est fermee AVANT la premiere ligne du flux, donc sans `traceId`, et le temoin s.est tu au moment ou il etait le seul a savoir quelque chose. Il porte desormais `periodeId` ; la route cree une ligne `issue = 'abandon_client'` | OK |
| B-104 | Deux etats a ne JAMAIS confondre, et le schema les separe | `fermee_le IS NULL` = le serveur avait commence, il est **mort**. `issue = 'abandon_client'` = le serveur n'a **jamais** commence, seul le navigateur en temoigne. Causes opposees ; les ranger sous un meme symptome, c'est refaire le diagnostic a l'aveugle | OK |
| B-104 | Le type TypeScript et le CHECK SQL alignes, avec la raison ecrite sur place | `VerdictGeneration.issue` etendu ; `tsc` a d'ailleurs **refuse** le premier jet (`'refusee' is not assignable`). Le commentaire dit pourquoi les decaler serait grave : l'ecriture echouerait en silence, et **la trace manquante serait prise pour une fonction morte** | OK |
| B-104 | Migration appliquee et **verifiee en base**, pas sur le `success` | `pg_get_constraintdef` → `CHECK (issue = ANY (ARRAY['complet','partiel','echec','erreur','refusee','abandon_client']))`. Fichier de migration ajoute au depot pour qu'il reste la source de verite | OK |
| B-104 | Preuves d'execution | `npx tsc --noEmit` → **0 erreur** · `npx vitest run` → **1676 passed**, 1 skipped · `npm run build` → « Compiled successfully in 22.1s » · `npx eslint` sur les 3 fichiers → **0 probleme** | OK |
| B-104 | ⚠️ CE QUI RESTE OUVERT — la cause n'est TOUJOURS pas connue | Ces deux correctifs ne reparent **rien** : ils rendent le prochain incident lisible, c'est tout. ⚠️ **La piste Vercel est ECARTEE** — MiKL : *« rien a voir avec Vercel, ce n'est pas la premiere fois »*. Ce qu'on sait desormais : la fenetre se ferme **et** aucune trace ne s'ouvre, donc la fermeture est probablement la CAUSE (l'ecran disparait, le `fetch` est annule avec lui, le serveur ne commence jamais). **Aucune generation reelle relancee depuis** | ⚠️ **ouvert** |
| B-107 | ⚠️ CE QUI N'EST PAS VERIFIE | **Le rendu n'a jamais ete vu a l'ecran.** La logique, les calculs et le build sont prouves ; l'apparence, l'espacement et le comportement reel des replis ne le sont pas — je n'ai pas de navigateur. **C'est la recette de MiKL**, et c'est elle qui dira si « imbuvable » est reellement corrige. ⚠️ Le CSS des `<details>` stylises (fleche, coins arrondis a l'ouverture) est le point le plus susceptible d'avoir besoin d'un ajustement | ⚠️ **a recetter** |
| B-107 | 03/09 — MiKL a recette Hiver P2 regenere : « encore imbuvable », deux captures a l'appui, trois points precis | Trois pistes validees et livrees le meme jour : ① objections du moteur reformulees en phrase naturelle plutot que « (min 2) » technique ; ② la liste brute des 6 mouvements devient un tableau (jour / creneau / echange) ; ③ le motif de Filou separe visuellement des places touchees. `humaniserObjection` et `parserGeste(s)` dans `lib/relecture/resume.ts`, appeles depuis `RapportRelecture.tsx` | OK |
| B-107 | 💣 `Violation.detail` (le texte brut du moteur) N'A PAS ete modifie, et c'est voulu | Ce texte est lu ailleurs dans le produit (`CartesViolations.tsx`, `GardienFilou.tsx`) — le reecrire aurait deplace le probleme au lieu de le resoudre (INSPECTION CONSUMERS). La reformulation vit UNIQUEMENT dans l'affichage de cet ecran, avec repli sur le texte brut si le motif n'est pas reconnu (3 motifs geres : espacement, espacement week-end, repos apres serie) | OK |
| B-107 | Preuves d'execution | 7 tests ajoutes dans `tests/lib/relecture-resume.test.ts` (dont le cas exact de la capture de MiKL, copie mot pour mot) → **18/18 passed** · `npx tsc --noEmit` → 0 erreur · `npx eslint` sur les 4 fichiers touches → 0 probleme | OK |
| B-107 | ⚠️ CE QUI N'EST TOUJOURS PAS VERIFIE | **Le rendu n'a toujours pas ete vu a l'ecran** — meme limite que le 02/09, je n'ai pas de navigateur. Seule la recette de MiKL sur Hiver P2 dira si les trois points signales sont reellement resolus | ⚠️ **a recetter** |
| B-108 | MiKL a repere un delta entre le texte de Filou (27) et l'ecran Compteurs (22), et a demande ce que ca voulait dire | Verifie EN BASE avant toute hypothese : `compteurs_gardes` (source de l'ecran) donne 22 pour Antoine, 13 pour Anne-Sophie. Les 6 lignes de `relectures_planning` de la journee montrent le meme couple errone (27, 17) a chaque fois, sur des plannings pourtant differents a chaque regeneration — le motif reproductible ecarte la coincidence et pointe une erreur de LECTURE du dossier, pas de calcul cote moteur (`compterDansPlanning` correspond structurellement a la vue SQL, verifie sur le type `AttributionGarde` : une seule date par attribution, pas de doublon samedi/dimanche) | OK |
| B-108 | Preuves d'execution | `npx vitest run tests/lib/relecture-dossier-en-texte.test.ts` → **11/11 passed** (nouveau cas verrouille la phrase exacte et interdit `/22 gardes, dont/`) · `npx tsc --noEmit` → 0 erreur · `npx eslint` sur les 2 fichiers → 0 probleme | OK |
| B-108 | ⚠️ CE QUI N'EST PAS VERIFIE | **Aucune relecture reelle relancee depuis le correctif.** La reformulation est un pari raisonnable (rendre l'addition explicitement fausse plutot que juste implicitement incluse) mais seule une nouvelle relecture dira si Filou lit enfin « 22 » — un modele peut retomber dans le meme travers sous une autre forme | ⚠️ **a recetter** |
| B-109 | Verifie EN BASE avant toute hypothese, pas suppose | `dossier -> 'personnesAllegeables'` de la relecture `ffb4efea` contient bien "Antoine", et `dossier -> 'mouvementsParGenre'` compte 108 mouvements legaux dont 2 `refaire_semaine`. `appliques` et `a_trancher` sont VIDES (0 changement au total). Le constat du critere « les gardes ne s'entassent pas sur une seule semaine » porte `corrigeable: true` malgre ca | OK |
| B-109 | Ce qui est corrige, et ce qui NE L'EST PAS | Corrige : la contradiction (promettre sans agir) est desormais VISIBLE a l'ecran au lieu d'un silence qui ressemblait a une action en cours. **NON corrige : Filou ne propose toujours aucun changement.** Le controle attrape le symptome (la promesse fausse), pas la cause (pourquoi il n'utilise aucun des 108 leviers) | ⚠️ **partiel, assume** |
| B-109 | Preuves d'execution | `npx vitest run` (suite complete) → **1687 passed**, 1 skipped · `npx tsc --noEmit` → 0 erreur · `npx eslint` sur les 4 fichiers → 0 probleme | OK |
| B-104 | Poser un traceur, decide par MiKL apres presentation des deux voies | Table `generations_trace` (13 colonnes) + RPC `fermer_trace_generation` + `src/data/tracerGeneration.ts`, cables dans `/api/generate`. **Verifie EN BASE, pas sur la presence du fichier** : `relrowsecurity = true`, policies `cabinet_isolation (RESTRICTIVE)` + `admin_all (PERMISSIVE)`, 2 index, `anon` = **AUCUN DROIT**, RPC en `SECURITY INVOKER` (pas DEFINER) | OK |
| B-104 | 🔦 **La decision qui fait tout : la trace s'ouvre AU DEBUT, pas a la fin** | Une trace ecrite a la sortie ne garde que les generations qui ABOUTISSENT — donc **jamais celles qu'on cherche**. Quand la fonction serverless est tuee, aucun code de sortie ne s'execute : ni le `finally`, ni la liberation du verrou, ni un rapport. **Une ligne ouverte et jamais refermee EST la preuve que la fonction est morte**, et `ouverte_le` en donne l'heure. On lit une ABSENCE, pas une presence | OK |
| B-104 | 💣 **Le serveur seul ne pouvait PAS repondre — d'ou un second traceur, cote navigateur** | Le symptome de MiKL est *« la fenetre se ferme »*, et la lecture a etabli que **tous** les chemins d'erreur du client affichent un message (`ParcoursGeneration.tsx:182, 699`) — **aucun ne ferme la fenetre**. Ce qu'il voit n'est donc AUCUN chemin prevu, et aucune trace serveur ne pouvait l'expliquer. Correctif : le serveur annonce son `traceId` en 1re ligne du flux, le navigateur l'annote par `sendBeacon` (et non `fetch`, **annule** par le navigateur au moment precis d'un demontage). Trois raisons distinctes : `ecran-demonte-pendant-le-travail`, `serveur-injoignable`, `flux-clos-sans-resultat` | OK |
| B-104 | L'instrument ne pese pas sur ce qu'il mesure | `generations_trace` est **volontairement hors de la publication `supabase_realtime`** (verifie : `dans_realtime = 0`). La page planning s'abonne a `gardes` et `periodes` et se rafraichit a chaque ecriture — publier cette table aurait ajoute des rafraichissements pendant l'incident meme qu'on observe | OK |
| B-104 | Le traceur ne peut RIEN casser | `ouvrirTrace` rend `null` en cas d'echec et la generation continue sans etre tracee ; toute erreur est avalee et **journalisee** (`console.error`). Exception assumee a « une erreur avalee devient zero ligne » : ici zero ligne est le bon repli, mais l'echec doit rester visible — une table vide et une table jamais ecrite se ressemblent trop | OK |
| B-104 | Les `ref` plutot que des `useState`, et c'est necessaire | Les valeurs du temoin sont lues dans le NETTOYAGE d'un effet, au demontage. Un `useState` y serait fige a sa valeur du dernier rendu — donc faux au moment precis ou il doit etre juste | OK |
| B-104 | Le garde-fou « Filou suit le produit » a MORDU, et c'est la preuve qu'il vit | La suite a **refuse** le premier jet : `api/generate/incident#POST` ajoutee sans decision declaree → 1 test rouge nommant la route. Decision ecrite (`hors` : instrument de mesure, aucun geste metier) → **1665 passed**. La regle du 25/08 fonctionne sans qu'on ait eu a y penser | OK |
| B-104 | Preuves d'execution | `npx tsc --noEmit` → **0 erreur** · `npx vitest run` → **1665 passed**, 1 skipped · `npm run build` → « Compiled successfully in 22.0s », **route `/api/generate/incident` presente dans la sortie du build** · `npx eslint` sur les 4 fichiers → **0 probleme** | OK |
| B-104 | ⚠️ CE QUI N'EST PAS FAIT, ET QUI COMPTE | ① **Le traceur n'a JAMAIS tourne** — aucune generation reelle depuis sa pose, donc **zero ligne mesuree**. Il est prouve par les tests et le build, pas par un incident capture. ② **Il ne CORRIGE rien** : c'est un instrument, le bug de MiKL est entier. ③ Aucun ecran ne lit ces traces — la lecture se fera par requete SQL, par moi. ④ Le verrou de 3 minutes n'a pas ete touche : tant que la cause est inconnue, le raccourcir ne ferait que masquer le symptome | ⚠️ **assume et trace** |
| B-103 | ⚠️ Un point NON traite, signale au lieu d'etre absorbe | `crise/reparer.ts` (depannage apres publication) ne filtre pas le dernier recours : il le **propose** dans la liste des candidats, penalise donc classe dernier, et l'admin tranche. C'est conforme a la regle (rien n'est pose automatiquement) mais le champ `meilleur` peut le recommander s'il est le seul legal. **A arbitrer par MiKL** — pas touche | ⚠️ **ouvert** |
| B-103 | ⚠️ Ce qui n'est PAS verifie | **Aucune relecture reelle relancee depuis le correctif.** On ne sait donc pas si Filou allege encore Antoine une fois Anne-Catherine retiree des candidats — la reconstruction peut rendre `null` plus souvent sur les semaines saturees. C'est la recette de MiKL, et le planning actuel est **a jeter** (5 poses d'Anne-Catherine) | ⚠️ a recetter |
| B-101 | ⚠️ Ce qui n'est PAS fait | ① **Aucune relecture reelle relancee** — on ne sait pas si Filou prendra ce mouvement. ② La reconstruction cherche une solution LEGALE, pas la MEILLEURE : c'est `effetsDesMouvements` qui dira si elle vaut le coup, et elle peut sortir marquee DEGRADE. ③ Plafond de 200 000 poses, non atteint sur le cas reel mais **jamais mesure sur une periode d'ete (17 semaines)**. ④ B-097 (la revue decrit le planning d'AVANT) reste entier | ⚠️ a recetter |
| B-096 | ⚠️ Ce que le lot E n'a PAS resolu, et qui reste entier | **Antoine a toujours 5 week-ends apres cinq relectures.** Le lot E ne corrige rien — il rend la prochaine mesure possible, c'est tout. Et un fait nouveau du 5e rapport reste inexplique : Filou ecrit « 8 mouvements qui ameliorent, TOUS des inversions de role », donc la chaine n'etait pas dans sa liste, alors qu'elle y etait dans le test sur le planning de 12h. Le planning a ete modifie par les relectures successives entre-temps. **Prochaine relecture = la reponse, sans deviner** | ⚠️ **ouvert** |
| B-096 | ⚠️ Ce qui n'est PAS verifie, et se dit | ① **Aucune relecture reelle n'a ete relancee.** Zero mesure de ce que Filou fait avec tout ca — c'est la recette de MiKL, et la seule qui compte. La table de trace existe justement pour la rendre mesurable. ② `tracerRelecture` **n'a jamais ecrit une ligne** : la table a ete verifiee vide et conforme, l'insertion n'a pas ete exercee. ③ Une erreur eslint PREEXISTANTE subsiste dans `src/components/conges/CongeForm.tsx` — fichier non touche, hors perimetre, signalee et non corrigee | ⚠️ a recetter |
| B-096 | ⚠️ **ET LE RESULTAT METIER EST NUL — il faut le dire aussi** | ① **Les lundis de Victor : 4 avant, 4 apres** (→ B-098). ② **Le role de 1er de week-end : ecart max−min de 2 avant, 2 apres** — Victor passe de 2 a 3, Manon de 2 a 1, le desequilibre se deplace au lieu de se reduire. Le motif disait « Victor, qui l'a moins souvent que d'autres » : il en avait autant que Manon. ③ **Le desequilibre d'origine de MiKL est INTACT** : Antoine toujours 5 week-ends, Fanny toujours 3. ④ **4 changements sur 5 DEGRADENT le score** et ont ete appliques quand meme — c'est permis (seule la legalite fait veto, arbitrage du 27/08), mais 4 sur 5 est un signal, pas un detail. **Filou a enfin des leviers ; il les utilise mal.** Les cinq lots etaient necessaires et ne suffisent pas | ⚠️ **partiel — B-097 et B-098 ouverts** |
| B-096b | ⚠️ Ce qui n'est PAS verifie | **Aucune relecture reelle relancee depuis ces trois correctifs.** On ne sait donc pas si Filou allege enfin Antoine — c'est la recette de MiKL. Le plafond de 400 est un chiffre CHOISI, pas mesure contre la qualite des reponses : il tient le temps et la lisibilite, personne n'a verifie qu'il ne coupe pas quelque chose d'utile. **B-097 (la revue decrit le planning d'AVANT) reste entier** | ⚠️ a recetter |
| B-096 | ⚠️ Limites assumees dans le code | ① Un echange entre une place de WEEK-END et une place de SEMAINE n'est pas genere (la personne sortante devrait reprendre deux creneaux contre un). ② Une grappe a plus de 2 personnes n'est pas « inversee » — « inverser » n'a de sens univoque qu'a deux. ③ Les penalites CABLEES du moteur (2 WE consecutifs, WE avant vacances, fete de fin d'annee) n'ont pas de `force` a requalifier et ne remontent pas dans les preferences enfreintes | ⚠️ assume et trace |
| B-093 | ⚠️ Ce qui n'est PAS verifie | **Aucune relecture reelle n'a ete relancee.** On ne sait donc pas encore combien des 7 constats deviennent des propositions appliquees — c'est la recette de MiKL, et c'est la seule mesure qui compte | ⚠️ a recetter |
| — | **Les 33 convergences anterieures au 01/09 sont archivees** — B-092, B-091, B-086, B-087, B-078 a B-081, B-076, B-077 | Detail integral → archive 19 | archive |

**Regles :**

- **Une ligne par case cochee** au tableau KIT COMPLET, ou par element de l'item du board.
- **Verdict** : `OK` · `partiel` · `absent`.
- **Verifier par la preuve, jamais de memoire** — un grep, un fichier ouvert, un test passe.
  Ecrire « livre » parce qu'on se souvient l'avoir code est exactement l'erreur que ce
  controle existe pour attraper.
- **Partiel ou absent -> ca ne commite pas.** Deux issues, jamais trois : finir maintenant, ou
  creer le reste-a-faire au board (`B-012` -> `B-012a`, avec son perimetre) et le dire a MiKL.
- **Le reste-a-faire porte le numero d'origine** : la filiation doit rester lisible six mois plus tard.
- Ne pas refaire une ligne a l'identique d'un commit a l'autre : le harnais le detecte et refuse.

> Cette section s'archive avec le « Livre » — c'est du passe verifie, reconstituable.
>
> **Archivages du 2026-08-27, deux passes le meme jour** — la journee a produit huit
> lots, et le board a franchi le seuil deux fois.
> · **1re passe** (matin) : convergences anterieures a `082f04e` → archive, section 14
>   (77 lignes, 23 517 octets). Board 101 920 → 78 403.
> · **2e passe** (soir) : convergences de `082f04e` a `45978d3` → archive, section 15
>   (60 lignes, 23 265 octets) ; **et onze recits INBOX d'items DEJA LIVRES** (B-062,
>   B-063, B-064, B-070 a B-077) → archive, section 16, resumes en une ligne dans
>   « Livre ». Board 109 593 → 73 872.
>
> ⚠️ **Ce que la 2e passe a appris** : la section 4bis n'etait pas le vrai poids. L'INBOX
> l'etait — elle gardait, en texte integral, des items **livres dans la journee**. Une
> inbox qui conserve ce qui est fait n'est plus une inbox : c'est une archive qui se
> presente comme une liste de travail, et elle pesait a elle seule un tiers du board.
> Le reflexe a garder : quand un item passe a « Livre », son recit part a l'archive le
> jour meme, et le board n'en garde qu'une ligne.
>
> **Archivage du 2026-09-02** — board **161 152 → 89 057 octets** (-45 %), cinq lots :
> section 9 integrale (→ 18) · convergences du 27 au 31/08 (→ 19) · recits de « Livre »
> (→ 20) · recits INBOX et Backlog d'items livres (→ 21) · convergences deja consommees
> par un commit (→ 22). **0 identifiant perdu sur 115**, verifie par `comm` entre l'avant
> et l'apres, pas par relecture.
>
> ⚠️ **Ce que cette passe a appris** : le hook conseillait d'archiver « Livre et Ecarte ».
> Mesure faite section par section, « Ecarte » pesait **800 octets** — l'appliquer aurait
> rendu 10 % de ce qu'il fallait. Le poids etait dans les DEUX sections Convergence
> (48 Ko a elles deux). *Mesurer avant de couper, meme quand l'outil dit ou couper.*
>
> ⚠️ **Le critere retenu pour deplacer un recit, et il est verifiable** : l'item a-t-il au
> moins une ligne de convergence ? Si oui, du code est livre, le recit peut partir. Sinon
> il n'a jamais ete developpe et **rien n'est touche** — c'est ce qui a protege B-095,
> B-085, B-083, B-080, B-049, B-051 et dix autres. Un item ouvert dans une archive est
> un item perdu.

---

## 4. Livre

| ID | Titre | Commit | Date | Perimetre |
|---|---|---|---|---|
| B-102 | **Archivage du board — 161 152 → 89 057 octets (-45 %), demande par MiKL le 02/09.** Cinq lots (→ archive 18 a 22). **0 identifiant perdu sur 115**, prouve par `comm`. Critere verifiable : un recit ne part que si l'item a une convergence, donc du code livre — les 17 items sans convergence sont intacts. Detail → journal, section 8. | (ce commit) | 2026-09-02 | Interne |
| B-096b | **Aucun mouvement livre le matin du 02/09 ne pouvait ALLEGER quelqu'un** — echanges et inversions sont des PERMUTATIONS, le total d'Antoine ne pouvait pas baisser. Genre `remplacement_weekend` ajoute. Recit et convergences → archive 21 et 22. | `558ab98` | 2026-09-02 | Interne |
| B-096c | **Un week-end est ENCADRE : il fallait six places, pas quatre** — le vendredi le precede (meme binome), le lundi le suit depuis B-092 ; sur six veterinaires il ne restait personne. Genre `remplacement_weekend_en_chaine`, prouve sur le planning REEL. Convergences → archive 22. | `a8b321b` | 2026-09-02 | Interne |
| B-078 | **L'agenda Google eclate : un evenement par personne ET par jour**, en journee entiere, avec la couleur Google et le nom choisis par l'admin, et un… Detail → archive 20. | `e51905a` | 2026-08-27 | A qualifier |
| B-079 | **L'agenda affichait 20h-10h la ou la structure dit 18h-08h** — bug de fuseau, PROUVE par sonde avant correction (sous UTC : 20:00/10:00, exactement… Detail → archive 20. | `e51905a` | 2026-08-27 | Interne |
| B-081 | **Quatre lecteurs devenus aveugles aux nouveaux evenements** — une regeneration aurait laisse des orphelins dans l'agenda, et le compteur admin… Detail → archive 20. | `e51905a` | 2026-08-27 | Interne |
| B-062 | **LA RELECTURE DE FILOU — la derniere couche, livree en 5 lots le 27/08.** Detail → archive 20. | `e80dcac` | 2026-08-27 | A qualifier |
| B-070 | **La progression de Filou etait affichee et INVISIBLE**, sous plusieurs ecrans de cases a pourvoir. Le code etait juste, le placement le rendait nul.… Detail → archive 20. | `62a5a36` | 2026-08-27 | Interne |
| B-071 | **Filou se taisait parce que MON PROMPT le faisait taire.** Detail → archive 20. | `f707468` | 2026-08-27 | Interne |
| B-072 | **Un anneau tourne pendant la relecture.** La generation avait deja sa jauge ; c'est l'attente de Filou qui restait figee. Detail → archive 20. | `f707468` | 2026-08-27 | Interne |
| B-073 | **Le rapport devient lisible** — « a vomir », disait MiKL, et le fond etait pourtant juste. **Des UUID bruts s'affichaient** (B-023 qui revient)… Detail → archive 20. | `98b98e0` | 2026-08-27 | Interne |
| B-074 | **La relecture se deroule DANS le parcours**, plus apres : « la personne peut fermer avant d'avoir vu la conclusion ». L'ecran ne rend la main qu'une… Detail → archive 20. | `98b98e0` | 2026-08-27 | Interne |
| B-075 | **Filou sait enfin QUI PEUT ALLER OU, donc il peut proposer.** Detail → archive 20. | `45978d3` | 2026-08-27 | A qualifier |
| B-076 | **Bouton « Alterner les deux roles »** dans la modale de garde. Il n'enregistre rien : il echange les selections, l'admin valide, et l'inversion… Detail → archive 20. | `73e7b06` | 2026-08-27 | A qualifier |
| B-077 | **« Quelles gardes ce jour-la ? Par defaut, toutes »** — l'explication passe AVANT les cases. Plus deux mots colles corriges (« régléesur », «… Detail → archive 20. | `73e7b06` | 2026-08-27 | Interne |
| B-063 | **Nouvelle regle « eviter les gardes la veille d'un repos »** — moteur, catalogue, saisie, libelle, validateur, couverture Filou, tests. Detail → archive 20. | `ca855e6` | 2026-08-26 | A qualifier |
| B-064 | **Filou ne voyait AUCUNE preference restee au defaut** — une preference au defaut n'a aucune ligne en base. Il repondait « rien ne le prevoit » sur… Detail → archive 20. | `63aa4cb` | 2026-08-26 | Interne |
| B-066 | **Horodater les demandes de conges et d'absences.** Detail → archive 20. | `a4fe5c4` | 2026-08-27 | A qualifier |
| B-067 | **Quatre ordres de lecture pour les conges**, et ils commandent les DEUX listes de l'onglet. Ne fait pas qu'ajouter un choix : supprime une… Detail → archive 20. | `a4fe5c4` | 2026-08-27 | A qualifier |
| B-068 | **« Organisation » ouvre sur Regles**, plus sur Periodes types. Une ligne. Les liens qui visent vraiment une periode type portent deja leur parametre. **A RECETTER.** | `6725a24` | 2026-08-27 | A qualifier |
| B-069 | **« Places » dit enfin que c'est un PLAFOND.** Detail → archive 20. | `6725a24` | 2026-08-27 | A qualifier |
| B-065 | **La semaine a cheval sur deux mois s'affiche entiere — avec son contenu.** Detail → archive 20. | `a venir` | 2026-08-26 | A qualifier |
| B-045 | **Le repos conditionnel n'exige plus ses deux volets.** Detail → archive 20. | `a venir` | 2026-08-26 | A qualifier |
| B-044 | **Le produit dit « nuit » la ou il disait « jour ».** Detail → archive 20. | `a venir` | 2026-08-26 | A qualifier |
| B-043 | **Les demi-journees retirees du produit — elles annoncaient une portee que le moteur n'appliquait pas.** Detail → archive 20. | `a venir` | 2026-08-26 | A qualifier |
| B-041 | **Une regle de repos porte desormais PLUSIEURS jours.** Detail → archive 20. | `a venir` | 2026-08-26 | A qualifier |
| B-042 | **Filou distingue un conge ponctuel d'un repos permanent.** Detail → archive 20. | `a venir` | 2026-08-26 | A qualifier |
| B-040 | **Filou demandait son prenom a quelqu'un de connecte.** Detail → archive 20. | `a venir` | 2026-08-26 | Interne |
| B-039 | **Le bloc « Apercu » disait visuellement « rien ici ».** Detail → archive 20. | `a venir` | 2026-08-26 | A qualifier |
| B-038a | **L'apercu ne montrait pas la parite qu'on venait de choisir.** Detail → archive 20. | `a venir` | 2026-08-26 | Interne |
| B-038 | **Repos fixe « une semaine sur deux » — saisissable a l'ecran et par Filou.** Detail → archive 20. | `a347632` | 2026-08-26 | A qualifier |
| B-036 | **⚠️ Filou visait la MAUVAISE regle — vu en demonstration devant la cliente.** Detail → archive 20. | `f388974` | 2026-08-26 | Interne |
| B-034 | **Le lien « Je prends ce creneau » ne disait pas a QUI il avait ete envoye.** Detail → archive 20. | `9ab90df` | 2026-08-26 | A qualifier |
| B-017 | **Role SECRETAIRE — livre et complet le 25/08.** Detail → archive 20. | `23fd1b2`…`b1eee27` | 2026-08-25 | Interne |
| B-005 | **REGLE PERMANENTE : le tableau ne peut plus se taire sur ce qui attend quelqu'un.** Detail → archive 20. | `20260825190000` | 2026-08-25 | Interne |
| B-020 | **Les 2 erreurs rouges du Security Advisor, ouvertes depuis le 4 aout.** Detail → archive 20. | `20260825191000` (non joue) | 2026-08-25 | Interne |
| B-002 | Selecteur de couleur libre livre (degrade/teinte/hexa/pipette). 27 fichiers, 1223 tests verts, build vert. A recetter a l'ecran. Detail → archive 20. | `9aa6676` | 2026-08-24 | A qualifier* |
| B-002 | Recette MiKL du 25/08 : la fiche veto restait desequilibree — le champ couleur ouvrait une rangee a lui seul, quatre colonnes vides a sa droite. Les… Detail → archive 20. | `314638e` | 2026-08-25 | A qualifier* |
| B-016 | **Onglet SUPPORT livre `9bfaf99` + `9ec3a36` + `d385b46` — RECETTE VALIDEE** (« ca marche nickel », e-mail recu, 3 captures verifiees en base). Depot… Detail → archive 20. | `9bfaf99` | 2026-08-25 | Interne |
| B-004 | Clos SANS developpement : la regle `eviter_we_avant_vacances` existe deja et tourne par defaut a l'etage « A eviter ». A durcir en « Jamais » avec… Detail → archive 20. | — | 2026-08-24 | Interne |
| B-001 | Couleur des autres vetos : ne se reproduit plus (retestte OK par MiKL). Cause non prouvee — a rouvrir si ca revient. | — | 2026-08-24 | Interne |
| — | Gardien des regles sur les 4 chemins d'ecriture d'une garde | `c829d5a` | 2026-08-22 | LOT 1 |
| — | Dimanche juge comme le samedi | `490bb67` | 2026-08-22 | LOT 1 |
| — | Une seule voix pour les regles enfreintes (4 ecrans unifies) | `c829d5a` | 2026-08-22 | LOT 1 |
| — | Faille : les vues s'ouvraient au visiteur non connecte | `490bb67` | 2026-08-22 | Securite — LOT 1 |
| — | Banc d'essai du solver — refusait les scenarios avec conges | `e22c84c` | 2026-08-22 | — |
| — | Retrait d'un planning publie, agenda compris, 2 confirmations | `e1c7eef` | 2026-08-24 | — |
| — | Fiche veterinaire sans adresse e-mail | `f0e999e` | 2026-08-24 | — |
| — | Cabinet bac a sable clonable, isolation prouvee | `e1c7eef` | 2026-08-22 | — |
| — | Historique ete refait (7-20 septembre, soirs de semaine inclus) | base | 2026-08-22 | LOT 3 |

---

## 5. Ecarte et gele — registre des decisions

| Titre | Decision | Raison | Decide par | Date | Reouvrable ? |
|---|---|---|---|---|---|
| Console support dans le hub MonProjetPro | Reporte | Le support passe par e-mail en attendant le hub et ses API | MiKL | 2026-08-22 | Oui, des que le hub existe |
| Chat entre vetos | Ecarte (V3) | Decide avec Anne-Sophie | Anne-Sophie/MiKL | 2026-08-21 | Oui, en V3 |
| Import de planning | Eteint volontairement (code intact, flag `IMPORT_PLANNING_ACTIF`) | Devient une prestation payante — detail chiffre → archive 5bis / `decisions-produit.md` | MiKL | 2026-08-18 | Oui |
| Role secretaire (lecture seule) | Gele | Pas en meme temps qu'un chantier RLS (dette `security_invoker`) — detail → archive 5bis | MiKL | 2026-08-22 | Oui, hors chantier RLS |

---

## 6. Hors devis a chiffrer

> Section a reconfirmer par MiKL depuis la clarification du 24/08 (GuardVeto = produit MPP en
> abonnement, pas un projet au forfait) : reste-t-elle pertinente telle quelle, ou ces items
> relevent-ils du roadmap produit `Interne` inclus dans l'abonnement ?

| ID | Titre | Demande par | Date demande | Statut chiffrage |
|---|---|---|---|---|
| B-003 | Compteur de jours a recuperer (formation pendant conges) — A qualifier*, voir section 3 | Anne-Sophie | 2026-08-24 | A chiffrer |

B-004 retire de cette section le 24/08 : clos sans developpement (voir section 4), rien a
chiffrer.

---

## 7. En attente d'action externe

| Quoi | Qui doit agir | Bloque quel item | Depuis |
|---|---|---|---|
| **① RECETTE A FAIRE — les nouvelles fiches du tableau apparaissent-elles SANS rafraichir ?** Ouvrir l'accueil sur le deploiement, faire deposer une demande de conge (ou proposer un echange) depuis un AUTRE compte, et regarder l'ecran sans y toucher. **C'est le seul point de B-005 qui n'a pas pu etre verifie cote dev**, et c'est celui qui repond a la question d'origine de MiKL (« pourquoi je ne le sais que si je demande ? »). Si rien n'apparait : la cause n°1 est la publication `supabase_realtime` — mais elle a ete verifiee en base le 25/08 (9 tables), donc regarder plutot la console du navigateur. | MiKL | B-005 | 2026-08-25 |
| **② Activer la protection contre les mots de passe compromis** (Supabase → Authentication → Policies → « Leaked password protection »). Interrupteur de console, aucun code : MAX ne peut pas le faire a la place de MiKL. Verifie chaque mot de passe contre HaveIBeenPwned au moment ou il est defini. **Seul avertissement de securite qui demande une action humaine** — les 26 autres sont du code. | MiKL | B-020 | 2026-08-25 |
| ~~Decider si B-005 (page Absences, LOT 2) se lance maintenant~~ — **tranche le 25/08 : lance et livre.** | MiKL | B-005 | 2026-08-24 |
| **③ RECETTER LA REPARATION D'UNE ABSENCE (T-006, livre le 26/08).** Declarer une absence sur un planning publie, choisir un remplacant qui enfreint une regle du cabinet : une fenetre « Reparation a confirmer » doit s'ouvrir et lister ce qui est enfreint, avec « Appliquer quand meme ». **Point d'attention** : si la fenetre ne s'ouvre JAMAIS, ce n'est pas qu'il n'y a rien a signaler — c'est le symptome du piege `onClick` decrit dans T-006. Si aucune regle n'est enfreinte, le comportement d'avant est inchange (application directe). | MiKL | T-006 | 2026-08-26 |
| **④ ARBITRER T-002 — que doit dire le bandeau sur un jour exceptionnellement remplace ?** La re-validation juge aujourd'hui le titulaire d'origine. La corriger sans trancher cette question ferait apparaitre des violations fantomes. Aucune urgence, mais rien ne peut avancer sans la reponse. | MiKL | T-002 | 2026-08-26 |
| **⑥ QUESTION FORGE (T-008) — `lib/realtime/statut-abonnement.ts` doit-il remonter au catalogue ?** Ma recommandation : **NON, et la question se pose a l'envers.** Ce fichier n'est pas une brique nouvelle : c'est un EXTRAIT du kit `realtime-refresh-supabase-next`, deja inscrit au catalogue le 25/08 — le garde-fou vient de lui, il n'y a rien a lui rendre. Ce qui reste vrai, c'est que GuardVeto n'a repris **qu'une** des quatre protections du kit (la detection d'echec, la plus couteuse). Les trois autres — relecture au retour d'onglet, suffixe unique de canal, singleton du client navigateur — restent a reporter. **La question utile n'est donc pas « extraire ? » mais « finit-on le report ? »**, et elle n'est pas urgente : les 5 abonnements ne se taisent plus, ce qui etait le seul defaut a consequence silencieuse. | MiKL | T-008 | 2026-08-26 |
| **⑤ POSER LA QUESTION DES FERIES EN SEMAINE A ANNE-SOPHIE (T-005).** Un ferie qui tombe un mardi doit-il etre couvert comme un dimanche, comme un soir de semaine, ou selon un reglage ? A joindre aux 6 questions de B-003 deja en attente. | MiKL / Anne-Sophie | T-005 | 2026-08-26 |

---

## 8. Journal des decisions et changements de perimetre

| Date | Evenement | Items | Decide par |
|---|---|---|---|
| 2026-09-03 | **2e archivage du board : 110 617 → 76 244 octets** (-34 %), demande par MiKL. ⚠️ **LE CRITERE A DU EVOLUER, et c'est la lecon.** La 1re passe deplacait un recit des lors que l'item avait une convergence — donc du code livre. **Insuffisant** : B-104 a six convergences et reste le bug ACTIF ; l'archiver aurait range le dossier en cours. Nouveau critere en DEUX conditions : **une convergence ET un probleme qui n'existe plus** — le premier se mesure, le second se juge, d'ou une liste nommee et assumee. 🔎 **Nuance ajoutee** : les 17 items encore OUVERTS ont vu leur RECIT condense, **sans etre deplaces** — identifiant, perimetre et premiere phrase restent au board, donc dans le digest. « Un item ouvert dans une archive est un item perdu » reste vrai ; ce n'est pas ce qui a ete fait. **Preuve : 121 identifiants avant et apres, 0 perdu, 0 sorti du board** (`comm`). | Tous | MiKL / OTTO |
| 2026-09-02 | **Archivage du board : 161 152 → 89 057 octets** (-45 %), demande par MiKL. Cinq lots → archive 18 a 22. **0 identifiant perdu sur 115**, prouve par `comm` entre l'inventaire d'avant et celui d'apres, jamais par relecture. Critere de deplacement **verifiable** : un recit ne part que si l'item a au moins une ligne de convergence — donc du code livre. Les 17 items sans convergence (B-095, B-085, B-083, B-080, B-049, B-051…) n'ont pas ete touches : **un item ouvert dans une archive est un item perdu.** Le hook conseillait « Livre et Ecarte » ; la mesure a montre qu'« Ecarte » pesait 800 octets et que le poids etait dans les deux sections Convergence. | B-102 | MiKL / OTTO |
| 2026-08-27 | **La recette de la relecture de Filou se fait sur les VRAIES donnees de Val d'Allier, avec l'accord d'Anne-Sophie.** MiKL : « je peux le faire directement sur ma cliente elle est ok ». Perimetre de risque nomme avant de commencer : la relecture **refuse tout planning publie ou verrouille** (409), elle ne peut donc toucher qu'un brouillon que l'equipe ne voit pas ; elle emprunte le meme chemin d'ecriture que la generation, donc les gardes **verrouillees sont preservees** et l'agenda Google n'est pas touche ; et une regeneration ecrase tout, ce qui rend l'operation reversible. **Seul point non reversible : aucun.** ⚠️ Le code n'a jamais tourne sur un appel Opus reel — c'est ce que cette recette verifie en premier. | B-062 | MiKL + Anne-Sophie |
| 2026-08-26 | **Fausse alerte sur B-036, et une lecon d'enquete.** Apres le correctif, Filou annonce « une regle n°16 : Victor ne fait pas de garde le mardi, en interdiction ferme » — MiKL ne la reconnait pas et croit le correctif rate. **Sonde executee (`phraseRegle` appele sur la ligne reelle) : le code rend bien « le lundi »**, donc il ne s'agissait pas de cette regle. Verification en base : la regle decrite existe telle quelle, mais dans le cabinet **du Val d'Allier**, en 16e position exactement. **MiKL etait sur le cabinet REEL de la cliente, pas sur le bac a sable** — et toutes les analyses de la matinee (remplacants du 9 septembre, candidats a l'appel aux volontaires, e-mails) portaient sur « Demo MonProjetPro ». Les deux cabinets ont diverge : le repos du lundi de Victor y est `si_possible` d'un cote, `sauf_crise` de l'autre. **Lecon : etablir sur QUELLES donnees un ecran tourne avant de l'expliquer.** Une analyse juste sur le mauvais jeu de donnees est indiscernable d'une analyse fausse. | B-036 | MAX |
| 2026-08-26 | **Session de soldage — et la lecon du jour n'est aucun des correctifs.**  Detail integral → archive 12. | tous | MiKL + MAX |
| 2026-08-25 | **FORGE — le mecanisme « l'ecran se met a jour tout seul » entre au catalogue des modules reutilisables.**  Detail integral → archive 12. | FORGE, catalogue | MiKL |
| 2026-08-24 | **GuardVeto est un produit MonProjetPro** ; les clients (dont Val d'Allier) y accedent par abonnement. Repo `MonprojetPro/guardveto` confirme au bon endroit, la regle projet-client→comptes-client ne s'applique pas. Corrige une question ouverte a tort par OTTO en section 7. | Gouvernance (en-tete) | MiKL |
| 2026-08-24 | « B-009 confirme reel puis corrige (`360794a`). Une verification faite pendant que le correctif etait en cours l'avait conclu a tort faux positif — verifier l'horodatage du depot, pas seulement son contenu.  Detail integral → archive 12. | B-009, B-013, B-014 | OTTO + team-lead |
| 2026-08-24 | B-004 clos sans dev — regle deja active dans le moteur, a durcir avec Anne-Sophie. Detail → archive 8bis. | B-004 | MiKL |
| 2026-08-24 | B-003 — 6 questions metier posees a Anne-Sophie avant tout code. Detail → archive 8bis. | B-003 | En attente d'Anne-Sophie |
| 2026-08-24 | Selecteur de couleur livre (27 fichiers) + 2 fix critiques publies (retrait planning publie, fiche veto sans email). Detail → archive 8bis. | Livre, B-002 | ruflo |
| 2026-08-24 | Reconciliation OTTO (2e passage) : board condense sous la limite d'injection (16,6 → archive), surplus des sections 3/4/5/8 deplace sans perte | toutes | OTTO |
| 2026-08-24 | Reconciliation OTTO (1er passage) : board aligne sur le template, dates de commit corrigees, LOT 2 (B-005) detecte comme chantier oublie | B-005, Livre | OTTO |
| 2026-08-22 | Recette 21/08 → plan en 6 lots ; lots 1, 3, 4 (reporte), 5 (gele) traites, lot 2 laisse de cote. Detail → archive 8bis. | Livre, B-005 | MAX |
| 2026-08-22 | Mise en service Val d'Allier engagee | Backlog | Anne-Sophie/MiKL |
| 2026-08-18 | Reprise d'historique retiree, devient prestation payante — detail → `decisions-produit.md` | Section 5 | MiKL |

---

## 9. Convergence — a-t-on livre ce qui etait annonce ?

> Controle de sortie, avant chaque commit. Une ligne par element annonce, verifie
> PAR LA PREUVE (un grep, un fichier ouvert, un test passe). Ecrire « livre » de
> memoire est exactement l'erreur que ce controle existe pour attraper.

| ID | Ce qui etait annonce | Ce qui est reellement livre | Verdict |
|---|---|---|---|
| — | **Les 51 convergences de cette section sont archivees** — B-027, B-031, B-089, B-090, toutes livrees et tranchees | Detail integral → archive 18 | archive |

> **Les 22 convergences anterieures sont archivees** (→ archive 13). Une
> convergence a rempli son role au moment du commit : la garder ici ferait grossir
> le board sans que personne ne la relise. Elle reste consultable, datee et entiere.

## Conventions

- **Identifiants** : `B-nnn` (idees et bugs, convention deja etablie sur ce projet) · `T-nnn`
  (dette technique, introduit le 24/08). Jamais reutilises, jamais effaces.
- **Aucune ligne de code sans ID** quand un ID existe ; les commits techniques sans item dedie
  restent traces par leur hash dans la section Livre.
- **Perimetre par defaut** : `A qualifier`. GuardVeto est un produit MPP en abonnement (decide
  par MiKL le 24/08) : la regle projet-client→comptes-client ne s'applique pas, la majorite du
  dev est `Interne`. `A qualifier*` marque un item ne d'un besoin specifique de Val d'Allier
  (premier client abonne) dont le perimetre commercial n'est pas encore tranche — a confirmer
  par MiKL au cas par cas, jamais suppose par OTTO.
- **Le board tient en une page.** Au-dela de la limite d'injection, le surplus part dans
  `00-product-board-archive.md` (sections `Nbis`) — jamais de perte, jamais de suppression d'ID.
- **Une decision qui n'est pas ici n'existe pas.**

---

*Template MPP applique le 2026-08-24 par OTTO. Board condense le 24/08 (2e passage) pour tenir
sous la limite d'injection du hook OTTO — voir `00-product-board-archive.md` pour le detail
integral de chaque entree condensee.*
