# Product Board — GuardVeto

> Noyau et memoire du projet. Une decision qui n'est pas ici n'existe pas.
> Tenu par OTTO. Le PRD (`01-prd.md`) porte la vision de fond ; ce board porte la vie du projet.
> **Regle d'or : un identifiant est immortel. On change son statut, jamais son existence.**
> Detail complet des entrees condensees ci-dessous → `docs/00-product-board-archive.md`.

| | |
|---|---|
| Projet | GuardVeto |
| Proprietaire | **MonProjetPro** (produit propre, meme modele que `foxeo-one` : MPP possede la base technique). Le Cabinet veterinaire du Val d'Allier (Anne-Sophie) est un **client abonne**, pas proprietaire — decide par MiKL le 24/08, tranche apres une question ouverte a tort par OTTO. Repo `MonprojetPro/guardveto` confirme au bon endroit. |
| Phase | Build → bascule en cours vers la mise en service reelle chez le premier client abonne, Val d'Allier |
| Derniere mise a jour | 2026-08-31 par OTTO (B-089 lot 1 : la relecture n'avait jamais choisi son niveau d'application — banc de mesure livre, MiKL tranche · bac a sable remis a l'identique du compte client) |
| Prochain jalon | Mise en service Val d'Allier (rollout, section 3) — pas de date cible fixee |

**Perimetre par defaut : `A qualifier`.** Jamais `Devis` par defaut.

---

## 1. Vision en 5 lignes

GuardVeto genere et publie le planning de gardes d'un cabinet veterinaire (7 veterinaires chez
Val d'Allier, dont un « dernier recours ») a partir de regles configurables — equite,
frequences, enchainements, absences — resolues par un moteur de contraintes (deux gardiens
independants : moteur + validateur). Filou est l'assistant IA porte-parole de ces regles
(jamais decisionnaire). Reussi si : le cabinet publie un planning sans intervention manuelle
ni violation de regle dure, et si Anne-Sophie mene la mise en service sans secours de MiKL.

---

## 2. INBOX — idees brutes non triees

| ID | Idee | Origine | Date | A qualifier avec |
|---|---|---|---|---|
| B-088 | **Deux ecrans traduisent les roles avec une table AMPUTEE — « troisieme » s'affiche en brut.** Trouve le 27/08 par l'agent AGENDA en verifiant la deduplication de B-087, alors que rien ne l'y obligeait. `CompositionEquipeClient.tsx:84` et `OngletMoteur.tsx:211` portent chacun `const ROLE_LABELS = { premier: '1er', second: '2nd' }` avec repli `?? r` — **la semantique exacte de `roleClair`, mais privee de `troisieme`, `quatrieme`, `cinquieme`**. Consequence concrete : sur un creneau sur-mesure a 3 places, ces deux ecrans affichent **« troisieme »** en brut la ou « Regles & structure » affiche « 3e ». *Les creneaux sur-mesure existent : ce n'est pas une incoherence theorique.* Correctif : importer `ROLES_COURTS` (desormais la table unique, dans `lib/agenda/libelle.ts`, module feuille sans dependance serveur). ✅ **Trois fausses alertes ECARTEES avec leur raison** : `DepannagesClient.tsx:35`, `HistoriqueFetesCard.tsx:22`, `ConflitPublieBadge.tsx:26` sont des tables **exhaustives sur un type ferme a deux valeurs** (le role de garde V1), pas le libelle libre de `creneau_modele.roles` — *le vocabulaire coincide, le domaine differe ; les fusionner melangerait deux notions.* | Auto-detecte 27/08 (agent AGENDA) | 2026-08-27 | Coherence d'affichage — a faire |
| B-087 | **Le role s'ecrit « premier » / « second » en toutes lettres dans l'agenda — MiKL veut « 1er » / « 2nd ».** Recette du 27/08 **sur l'agenda REEL de la cliente**, premiere synchro reussie : les bandeaux affichent `Garde-JD-premier`, `Garde-VC-second`. Demande : la forme courte, « et pareil s'il y a un 3eme, etc. ». **Origine trouvee** : `creneau_modele.roles` vaut litteralement `['premier','second']` en base pour les 4 creneaux de Val d'Allier, et `google-calendar.ts:528` les reprend TELS QUELS. ⚠️ **Ne PAS corriger en base** : `roles` sert au moteur et a d'autres affichages, le renommer changerait le vocabulaire partout pour un besoin propre au bandeau d'agenda. La correction va dans la couche d'affichage. ⚠️ **Et ne pas ecraser un cabinet qui a nomme ses roles** : si un cabinet ecrit « titulaire » / « renfort », son mot doit passer intact — seuls les noms canoniques s'abregent. ✅ **RENDU le 27/08.** `roleCourt(role, placeIndex)` dans `src/lib/agenda/libelle.ts`, cablee dans `google-calendar.ts`. **La base n'a PAS ete touchee.** *L'agent a cherche la convention au lieu de l'inventer, et elle EXISTAIT DEJA* : `roleClair` (`reglesStructure.ts:53`) fait exactement ca — `premier→1er`, `second→2nd`, `troisieme→3e`… et **tout autre libelle rendu tel quel**, donc le respect des mots du cabinet etait deja la regle du projet. ⚠️ **DETTE ASSUMEE, signalee par l'agent** : `roleClair` n'etait pas importable (son module tire `supabase/server`, alors que `libelle.ts` doit rester une feuille pour l'apercu cote client) — **la table est donc recopiee, et deux tables pour un meme vocabulaire finissent par diverger**. Sortie propre : faire deleguer `roleClair` a `roleCourt`. ⚠️ **DIVERGENCE REELLE** : `roleParDefaut(1)` rend `2e`, MiKL veut `2nd` — les deux vocabulaires du projet divergent sur cette seule place, l'agenda suit MiKL. *L'agent avait ecrit « un seul vocabulaire » dans un commentaire, l'a constate faux et l'a corrige : le code dit la verite.* Tests **1546 → 1553 (+7)**, ecart explique. *Cinq tests de la veille ont d'abord ECHOUE en figeant `2e` : modification d'attente assumee et expliquee, pas un contournement.* 🔗 **Cablage de l'APERCU confie a COMPOSITION** — sans lui l'ecran afficherait `premier` pendant que l'agenda ecrirait `1er` : **un apercu qui ment est pire qu'une absence d'apercu, parce qu'on le croit.** **AVANCEMENT** : `ReglagesV2.tsx` touche par COMPOSITION (cablage de l'apercu en cours). En parallele, **decision prise en autonomie** : faire refermer la duplication de table par AGENDA (`roleClair` delegue a `roleCourt`) *pendant qu'il a le contexte* — une dette signalee le jour meme se repare en dix minutes, six mois plus tard on decouvre que les deux listes ont diverge sans que personne ne l'ait vu. **Avec consigne d'ARRET** : si les deux fonctions different sur un cas, ne pas aligner d'autorite, s'arreter et le dire. *Harmoniser en silence ce qu'un ecran affiche depuis toujours n'est pas une amelioration, c'est un effet de bord.* AGENDA a cree `tests/lib/regles-structure-role-clair.test.ts` — **il verrouille le comportement de `roleClair` AVANT de le faire deleguer**, ce qui est le bon ordre : sans ce filet, une deduplication qui change ce qu'un ecran affiche ne se verrait nulle part. ✅ **APERCU CABLE (COMPOSITION, 27/08) — et il a trouve PIRE que le defaut annonce.** Son apercu n'affichait pas seulement l'ancienne forme : il ecrivait **`'1er'` EN DUR**. Donc un cabinet ayant renomme sa place « titulaire » aurait vu « 1er » dans l'apercu et « titulaire » dans son agenda — *une valeur inventee, pas seulement mal formatee.* Corrige en passant par **exactement le meme chemin que l'agenda reel** (`roleCourt(roles?.[0], 0)` sur `creneau_modele.roles`, verifie en lisant `google-calendar.ts:530` avant d'ecrire), plus `roles` ajoute a la requete de `reglages/page.tsx` — la donnee n'etait meme pas chargee. Preuves : `tsc` 0 erreur · `npm test` **1553 passes, 0 echec** · `eslint` 0 probleme. ⚠️ **NON VERIFIE : aucun rendu vu.** *« Mon raisonnement dit que ca devrait marcher, mais "devrait" n'est pas "verifie". »* ✅ **DETTE REFERMEE — et l'agent a REFUSE ma consigne, avec raison.** Je lui demandais de faire **deleguer** `roleClair` a `roleCourt`. Son test de non-regression, ecrit AVANT de toucher quoi que ce soit, a revele **quatre divergences reelles** : sur `'Premier'` (majuscule), `'Troisième'` (accent), `' premier '` (espaces) et `''` (vide), `roleClair` rend la chaine telle quelle la ou `roleCourt` normalise et se replie. **Deleguer aurait donc fait basculer l'ecran « Regles & structure »** — un cabinet ayant saisi « Premier » avec sa majuscule aurait vu « 1er » du jour au lendemain. Il a donc partage **la TABLE, pas la fonction** : `ROLES_COURTS` vit dans le module feuille, le module serveur l'importe (bon sens de dependance), et chacun garde sa politique de lecture — *`roleCourt` normalise et se replie parce qu'un titre d'agenda doit toujours porter un role ; `roleClair` cherche a l'identique parce qu'une phrase de regles rend le mot du cabinet tel qu'il l'a ecrit.* Objectif atteint (une seule table) **sans un seul changement de comportement**. Tests **1553 → 1559 (+6, uniquement le filet de non-regression)** — *aucun test existant n'a bouge, et c'est precisement la preuve que la deduplication est neutre.* 🔎 **Trouvaille collaterale → B-088.** | MiKL (recette sur l'agenda reel) | 2026-08-27 | Libelle agenda — **fait, apercu en cours de cablage** |
| B-086 | **DEUX encarts Google Agenda sur le meme ecran, et un bouton qui deborde.** Recette de MiKL le 27/08, captures a l'appui, apres la mise en production du chantier agenda. Trois points : ① **Le selecteur « Couleur par defaut de l'agenda » DEBORDE de sa cellule** dans la fiche veto — le libelle est plus large que la colonne, et la phrase d'explication au-dessus est coupee a droite (« …ne sert que dans **Googl** »). ② **Deux encarts distincts traitent tous les deux de Google Agenda** : la carte « Google Agenda » (adresse de l'agenda, periode a renvoyer, Re-synchroniser) en haut de la grille, et la carte « Presentation dans Google Agenda » (journee entiere, horaires, base par creneau, apercu) en pleine largeur en dessous. *« C'est dommage qu'il y ait 2 encarts differents alors que ca concerne Google Agenda pour les 2. »* → a fusionner en un seul. ③ **La page doit rester equilibree** apres fusion — « pas de trous abuses ». Constat visible des maintenant sur la carte « E-mails aux vetos », dont la moitie basse est vide parce que ses voisines sont plus hautes. ⚠️ Le point ② n'est pas qu'esthetique : deux encarts pour un meme sujet obligent a chercher **ou** se regle quoi, et c'est exactement le genre de dispersion qui fait rater un reglage. **EN COURS depuis le 27/08** — confie a COMPOSITION (l'agent qui a fait les deux ecrans). `EquipeV2.tsx`, `v2-equipe.css` et `ReglagesV2.tsx` deja touches, rapport pas encore rendu. Consigne posee sur le point ② : **une seule carte, oui ; une bouillie indistincte, non** — le BRANCHEMENT (quel agenda, resynchroniser) et la PRESENTATION (a quoi ressemblent les evenements) restent deux zones lisibles a l'interieur. Et interdiction de casser le champ d'identifiant, le selecteur de periode et le bouton « Re-synchroniser » au passage : **cette carte porte le seul moyen de declencher une synchronisation a la main.** ✅ **RENDU le 27/08.** ① Debordement : le selecteur dit « Par defaut » au lieu de « Couleur par defaut de l'agenda », plus une troncature CSS en filet ; la phrase d'explication est **sortie de la grille a deux colonnes** ou elle vivait (c'est une piste de colonne qui la rognait — cause la plus probable, non confirmee visuellement). ② **Fusion faite** : UNE carte « Google Agenda » pleine largeur, seule sur sa ligne, avec deux zones titrees a l'interieur — **« Branchement »** (quel agenda, periode, Re-synchroniser : contenu d'origine, aucune ligne de logique touchee) et **« Presentation des evenements »** (interrupteurs, libelle par creneau, apercu). La distinction demandee est tenue : une carte, deux zones lisibles. ③ La grille restante ne porte plus que 2 cartes au lieu de 3 — le desequilibre venait de la carte agenda, de loin la plus haute, forcee dans la meme rangee. *L'agent signale spontanement une erreur d'inattention qu'il a lui-meme rattrapee en recomptant les balises a la main (`</section>` transforme en `</div>`), alors que `tsc` l'aurait attrapee de toute facon.* Preuves : `tsc` 0 erreur · **`npm test` 1546 passes, 0 echec** (pile la reference) · `eslint` 0 probleme. ⚠️ **NON VERIFIE : aucun rendu vu** — ni la fiche veto, ni les Reglages, ni sur mobile. *« Un raisonnement CSS a froid, pas une observation »* — si la coupure persiste, il faudra une capture. | MiKL (recette) | 2026-08-27 | UI Reglages + fiche veto — **fait, a recetter** |
| B-085 | **🔴 SECURITE — 8 fonctions `SECURITY DEFINER` sont EXECUTABLES par `anon`, dont `prendre_snapshot`.** Decouvert le 27/08 par MAX **en verifiant les migrations qu'il venait d'appliquer** : le controle « `anon` peut-il executer les RPC ? » repondait `false` pour `configurer_agenda_cabinet` (attendu) mais **`true` pour `prendre_snapshot`**. ⚠️ **Ce n'est PAS une regression introduite aujourd'hui** — `CREATE OR REPLACE` conserve les droits existants, donc le `GRANT` etait deja la ; c'est le controle qui a revele une faille preexistante. **Liste complete** : `auth_cabinet_actif`, `auth_cabinet_de_secretaire`, `get_calendrier`, `get_user_role`, `get_veterinaire_id`, `marquer_invite_complete`, `prendre_snapshot`, `sync_feries`. **Mesure du risque reel, faite** : un appel `anon` a `prendre_snapshot` **entre bien dans la fonction** (teste en transaction annulee — la garde `IF auth.jwt() IS NOT NULL` ne s'applique pas quand il n'y a pas de JWT) et n'echoue que sur une contrainte `NOT NULL`, parce que la RLS a masque les identifiants passes en parametre. Via PostgREST la cle `anon` porte toujours un JWT, donc la garde s'active et `auth_cabinet_actif()` renvoyant NULL leve l'exception — **la protection tient, mais elle tient a UN SEUL fil**, et la cle `anon` vit dans le bundle du navigateur. Certaines de ces fonctions sont legitimement appelees par les policies (`get_user_role`, `auth_cabinet_actif`) ; d'autres n'ont aucune raison d'etre exposees (`prendre_snapshot`, `sync_feries`, `marquer_invite_complete`). ⛔ **NE PAS REVOQUER A L'AVEUGLE** : retirer un `GRANT` dont une policy depend casserait l'authentification de tout le monde. A qualifier fonction par fonction avec CERBERE. *Meme famille que les vues `security_invoker` ouvertes a `anon` (22/08) et les tables `_backup_*` (25/08) : la troisieme fois que ce projet decouvre une porte ouverte a `anon`.* | Auto-detecte 27/08 (MAX, en verifiant les migrations) | 2026-08-27 | Securite — a qualifier avec CERBERE, ne rien revoquer sans analyse |
| B-083 | **🔴 BOMBE A RETARDEMENT SUR L'ETE — la synchro d'une periode pleine frole les 60 s.** Mesure du 27/08, apres calibrage d'un modele sur 3 mesures reelles (ecart 3-4 %). Le volume de 56 evenements servant jusque-la de reference etait celui d'une periode **partiellement remplie** ; une periode PLEINE (4 soirs + 1 week-end par semaine, 2 places) donne : **12 semaines (hiver) → 168 evenements → 22 a 42 s** selon la latence Google ; **17 semaines (ete) → 238 evenements → 32 a 59,8 s**. ⛔ **59,8 s pour 60 s de budget : 200 millisecondes de marge.** Une latence Google au-dessus de 500 ms, ou un cabinet avec des creneaux a 3 places, et ca depasse. **Ce que fait le code au depassement** : Vercel tue la fonction net, l'admin voit un 504 muet. **Attenuation reelle** : `garde_evenements` est ecrit au fil de l'eau et le rapprochement est idempotent — une relance reprend ou ca s'est arrete. **Fenetre de risque residuelle** : la bascule supprime avant de creer, donc une coupure juste apres laisse ces gardes-la ABSENTES de l'agenda jusqu'a la relance. ⚠️ **A NE PAS OPTIMISER A L'AVEUGLE** : le modele est calibre sur des latences **simulees**. La vraie latence de l'API Google depuis Vercel n'a **jamais ete mesuree**, et c'est exactement le parametre dont depend le seuil. Recommandation : mesurer la vraie latence a la premiere synchro de recette, PUIS decider (lots plus larges, pause plus courte, decoupage en plusieurs appels, ou message honnete « synchronisation partielle, relancez » plutot qu'un 504 muet). *Regler un budget sur un chiffre invente serait exactement la faute que ce chantier a passe la journee a corriger.* | Auto-detecte 27/08 (agent AGENDA) | 2026-08-27 | Arbitrage MiKL — mesurer d'abord, optimiser ensuite |
| B-084 | **Un test « golden » fige desormais un chemin qui n'existe plus.** `tests/engine/aval/golden-aval.test.ts:59` porte une fonction `deriverAgendaDescriptionActuelle`, **replique manuelle** de `buildEventDescription` supprimee le 27/08. Il ne DEPEND pas du code supprime — il le recopie — donc **il passe toujours**, et c'est precisement le probleme : il pretend figer la coherence R8 entre quatre chemins dont l'un a disparu. *Un test qui ne ment pas encore, mais qui pointe vers du vide — et qui mentira des que le nouveau chemin divergera, sans que personne ne le remarque.* A reprendre pour qu'il replique le nouveau chemin (titres par jour). Non touche par l'agent : golden test hors perimetre, servant de filet a d'autres travaux en cours. | Auto-detecte 27/08 (agent AGENDA) | 2026-08-27 | Dette — a reprendre apres la recette |
| B-082 | **Les evenements d'agenda n'ont plus de description.** Consequence assumee de l'eclatement (B-078) : le bloc unique portait une description riche (composition du week-end, jours exceptionnels) ; un evenement par personne et par jour rend l'essentiel dans son titre. **Rien n'a ete invente, car MiKL ne l'a pas demande** — mais la question se pose : un veterinaire qui ouvre sa garde dans l'agenda ne peut plus savoir **qui d'autre est de garde ce jour-la**. A trancher : soit on l'accepte (le titre suffit, l'info est dans GuardVeto), soit on remet une description courte du type « avec : FA (2nd) ». Lie au lien de retour vers GuardVeto, que MiKL voulait « voir ce que ca donne visuellement ». | Auto-detecte 27/08 (agent AGENDA) | 2026-08-27 | Decision produit — a trancher par MiKL |
| B-080 | **Des listes de colonnes figees en SQL rendent MUETTE toute colonne future.** Decouvert le 27/08 par l'agent SOCLE pendant le chantier B-078, et signale spontanement. La fonction qui prend le **snapshot de structure** (`20260702150000_p5s3d_snapshot_structure.sql`) enumere les colonnes de `creneau_modele` **une par une** dans un `jsonb_build_object`. Consequence : on ajoute une colonne, on la mappe correctement aux deux endroits du code TypeScript, **et elle reste quand meme vide au rejeu** — sans erreur, sans avertissement, sans qu'aucun test ne tombe. Le cas precis (`libelle_agenda`) est comble par une migration dediee, mais **le defaut n'est pas la colonne oubliee, c'est le motif** : chaque enumeration de ce genre rejouera exactement le meme piege a la prochaine colonne, quelle qu'elle soit et quel qu'en soit le chantier. Meme famille que les incidents deja payes ici : « inversion vendredi » (regle posee dans une couche, pas dans l'autre), « deux chemins d'envoi, un seul corrige », « trois chemins d'ecriture, deux gardiens ». **A qualifier** : recenser toutes les enumerations de colonnes figees sur `creneau_modele`, `veterinaires` et `cabinets` (inventaire demande a SOCLE, non code) ; puis decider s'il faut un garde-fou automatique — un test qui compare les colonnes reelles d'une table a celles que la RPC enumere echouerait le jour de l'ajout, au lieu de laisser decouvrir le trou des mois plus tard par un client. *C'est la meme parade que `filou-couverture-produit.test.ts` et `couverture-attentes.test.ts` : ce qui est deja oublie une fois ne se repare pas en promettant d'y penser.* **INVENTAIRE FAIT le 27/08** (25 fichiers de migration contenant `jsonb_build_object`, croises avec `FROM public.(veterinaires|cabinets|creneau_modele)`) — **deux trous supplementaires, listes et NON corriges** : ① **la vue `planning_semaine`**, a **trois** endroits repetes dans `20260729180000_planning_semaine_places_supplementaires.sql` (ligne native, branche « inversion vendredi », branche exception) qui enumerent `vp.prenom`, `vp.couleur`, `vs.prenom`… **plus** le sous-bloc `places_sup` (meme motif pour les places 3 et 4). C'est **la vue la plus consultee du projet** : le jour ou un ecran voudra afficher `couleur_google` ou `libelle_agenda` par personne, elle ne les portera pas. ② `get_calendrier` lit `region_feries` / `zone_scolaire` sur `cabinets` — **verifie et ECARTE**, il s'agit des feries et vacances scolaires, aucun rapport avec l'agenda. *A noter : aucun de ces trous ne gene le chantier B-078 (l'agenda lit `gardes` en direct, pas `planning_semaine`). C'est de la dette a venir, pas un blocage.* | Auto-detecte 27/08 (agent SOCLE) | 2026-08-27 | Dette de conception — inventaire FAIT, reste a decider le garde-fou |
| B-037 | **Le planning est equitable en VOLUME et intenable en RYTHME.** Constate par MiKL le 26/08 sur Hiver P1 (Val d'Allier), deux fois de suite : Jean 1er de garde les 21, 22 et 24 septembre ; Antoine de garde le week-end des 3-4 octobre PUIS lundi 5, mardi 6 et jeudi 8 — cinq jours de garde en six. **Mesure : les totaux sont pourtant justes.** Sur la periode entiere (21/09-18/10), Jean, Antoine, Manon et Victor ont chacun exactement 3 « 1er de semaine ». Le defaut n'est pas la quantite, c'est la CONCENTRATION : tout le quota d'une personne tombe sur une seule semaine, et rien apres. Cause connue et deja consignee le 20/08 (« le moteur compte les totaux, jamais le rythme »). **Ce qui existe deja et n'est pas active** : la brique `au_plus_n` (« au plus N gardes par semaine civile »), `serie_max`, `repos_apres_serie`, `succession_interdite`. Et `espacement_min` EST posee a 2 jours mais reglee sur « a eviter » — le moteur la viole donc legalement (dimanche → lundi). ⚠️ **Ne pas durcir sans simuler** : les semaines de septembre-octobre tombent a 3-4 vetos mobilisables (conges longs de Manon, semaines impaires d'Anne-Sophie, dernier recours d'Anne-Catherine) — un plafond ferme pourrait rendre la periode insoluble. | MiKL | 2026-08-26 | Moteur — a trancher avec Anne-Sophie |
| B-035 | **Quand il ne reste qu'UN remplacant possible, l'ecran ne le dit pas.** Constate le 26/08 sur le mercredi 9 septembre : un seul nom propose (Victor), presente comme « proposition la plus equitable ». Or il n'y avait aucun arbitrage d'equite a faire — **quatre regles dures se cumulaient** (Manon deja 2nde ce soir-la, Fanny en repos fixe le mercredi, Antoine en duo interdit avec Manon, Anne-Sophie hors soirs de semaine les semaines impaires) et il ne restait qu'un nom. L'admin ne peut pas savoir qu'un refus de Victor la met en impasse, ni pourquoi les autres manquent. Piste : une phrase du type « seul remplacant possible ce soir-la », et l'acces au detail des exclusions. Meme famille que B-005 (le tableau ne peut pas se taire) et B-019 : **le risque n'est pas la liste courte, c'est la liste courte presentee comme un choix.** | MiKL | 2026-08-26 | Ecran de reparation |
| B-049 | **Le rapport d'impasse est un dump technique, pas une explication.** MiKL, 26/08 : « ouah le truc horrible !!!!! ». L'ecran empile des codes machine (`R16 : ...`), des comptages bruts (« bloque 32 creneaux »), dix regles a plat sans hierarchie, trois « pistes » qui repetent la meme phrase, puis une liste de 25 creneaux date par date. Deja consigne le 19/08 : **un bandeau est un SIGNAL, pas un rapport** — grouper par CAUSE, jamais un code machine. La regle existe, l'ecran ne la suit pas. ⚠️ **Pire que moche : le chiffre est FAUX** (trouve le 26/08 en repondant a MiKL). « 25 creneaux non couverts » n'est pas un nombre de problemes — `joursNonCouverts = steps.slice(indexImpasse)` (`solver.ts:777`) renvoie **tout ce qui suit le point d'arret**. Un seul blocage rougit donc trois semaines de calendrier, et l'admin croit son planning irrecuperable alors qu'il bute sur un seul enchainement. **Un ecran qui compte faux est plus grave qu'un ecran illisible.** ✅ **LARGEMENT TRAITE le 26/08 par B-053, mais PAS CLOS.** Le mur rouge ne s'affiche plus dans le cas courant : la generation rend un planning partiel et l'ecran montre les vraies cases, groupees par cause, sans code machine. Le chiffre faux ne sert plus a rien de visible. **Ce qui reste** : l'ancien `DiagnosticImpasse` (codes R16, « bloque 32 creneaux », pistes repetees) s'affiche encore sur l'echec SEC — le cas ou le moteur ne pourvoit absolument rien. Rare, mais c'est exactement le moment ou l'ecran doit etre le plus calme. | MiKL | 2026-08-26 | Refonte de l'ecran d'impasse |
| B-051 | **MESURE FAITE — l'impasse est REELLE, et elle tient a DEUX gardes.** Generation Hiver P1 rejouee sur les vraies donnees de Val d'Allier (26/08). **AVEC Anne-Catherine : succes en 113 ms, elle prend exactement 2 gardes** — vendredi 2 octobre (2nde) et week-end 3-4 octobre (1re), precisement quand Jean (conge 1-4/10) ET Anne-Sophie (conge 2-4/10) manquent. **SANS elle : echec en 3,1 s, 25 creneaux non couverts, impasse PROUVEE** (exploration complete, pas un abandon sur timeout — donc « il y a d'autres solutions » est faux, le moteur a bien tout essaye). Creneau bloquant : week-end du 26 septembre. ⚠️ **CORRECTION apres 2e mesure (question de MiKL : « comment on passe de 2 trous a 25 ? ») — « l'impasse tient a deux gardes » etait FAUX, et le « 25 » ne veut pas dire ce qu'il affiche.** ① Le chiffre affiche est un artefact : `joursNonCouverts = steps.slice(indexImpasse)` (`solver.ts:777`, commente « rapport legacy ») = **tout ce qui reste APRES le point d'arret**, pas 25 creneaux impossibles. Le moteur ne rencontre pas 25 problemes, il rencontre UN mur et rougit toute la suite. ② Symetriquement, « 2 gardes » n'est que la solution qu'il avait trouvee AVEC elle — pas une liste de trous a boucher. **Mesure par tranches, sans le dernier recours : 21-30/09 passe (9 creneaux), 1-4/10 passe (3 creneaux), 5-18/10 passe (12 creneaux). AUCUN creneau n'est isolement impossible** — c'est leur ENCHAINEMENT (regles de rythme, roulement, equite) qui ne passe plus. *(Reserve honnete : decoupees, les tranches perdent le lookback entre elles, donc elles ont la tache facile — ca ne prouve pas que l'ensemble serait resoluble, seulement qu'aucun creneau ne l'est en soi.)* **Le vrai probleme n'est pas l'exclusion, c'est qu'on ne peut RIEN faire ensuite** : une generation en impasse ne persiste aucun planning, donc l'admin ne peut rien completer a la main — alors que c'est exactement ce que B-046 prevoit. **A trancher par MiKL** : (a) planning PARTIEL persiste avec ses trous, complete a la main ; (b) le moteur propose nommement « je peux boucler en utilisant Anne-Catherine, j'y vais ? » ; (c) statu quo, on leve un conge. **La 2e mesure fait pencher vers (a)** : il n'y a pas de « 2 creneaux » identifiables a l'avance a proposer. Lie a B-046, B-037, B-049. | MiKL | 2026-08-26 | Decision produit — mesure faite, en attente d'arbitrage |
| B-052 | **Le chargement du moteur ne filtre PAS par cabinet** — decouvert le 26/08 en mesurant B-051. `chargerInputDepuisSupabase` (`engine/loader.ts:268`) lit `veterinaires` avec `.eq('actif', true)` **et rien d'autre** ; idem pour `conges`. En production la RLS filtre, donc **aucune fuite constatee** — mais la mesure, lancee avec la service role, a ramene les **14 vetos des deux cabinets** (les prenoms du bac a sable sont les memes qu'a Val d'Allier) et le moteur a genere dessus sans broncher. Un chargement qui ne dit pas pour quel cabinet il travaille est une isolation qui tient a une seule couche. Le `cabinet_id` est pourtant deja passe en parametre a cette fonction. | Auto-detecte 26/08 | 2026-08-26 | Securite — a qualifier avec CERBERE |
| B-053 | **🔴 BLOQUANT PROD — une generation ne doit JAMAIS rendre l'admin bredouille.** MiKL, 26/08 : « faut plus que le moteur reagisse comme ca, t'imagine un client qui tombe la-dessus, il panique !! on est cense etre en prod ». **Constat technique :** `genererPlanningPur` est TOUT OU RIEN — le seed est un backtracking pur, et sur impasse il renvoie `planningPartiel: { attributions: [] }` (`solver.ts:832`). Ce champ existe dans le type de retour, **n'est jamais rempli et n'est lu par personne** : une coquille vide. L'admin perd donc 100 % du travail du moteur pour un seul enchainement impossible, sans aucun moyen de reprendre la main — alors que le planning au moment du blocage EXISTE en memoire (`blocage.value.planning`, utilise pour le diagnostic puis jete). **Principe a poser : le moteur rend toujours quelque chose d'exploitable** — ce qu'il a pu remplir, les cases vraiment vides marquees « a pourvoir », et une phrase calme. Un mur rouge sur trois semaines n'est pas un resultat, c'est un abandon. **Benefice collateral : les vrais trous deviennent mesurables**, ce qui corrige aussi le chiffre faux de B-049. Englobe B-051 (option a) et rend B-049 traitable. **LOT 1 (moteur) LIVRE le 26/08** : `remplirAuMieux` — mesure sur Hiver P1 sans le dernier recours, **43 places pourvues et 5 cases vides** au lieu de rien du tout, chacune avec le pourquoi de chaque veterinaire ecarte. **LOTS 2 et 3 LIVRES le 26/08** : le planning partiel est persiste par le meme chemin que le succes, la publication refuse (dur) tant qu'il reste une case sans veterinaire, et l'ecran affiche une troisieme issue « partiel » — bandeau ambre, cases groupees par cause avec leur levier, plus aucun code machine. **LOT 4 LIVRE** : le bouton qui mentait est supprime, il oriente desormais vers l'Assistance avec le formulaire pre-rempli et le rappel de la capture d'ecran. Precision de MiKL : chaque creneau problematique doit porter son POURQUOI **et son COMMENT debloquer** — « c'etait ce qui etait prevu initialement ». | Feature (correctif de fond) | MiKL | A qualifier | **Livre — 4 lots, a recetter** | 2026-08-26 |
| B-054 | **Le rapport des cases vides repete 5 fois la MEME objection, et c'est une CONSEQUENCE, pas une cause.** MiKL, 26/08 (recette) : « il repete plusieurs fois la meme objection ». Sur le week-end du 26/09, cinq lignes identiques : « Le 1er de garde WE doit etre designe avant le 2nd ». ⚠️ Ce n'est pas qu'un defaut d'affichage : cette raison ne dit RIEN de ce qui bloque. Elle apparait parce que la place du **1er** est restee vide — donc le 2nd devient mecaniquement impossible. **Une case vide en fabrique une seconde, et la seconde affiche un pourquoi trompeur.** A traiter en deux temps : dedupliquer, et surtout ne plus compter comme un probleme ce qui n'est que la consequence d'un autre. | MiKL | 2026-08-26 | Ecran + moteur |
| B-055 | **Dans une garde a moitie remplie, TOUT s'affiche en 1re position — on ne sait pas quelle place reste.** MiKL, 26/08 : sur le week-end du 10, seule Anne-Sophie etait posee et elle apparaissait « en 1re position » du vendredi au dimanche ; en completant avec Jean, **Jean s'est mis en 1re position le vendredi**. L'admin ne peut pas savoir quelle place il pourvoit. Consequence directe de B-053 (les plannings partiels existent desormais) : l'affichage des places n'avait jamais eu a gerer une garde incomplete. | MiKL | 2026-08-26 | Ecran planning + modale de garde |
| B-056 | **Le pre-vol a dit « Rien ne coince » avant une generation qui a laisse 5 cases vides.** MiKL, 26/08, capture a l'appui : « qu'est-ce qui est mesure ici ? ». L'ecran « Avant de lancer » affiche *« Rien ne coince — Les regles du cabinet sont coherentes avec l'equipe en place. On peut y aller »*, puis la generation rend un planning troue. **Le pre-vol ne verifie que la coherence des REGLES entre elles** (regles fantomes, contradictions arithmetiques evidentes) — il ne simule pas la generation. Sa phrase, elle, se lit comme une garantie de resultat. Meme famille que B-005 : le risque n'est pas le controle incomplet, c'est la phrase rassurante. | MiKL | 2026-08-26 | Ecran de pre-vol |
| B-057 | **L'encart « dernier recours » est desequilibre.** MiKL, 26/08 : « c'est bien par contre fais en sorte d'equilibrer l'encart, c'est encore tout desequilibre ». La note explicative occupe une colonne etroite a gauche sur cinq lignes, la moitie droite du formulaire reste vide. Meme defaut que B-015 (selecteur de couleur) sur le meme ecran. | MiKL | 2026-08-26 | Retouche visuelle |
| B-058 | **MiKL n'a pas pu recetter l'Assistance : le cas ne s'est pas presente.** Demande du 26/08 : « il faudrait que tu me crees l'occasion que je puisse tester en live ». Le bloc d'aide ne s'affiche que sur l'echec SEC (aucune garde attribuee), qui ne se produit plus en pratique. A prevoir : soit un chemin d'acces permanent a l'Assistance, soit un moyen de provoquer le cas sur le bac a sable. | MiKL | 2026-08-26 | Recette |
| B-059 | **« Pourquoi Victor ne fait pas le week-end du 25 septembre ? Il a 0 WE au compteur et n'est pas absent. »** MiKL, 26/08, exemple pris au hasard — **et il ajoute : « je suis sur qu'il existe d'autres cas de ce type, verifie ».** Doute de fond sur le remplissage au mieux livre le meme soir : le glouton ne revient JAMAIS en arriere, donc un mauvais choix pris tot (le duo du vendredi soir) peut condamner tout le week-end suivant via R8/R9, sans que le moteur ne reessaie. **MESURE FAITE, MiKL A RAISON — le glouton se saborde.** Releve du remplissage reel : `25/09 vendredi = Fanny + Antoine`, puis `26/09 week-end = PERSONNE` (les deux places). Pourquoi : R9 impose au week-end le duo du vendredi, R8 impose l'inversion → Fanny doit etre 2nde, Antoine devrait etre 1er mais FREQ_WE le bloque (pas plus d'un WE toutes les 2 semaines). **Or Victor et Jean etaient libres ce week-end-la** — ils auraient pu prendre le vendredi ET le week-end. Le moteur ne le voit pas parce qu'il choisit le vendredi AVANT le week-end (`genererSteps` : `[...weSteps, ...semaineSteps]`, chronologique dans le bloc) et ne revient jamais en arriere. **Bilan : Fanny 3 vendredis sur 4, Antoine 3 sur 4, Victor / Jean / Manon ZERO week-end, et 8 cases vides dont au moins 5 evitables.** Le defaut n'est pas l'equite du scoring, c'est que le vendredi et le week-end forment un BLOC indissociable (R8/R9) traite comme deux decisions separees. | MiKL | 2026-08-26 | Moteur — cause racine identifiee, correctif a valider |
| B-060 | **UNE PASSE DE RATTRAPAGE APRES LE REMPLISSAGE — proposition de MiKL le 26/08 (« un dossier tres serieux »).** *« Jusqu'a present le moteur remplit les cases dans l'ordre. Maintenant il ne s'arrete plus a ca et continue en laissant les cases qui lui semblent vides. Pourquoi ne pas creer une etape supplementaire qui viendrait verifier ce qui a ete produit et qui remplirait le reste des cases vides — ou en tout cas qui verifierait une derniere fois qu'aucune solution n'est possible sauf intervention humaine ? »* **Faisable, et c'est le prolongement direct de B-059** : celui-ci a supprime les trous causes par l'ordre a l'INTERIEUR d'un bloc (vendredi+week-end) ; il reste ceux causes par l'ordre ENTRE les blocs — une case du 14/10 peut dependre d'un choix fait le 3/10. **Deux livrables distincts, a ne pas confondre** : ① *rattrapage* — pour chaque case vide, defaire un voisinage borne (creneaux lies, gardes de la personne autour, meme jour) et reconstruire en cherchant a la pourvoir ; on garde si le nombre de places pourvues augmente STRICTEMENT ; repeter jusqu'a point fixe. ② *statut de preuve par case* — distinguer **« impossible, prouve »** (personne ne peut y aller quel que soit le reste du planning : conges, repos fixes, indispos) de **« pas trouve »** (recherche bornee epuisee). ⚠️ **Ne jamais ecrire « aucune solution n'existe » quand on a seulement cherche sans trouver** — c'est la faute que cette journee entiere a consiste a corriger. **FEU VERT de MiKL le 26/08, avec une exigence en plus** : *« ce n'est pas grave de prendre quelques secondes en plus, je prefere prendre plus de temps et l'utilisateur le comprendra si tu indiques EN DIRECT ce qu'il se passe... et en plus ca fait plus pro »*. → **La progression doit etre REELLE, jamais un decompte decoratif** : des etapes chronometrees cote client, sans lien avec ce que le serveur fait vraiment, seraient du theatre — exactement ce que cette journee a passe son temps a corriger. Le serveur DIT ou il en est (flux de reponse), l'ecran relaie. Budget rebalance : la recherche principale n'a plus a monopoliser 40 s d'une fonction qui en compte 60. | MiKL | 2026-08-26 | Moteur + ecran — EN COURS |
| B-061 | **Fanny fait 2 week-ends, les DEUX en 2nde — et le compteur « 1er WE » se desequilibre.** MiKL, 26/08 (recette) : « pourquoi Fanny ne fait pas un WE 1ere de garde, elle en fait 2 en 2nd, ce qui desequilibre en plus le compteur ? ». Le « 1er WE » est le role qui porte l'**avantage financier** : ne jamais l'obtenir n'est pas un detail d'affichage. **Piste identifiee a la lecture du code, a confirmer par la mesure** : `malusAvantageFinancier` (`solver.ts`) vaut `-compteur × poids` pour le role avantage et `+compteur × poids` pour l'autre — **le signal est PROPORTIONNEL au compteur, donc NUL quand il vaut zero**. La regle freine ceux qui en ont deja eu ; elle ne pousse JAMAIS ceux qui n'en ont aucun. Fanny, a 0, ne recoit aucun signal — et l'inversion vendredi↔WE (R8) la met 2nde a chaque fois qu'elle est 1re le vendredi. A relier a B-037 (equite en volume, pas en rythme) : meme famille, le moteur equilibre des totaux sans regarder ce qu'ils recouvrent. ✅ **MESURE FAITE — la piste est confirmee.** Releve Hiver P1 : **Fanny est 1re du vendredi soir 3 fois sur 4** (25/09, 02/10, 09/10), donc 2nde au week-end a chaque fois par l'inversion R8. Compteurs : Fanny 2 WE / **0 « 1er WE »** ; Anne-Sophie 1/1 ; Victor 1/1 ; Jean 1/1 ; Antoine 2/1. Le cabinet a bien `roleAvantageFinancier = 'premier'`. **Cause : le signal vaut `compteur × poids`, donc ZERO pour qui n'a jamais eu le role.** Il freine ceux qui en ont eu, il ne pousse personne vers ce qu'il n'a pas. **Correctif propose : raisonner en ECART A LA MOYENNE.** Fanny a 0 pour une moyenne de 0,67 → ecart negatif → elle devient deprioritaire pour le 1er VENDREDI, donc 2nde du vendredi, donc **1re du week-end**. 🛑 **TILT — deux correctifs tentes, AUCUN effet, le second degradait (1 case vide → 2). Annules tous les deux, sonde posee.** **CAUSE RACINE PROUVEE par la sonde** (scores reels au week-end du 10/10) : a ce moment-la il ne reste que **DEUX personnes disponibles**, Fanny et Anne-Sophie — Victor en conge, Jean et Antoine bloques par FREQ_WE, Manon en conge. Et **elles sont TOUTES DEUX a 0 sur le compteur « 1er WE »** : leurs scores sont identiques (100 / 100 sur les deux roles). **Rien ne les departage sur ce week-end.** Le role se decide donc en amont, au VENDREDI, ou Fanny a le meilleur score (50 contre 100) pour des raisons sans aucun rapport avec l'avantage financier — et l'inversion R8 la releguee 2nde. ⛔ **Pourquoi mes deux correctifs ne pouvaient PAS marcher** : ils reposaient sur l'ecart au compteur « 1er WE », or les deux candidates sont a egalite (0 et 0). Un ecart nul ne departage rien. **CE QUI MANQUE VRAIMENT, a valider par MiKL** : compter les **week-ends passes SANS le role avantage** (`weGardes − weekendPremier`). Fanny a 1 week-end et 0 fois le role : une occasion ratee. Anne-Sophie a 0 week-end : aucune. A egalite sur « 1er WE », celui qui accumule des week-ends sans jamais l'avantage doit passer devant. | MiKL | 2026-08-26 | Moteur — cause racine prouvee, correctif a valider |
| B-006 | V3 — etendre le produit aux journees de travail (aujourd'hui : soirs + week-ends seulement). Exclu volontairement de la V2. | MiKL | 2026-08-24 | V3, ne pas commencer |
| B-015 | **Le selecteur de couleur deseequilibre la fiche veto.** MiKL, 24/08, apres recette : « ca marche, par contre fais quelque chose au niveau de l'affichage parce que la fenetre est asymetrique et chargee ». Constat sur capture : le selecteur est deplie en PERMANENCE dans le formulaire, colle a gauche sur un tiers de largeur, la moitie droite vide — alors que la reference fournie par MiKL etait un panneau compact. S'y ajoutent 18 pastilles de suggestion sur deux rangees et trois blocs de texte d'aide. La fiche fait desormais deux ecrans de haut pour changer un prenom. | MiKL | 2026-08-24 | Retouche visuelle, en cours |

**B-003 ≠ B-004** (arbitrage MiKL 24/08) : B-004 est une regle **deja active** dans le moteur
(voir section 4) ; B-003 reste **a construire** (voir section 3). Detail → archive 2bis.

---

## 3. Backlog qualifie

> Statuts : Qualifie → Pret → En cours → En revue → Livre (ou Gele / Ecarte).
> Perimetre : GuardVeto est un produit MPP (abonnement) — la majorite du dev est **Interne**
> (invisible du client, inclus dans l'abonnement). `A qualifier*` marque un item ne du besoin
> SPECIFIQUE de Val d'Allier, dont on ne sait pas encore s'il reste inclus ou devient une
> prestation a part — decision MiKL du 24/08, nuance demandee par team-lead, a trancher au cas
> par cas (items marques * ci-dessous).

| ID | Titre | Type | Origine | Perimetre | Statut | MAJ |
|---|---|---|---|---|---|---|
| B-093 | **Filou relevait SEPT problemes et n'en corrigeait AUCUN — on lui donnait les remplacements, jamais les echanges.** Question de MiKL le 01/09 : « Filou annonce ca dans son rapport, mais pourquoi il n'agit pas ? ». Ce n'etait pas un manque d'outil : `arbitrer.ts` sait deja appliquer un echange a deux places et Filou sait deja le demander. **C'est ce qu'on lui DONNE qui l'en empechait.** `remplacantsPossibles` ne repond qu'a « qui pourrait prendre cette place si on ne libere QUE celle-la » — et le prompt lui disait, en toutes lettres, « si la liste est vide, personne d'autre ne peut prendre cette place ». Phrase **vraie pour un remplacement, fausse pour un echange**. Il la lisait comme « rien n'est possible ici » et s'abstenait, correctement au vu de ce qu'on lui avait dit. **Mesure sur Hiver P2 regenere : 53 places sur 118 sans aucun remplacant simple, 1,18 en moyenne la ou il y en a** — et c'est une borne haute (espacement et conges seuls, sans les repos fixes ni les duos interdits). C'est mecanique : Filou relit un planning que le moteur vient d'optimiser, donc les places ou l'on peut poser quelqu'un d'autre sans rien bouger n'ameliorent rien. **Livre** : module `engine/relecture/echanges.ts` (les permutations que `isValid` accepte, controle cumulatif, deux ordres de pose essayes), section dediee dans le dossier, et la phrase du prompt corrigee. Ciblage sur les personnes aux extremes de la charge pour borner le dossier (~6 900 paires sinon). | Correctif | MiKL | Interne | **Livre** — a recetter | 2026-09-01 |
| B-094 | **Le meme week-end a TROIS definitions differentes dans le produit.** Trouve le 01/09 en verifiant un chiffre de Filou. `joursCouvertsGarde` (moteur, regles de rythme) dit **samedi + dimanche** · `joursImpactesGarde` (couche attributions/affichage) dit **vendredi + samedi** · la table `attributions` en base, elle, porte la verite pour Val d'Allier : vendredi 16h → samedi 6h, PUIS samedi 10h → lundi 7h, soit **3 nuits**, avec les roles 1er/2nd **inverses entre le vendredi et le reste**. **Consequences mesurees** : ① le correctif d'espacement de B-092 protege le lundi mais **PAS le jeudi soir**, puisque le moteur ne voit pas que le week-end commence le vendredi (aucun cas sur Hiver P2, verifie — mais le trou existe) ; ② le compteur « 1er du week-end » ne compte que le samedi, donc **le constat de Filou « Fanny fait 3 week-ends mais n'est premiere qu'une fois » est faux** : avec l'inversion, elle est premiere le vendredi les deux autres fois, soit 3 fois sur 3 blocs. **Demande produit de MiKL, le 01/09** : chaque jour est independant, et la structure de garde doit etre **parametrable par cabinet** — Val d'Allier : week-end = vendredi → lundi matin, 3 nuits, roles inverses, compteur week-end vendredi compris, et soirs de semaine = lundi a jeudi. Un autre cabinet : week-end = samedi + dimanche, vendredi = soir de semaine ordinaire. ⚠️ **Ne pas traiter en correctif ponctuel** : touche le moteur, les compteurs, l'affichage et l'agenda. | Feature | MiKL | A qualifier | **Pret** | 2026-09-01 |
| B-092 | **Le moteur comptait un week-end comme un point, et rangeait l'equite derriere toutes les preferences.** Trouve par MiKL le 31/08 en recettant Hiver P2 : « Victor a 7 gardes sur 14 jours, dont un week-end + lundi + mercredi » et « Manon est encore en deficit, pourquoi ? ». **Deux causes distinctes, toutes deux mesurees en base.** ① `espacement_min` comparait les dates d'ANCRAGE : un week-end date du samedi et le lundi suivant etaient reputes a 2 jours d'ecart, alors que la personne sort de garde le lundi matin et reprend le lundi soir. **11 enchainements week-end → lundi sur la seule periode**, tous reputes conformes. La semantique juste existait deja (`joursCouvertsGarde`, ecrite pour les briques de rythme #13) ; `espacement_min`, plus ancienne, n'y avait jamais ete raccordee. ② **Toute l'equite vivait a l'etage 6, le dernier.** Combinee a la garantie lexicographique du systeme (« un point a l'etage N bat n'importe quel nombre de points a l'etage N+1 »), **une seule preference « si possible » respectee l'emportait sur n'importe quel desequilibre**. Mesure : Manon 3 seconds de semaine contre Victor 12, sur une dimension reglee « essentiel », sans aucune absence de Manon sur la periode — le moteur avait prefere respecter « Victor pas le lundi », cinq lundis de suite. **Livre** : nouvel etage `EQUITE_CRITIQUE` entre « sauf en cas de crise » et « a eviter au maximum », qui ne se declenche qu'au-dela d'un seuil par dimension. Arbitrage MiKL du 01/09 : « ca doit etre plus present, mais pas depasser les regles jamais ». ⚠️ **Les seuils ne sont pas encore reglables a l'ecran** (voir B-092a). | Correctif | MiKL | Interne | **Livre** — a recetter | 2026-09-01 |
| B-092a | **Exposer les seuils de desequilibre critique sur l'ecran Regles.** Reste-a-faire assume de B-092. Le moteur sait deja lire un reglage par cabinet (`EquityWeights.seuilsCritiques`, threade partout comme les poids et les cohortes) ; ce qui manque est l'ecran qui l'expose, dans le meme vocabulaire a quatre crans que les autres regles d'equite. D'ici la, les valeurs de depart s'appliquent a tous les cabinets et **aucun cabinet ne peut les changer** — contraire au principe « toutes les regles reglables ». Valeurs en place : 2 pour les week-ends, le 1er du week-end et les feries ; 3 pour 1er/2nd/renfort de semaine ; 0 (desactive) pour les grands week-ends, compteur derive qui suit mecaniquement celui des week-ends. | Feature | Reste-a-faire B-092 | Interne | **Pret** | 2026-09-01 |
| B-091 | **La copie du cabinet vers le bac a sable avait laisse 13 regles muettes — et personne ne l'a su pendant des semaines.** Trouve par MiKL le 31/08 : il lance une generation sur Hiver P2 dans le bac a sable et recoit treize alertes « une regle concerne un veterinaire retire de l'equipe », alors qu'aucun veterinaire n'avait ete retire. **Cause prouvee en base** : les 13 regles pointaient encore vers les veterinaires du Val d'Allier (`00000000-…-0000000X`), l'original copie. Les 154 lignes des ONZE autres tables, elles, avaient ete correctement remappees — parce que leur lien vers un veterinaire est une COLONNE. Dans `regles_cabinet`, il est enfoui dans `params_json` (`qui.refs[]`, `params.avec_veterinaire_id`), et rien dans le schema ne dit que ce JSON contient des identifiants : ni la copie ni aucun controle ne pouvaient le voir. **Ce qui coutait vraiment, plus que le bandeau** : dans le bac a sable AUCUNE regle individuelle ne s'appliquait — jours de repos, repos conditionnels, « jamais ensemble » invisibles du moteur. Une demo y produisait un planning plus propre que la realite, sans que rien ne le dise. Une regle qui ne designe personne ne proteste pas : elle se tait. **Livre** : 13 regles remappees sur les homonymes du bac a sable (14 refs, 0 orpheline) + trigger `trg_regle_refs_du_meme_cabinet` en base, qui refuse desormais toute regle designant un veterinaire d'un autre cabinet. Pose en BASE et non en code parce que la copie fautive a ete faite en SQL direct — un controle cote ecran ne l'aurait jamais vue passer. ⚠️ **Les snapshots de regles n'ont PAS ete touches**, contrairement au plan valide : 12 sur 27 sont rattaches a des periodes du bac a sable et servent au replay ; les remapper aurait rendu l'archive infidele au planning qu'elle documente. | Correctif | MiKL | Interne | **Livre** — a recetter | 2026-08-31 |
| B-090 | **Une marque « bac a sable » sur tous les ecrans du compte de demonstration.** Demande de MiKL le 31/08 : « mets-moi une marque dans la barre du bac a sable que c'est bien la version demo, car sinon je ne sais pas ». Le besoin est ne le matin meme : le bac a sable a ete remis **a l'identique** du compte client (memes prenoms, meme planning Hiver P1, memes compteurs), ce qui etait voulu pour mesurer — et les rend indiscernables a l'oeil. Une retouche « pour essayer » peut desormais atterrir chez le vrai cabinet sans que rien ne previenne. **Pose en BASE, pas en code** (`cabinets.est_bac_a_sable`) : ecrire l'identifiant du cabinet demo en dur aurait fait entrer un client precis dans le produit, et le prochain bac a sable n'aurait rien eu. Ruban monte dans les DEUX layouts (V1 et V2), donc sur chaque ecran connecte, y compris ceux qui n'existent pas encore. ⚠️ **Ce n'est pas une securite** : il informe, il n'empeche rien — l'isolation reste la RLS. | Feature | MiKL | Interne | **Livre** — a recetter | 2026-08-31 |
| B-089 | **La relecture de Filou est lente, et personne n'avait choisi son niveau d'application.** Demande de MiKL le 31/08 : « le temps de reflexion est anormalement long […] est-ce que c'est le niveau de reflexion d'Opus, ne vaudrait-il pas mieux rester sur Sonnet 5 ? ». **Cause trouvee a l'audit : `relecturePlanning.ts` n'envoyait PAS `output_config.effort`** — l'API applique donc son defaut, `high`, le deuxieme cran le plus fouille de la gamme. Ce n'est pas un choix qui a ete fait, c'est un choix qui n'a jamais ete fait : le Filou du quotidien, lui, tourne a `medium` depuis le 28/07. Le volume n'explique rien (40 places, 7 personnes, ~1 500 jetons de prompt systeme). **Consequence a venir, plus grave que la lenteur** : la route est plafonnee a 120 s (`maxDuration`) ; sur une periode de 12 semaines (3x plus de places) le meme reglage la depasserait et l'ecran afficherait « Filou n'a pas pu relire » sans dire que c'etait une question de temps. **Lot 1 livre** : banc de mesure (4 configurations sur le MEME planning, temps/jetons/cout/ce qu'elles ont trouve), `effort` rendu reglable par `GUARDVETO_IA_EFFORT_RELECTURE` sans changer le defaut. **Lots suivants, non commences** : ② brider et tenir sous les 120 s · ③ donner a Filou les regles souples reellement enfreintes et ce qu'un deplacement debloquerait (profondeur 2) · ④ reecrire le prompt et les 9 criteres avec les cas concrets de MiKL (5 gardes d'affilee, 3 week-ends de suite, N fois second et jamais premier). | Correctif + Feature | MiKL | Interne | **En cours** — lots 1 et 2 livres (banc + Sonnet 5 + 3 correctifs), MiKL recette | 2026-08-31 |
| B-048 | **⚠️ « L'equipe GuardVeto a recu ta situation » s'affichait SANS AUCUNE PREUVE D'ENVOI.** `signalerLimite` faisait `await sendBrevoEmail(...)` dans un try/catch — or cette fonction ne LEVE jamais, elle RETOURNE `{ error }`. La valeur n'etait pas lue : « C'est parti » s'affichait meme quand rien ne partait, et rien n'etait journalise (aucune ligne `email_log` le 26/08 a 18h49, verifie en base). **Corrige par SUPPRESSION** du chemin, pas par rafistolage : le signalement passe desormais par l'onglet Assistance, qui lui journalise (`demandes_support.email_envoye` / `email_erreur`). | Correctif | MiKL | Interne | **Livre** — a recetter | 2026-08-26 |
| B-050 | **« Prevenir l'equipe » envoyait un message vide.** Le champ libre etait facultatif. Le bloc ORIENTE maintenant vers l'Assistance : « prends une capture d'ecran avant de quitter », « decris la situation », et un lien vers `/support` avec titre + description pre-remplis (contexte technique inclus, relisible et modifiable). Rappel de la capture affiche a l'arrivee, quand on peut encore la joindre. | Correctif | MiKL | Interne | **Livre** — a recetter | 2026-08-26 |
| B-046 | **Le dernier recours n'entre PLUS JAMAIS dans une generation.** Demande de MiKL le 26/08. Avant : il n'etait pas exclu, seulement REPOUSSE (score 1 000 000) — donc mobilise des que le moteur n'avait plus personne, sans que l'admin l'ait decide. Desormais il sort de l'effectif AVANT le moteur (`engine/effectif.ts`, drapeau `pourGeneration` sur les 3 portes : generation, replay, pre-vol) ; equite et pre-vol ne le comptent plus non plus. **Consequence assumee et mesuree : une periode que seul lui pouvait boucler echoue maintenant** — et l'ecran NOMME le reglage au lieu de laisser chercher parmi les regles. Il reste propose pour les retouches a la main, la reparation d'absence et les appels aux volontaires. Note explicative ajoutee sous la case dans Equipe ; 4 textes de Filou devenus faux corriges ; garde-fou `tests/lib/generation-exclut-dernier-recours.test.ts` (mutation testee). | Feature | MiKL | A qualifier | **Livre** — a recetter | 2026-08-26 |
| B-047 | **Conges affiches SOUS les gardes** dans la case du calendrier (`PlanningV2.tsx`) : la garde est l'information principale, elle se lit en premier. | Retouche visuelle | MiKL | Interne | **Livre** — a recetter | 2026-08-26 |
| B-033 | **⚠️ FUITE ENTRE COMPTES — la conversation Filou d'une admin restait affichee a un veterinaire.** Cle `sessionStorage` FIXE, sans identifiant de personne : changer de compte dans le meme onglet gardait le fil du precedent. Corrige sur la conversation ET sur la reponse du tableau. Detail integral → archive 12. | Correctif | MiKL (recette) | Interne | **Livre** | 2026-08-26 |
| B-032 | **Filou est muet pour les veterinaires — decision produit a prendre.** Decouvert avec B-033. `FilouChat` conditionne le champ de saisie a `estAdmin` ; la branche veterinaire rendait `null`. Un veto voyait donc la tablette, le renard et le mot d'accueil, **sans rien pour ecrire ni aucune explication** — la coquille vide que ce projet refuse partout ailleurs. ⚠️ **Le serveur, lui, EST pret** : le catalogue ouvre une vingtaine d'outils a un veterinaire (`outilsPour`, mesure de l'audit B-007 : 20 pour un veto, 52 pour une admin). La capacite existe, l'ecran ne la donne pas. **Correctif immediat pose le 26/08 : une phrase honnete a la place du vide** (« Filou repond aux questions de l'administratrice ; ton planning et tes conges se consultent a l'ecran »). **Reste a trancher par MiKL : ouvre-t-on le champ aux veterinaires ?** Ce n'est pas un correctif — ca change ce que six personnes peuvent faire, et le perimetre par droits est deja code cote serveur. | Decision produit | Auto-detecte 26/08 | A qualifier | Qualifie — a trancher | 2026-08-26 |
| B-029 | **⚠️ L'effectif de nuit n'est plus reglable a l'ecran — et un test rassurait sur un ecran MORT.** Trouve le 26/08 en supprimant le code mort. `tests/lib/controle-impact-couverture.test.ts` verifiait que « les QUATRE ecrans » ouvrent le gardien d'impact ; le quatrieme, `components/admin/EffectifPeriodeSelect.tsx`, n'etait atteint par aucune page. **Le test rendait donc un vert sur du vide.** En V2 l'effectif de nuit est AFFICHE (Epicentre, parcours de generation) mais ne se regle plus sur un ecran dedie : il vient de la periode type (places par creneau). L'action `setEffectifPeriode`, avec son controle d'impact, **n'est plus appelee que par un outil de Filou**. Le test dit desormais « trois ecrans » et explique pourquoi. **A trancher : le controle d'impact « demander 2 vetos par nuit la ou l'equipe n'en fournit qu'un » doit-il etre re-pose sur le nouveau chemin (places par creneau) ?** Aujourd'hui cette porte n'est gardee que si l'on passe par Filou. | Dette technique | Auto-detecte 26/08 | Interne | Qualifie — a trancher | 2026-08-26 |
| B-030 | **`validerConfigBrique` ne valide rien en production** — il n'est appele que par son propre test. Decouvert le 26/08 : supprime par erreur en le prenant pour un simple ré-export, il DEFINIT en realite cette fonction, et son test s'est casse. Restaure et declare dans le garde-fou anti-code-mort, mais sans complaisance : un validateur de schema que la production n'appelle jamais ne garde rien. **A trancher : le brancher la ou une config de brique entre dans le systeme, ou l'assumer comme une aide de test.** | Dette technique | Auto-detecte 26/08 | Interne | Qualifie — a trancher | 2026-08-26 |
| B-031 | **L'appel aux volontaires n'a JAMAIS tourne — on ne peut donc pas dire qu'il fonctionne.** Question de MiKL le 26/08 : « est-ce qu'on est sur que la fonction appel aux volontaires fonctionne bien ? ». Reponse honnete : **non, faute de preuve.** `email_log` ne contient AUCUNE ligne de type `appel_volontaires` — la fonction n'a jamais ete declenchee en reel. Ce qui EST verifie : le type est bien autorise par la contrainte `email_log_type_check`, le code journalise succes ET echec, et les garde-fous de la route sont en place (admin, cabinet, creneau encore impacte, candidats legaux recalcules). Ce qui ne l'est PAS : l'envoi effectif, le lien recu, et le parcours du veto qui clique. ⚠️ **Obstacle a la recette : aucun veterinaire du cabinet bac a sable n'a d'adresse e-mail** (`email` a NULL sur les 7) — un appel aux volontaires n'y enverrait rien. Il faut soit des adresses de test (alias de MiKL), soit un test automatise de la chaine. **Contexte utile : sur les 29 e-mails jamais journalises, 9 sont partis et 20 ont echoue** — tous explicables (adresses `@guardveto.local` fictives sans MX, et expediteur non valide chez Brevo), et plus rien depuis le 21/08. | Verification | MiKL | Interne | Qualifie — a prouver | 2026-08-26 |
| B-022a | **⚠️ B-022 avait ete livre DANS DU CODE MORT** — la `key` etait dans `MonthView.tsx`, que rien n'importe ; l'ecran reel est `PlanningV2.tsx`. Le controle de convergence verifiait que la ligne EXISTAIT, pas qu'elle etait ATTEIGNABLE. **Un grep prouve qu'un code est ecrit, jamais qu'il est execute.**  Detail integral → archive 12. | Correctif | Auto-detecte 26/08 | Interne | **Livre** | 2026-08-26 |
| B-026 | **Fenetre « Reparer le planning » portee sur le systeme de design.**  Detail integral → archive 12. | Retouche visuelle | MiKL | Interne | **Livre** | 2026-08-26 |
| B-027 | **Code mort de la V1 — recense PAR MESURE puis nettoye.** 294 fichiers, 274 atteints ; **14 supprimes** (~1500 lignes), 4 faux positifs documentes dont `src/proxy.ts`, qui est le middleware d'authentification (Next 16 le charge lui-meme). Garde-fou pose : `tests/lib/aucun-code-inatteignable.test.ts`. Detail integral → archive 12. | Dette technique | Auto-detecte 26/08 | Interne | **Livre** — 14 fichiers supprimes | 2026-08-26 |
| B-028 | **LIVRE — le test qui refuse le code inatteignable.** Feu vert de MiKL le 26/08.  Detail integral → archive 12. | Process | MAX | Interne | **Livre** | 2026-08-26 |
| B-022 | **Le formulaire d'absence etait en retard d'un CLIC** — on pouvait declarer absent le mauvais veterinaire, sur un planning publie.  Detail integral → archive 12. | Correctif | MiKL (recette) | Interne | **Livre** | 2026-08-26 |
| B-023 | **Une regle affichait l'IDENTIFIANT TECHNIQUE d'un veto retire de l'equipe.** Six replis divergents, alors que la phrase juste au-dessus savait deja le dire en francais. Source unique + test. Piege : un cycle d'imports aurait rendu `undefined`. Detail → archive 10. | Correctif | MiKL (recette) | Interne | **Livre** | 2026-08-26 |
| B-024 | **Le bac a sable etait inexploitable** : le clone n'avait pas remappe les references, les 11 regles du Demo pointaient vers les vetos de Val d'Allier. Remappe et verifie personne par personne. ⚠️ Le CLONAGE lui-meme n'est pas corrige. Detail → archive 10. | Correctif (donnees) | Recette 26/08 | Interne | Partiellement livre | 2026-08-26 |
| B-025 | **L'amorcage pose des plannings `verrouille` avec des dates FUTURES**, ce qui fait disparaitre le lien « Absent·e » sans explication. ⚠️ Val d'Allier est dans le meme cas. Detail → archive 10. | Dette technique | Recette 26/08 | Interne | Qualifie | 2026-08-26 |
| B-026 | **Fenetre « Reparer le planning » portee sur le systeme de design.** Cause reelle : elle n'avait jamais ete portee sur la V2 (5 couleurs Tailwind par defaut). L'avertissement passe sur `.gf-card.souple` comme les 4 autres ecrans, le sortant redevient lisible, le pave vert passe sur `--ok-soft`. Piege attrape : 2 des 3 ecrans qui l'ouvrent n'importaient pas le CSS. Detail → archive 10. | Retouche visuelle | MiKL | Interne | Qualifie | 2026-08-26 |
| B-003 | Compteur de jours a rattraper (formation pendant conges). Design MiKL 24/08 : compteur par veto, pose en joker valide par l'admin, jour bloque en dur. Bloque sur 6 questions a Anne-Sophie (→ archive 3bis / 8bis). | Feature | Anne-Sophie/MiKL | A qualifier* | Qualifie — bloque | 2026-08-24 |
| B-019 | **REGLE PERMANENTE : Filou suit le produit, et c'est un REFUS qui l'impose.**  Detail integral → archive 12. | Process | MiKL | Interne | Livre | 2026-08-25 |
| B-018 | **Filou mis a jour des nouveautes du 25/08** — 4 outils secretariat, prompt systeme, orientation vers l'onglet Assistance. Le risque n'etait pas « il ne sait pas faire » mais une reponse incomplete presentee comme complete. Detail integral → archive 9. | Correctif | MiKL | Interne | Livre | 2026-08-25 |
| B-007 | **Audit de couverture de Filou — VOLET 2 RENDU.** Les 6 trous n'etaient pas oublies, ils etaient INVISIBLES : le test ne lisait que les `actions.ts`, les 18 routes API y echappaient.  Detail integral → archive 12. | Audit | MiKL | Interne | **Livre** | 2026-08-26 |
| B-021 | **Les 6 outils Filou manquants, nommes par B-007 volet 2.** Par valeur decroissante : ① **se porter volontaire** (geste veto le plus courant, aujourd'hui Filou explique puis dit « va sur l'ecran ») ; ② **modifier une garde** (retouche libre admin) ; ③ export PDF ; ④ lancer une generation ; ⑤ relancer la synchro agenda ; ⑥ bilan de fin de periode. ⚠️ **① et ② ecrivent sur les chemins les mieux gardes du produit** — `volontaire` emploie le SERVICE ROLE derriere 5 verrous, `PATCH gardes` porte regles dures + perimetre jour/bloc + 409 + audit. L'outil devra passer par une fonction partagee extraite de la route, **jamais reimplementer ces controles** (meme geste que `lib/crise/changements.ts` pour T-006). A recetter, donc a ne pas empiler sur une autre livraison. | Feature | Audit B-007 | Interne | Qualifie — Should | 2026-08-26 |
| B-009+B-013+B-014 | **LIVRE `360794a`** — les trois correctifs commites et pousses, verifies par execution reelle (tsc propre, 1253 tests verts, rejoues deux fois independamment). Detail integral → archive 9. | Correctif | Audit B-007 | Interne | Livre | 2026-08-24 |
| B-010 | **Clos par la MESURE, et le verdict de dette etait perime** : les vues sont bien en `security_invoker` (verifie en base), 0 erreur au linter, et les deux documents accuses disaient vrai. Ce qui manquait etait la trace de l'episode. Detail → archive 10. | Dette technique | Audit B-007 | Interne | **Livre** | 2026-08-26 |
| B-011+B-012 | **LIVRE `5657b30`** — 22 fichiers. Filou distingue enfin « la base a repondu, il n'y a rien » de « la base n'a pas repondu », et les sources consultees s'affichent. Detail integral → archive 9. | Correctif | Audit B-007 | Interne | Livre | 2026-08-24 |
| B-011+B-012-ancien | Remplace par la ligne ci-dessus — etat intermediaire ou le build etait rouge, conserve pour la lecon (ne pas livrer sur une verification portant sur un etat anterieur). Detail integral → archive 9. | Correctif | Audit B-007 | Interne | Remplace | 2026-08-24 |
| B-015 | **LIVRE `f227030`** — selecteur compacte en panneau flottant, tactile intact, pipette conditionnelle.  Detail integral → archive 12. | Retouche visuelle | MiKL | Interne | Livre | 2026-08-24 |
| B-011 | **Solde** — le gros etait livre le 24/08 (`5657b30`), il restait UN reliquat (`equipe.ts:352`) : une base muette faisait proposer « Inviter X » a quelqu'un ayant deja un compte. Detail → archive 10. | Correctif | Audit B-007 | Interne | **Livre** | 2026-08-26 |
| B-012 | **Rien ne veille sur ce que Filou REPOND.** Le second gardien (`agentFilou.ts:567-645`, appel isole + `tool_choice`) controle l'omission d'une action, pas l'invention d'une reponse. Deux chemins laissent passer un texte non fonde : `agentFilou.ts:291-304` (si le modele ne demande aucun outil au premier tour, son texte libre part sur le tableau) et `afficher.ts:20-41` (titre, introduction et lignes en texte libre, affiches tels quels). **Le materiau du controle existe deja et n'est branche nulle part** : `outilsAppeles` est constitue a chaque tour (`agentFilou.ts:332`) puis jamais teste, ni meme transmis au client. Deux options a trancher par MiKL : refuser d'afficher une reponse portant sur le cabinet quand aucune lecture n'a eu lieu, ou remonter les sources a l'ecran. | Decision produit | Audit B-007 | Interne | A trancher par MiKL | 2026-08-24 |
| B-013 | **LIVRE `360794a`** — le jumeau orthographique desactivait des regles en silence (etiquette « seniors » ecrite par Filou la ou l'equipe utilise « senior » : le geste reussit, et les regles cessent de s'appliquer). Detail integral → archive 9. | Correctif | Audit B-007 | Interne | Livre | 2026-08-24 |
| B-014 | **LIVRE `360794a`** — l'expediteur des e-mails n'etait pas revalide cote serveur ; une adresse invalide faisait tomber les sept chemins d'envoi, silencieusement. Detail integral → archive 9. | Correctif | Audit B-007 | Interne | Livre | 2026-08-24 |
| T-006 | **L'ecran de crise consulte enfin le gardien des regles.** Traduction partagee sortie dans `lib/crise/changements.ts`, validation separee de l'ecriture, 409 + modale + trace audit. Piege evite : `onClick={handleAppliquer}` aurait confirme d'office. Detail → archive 10. | Dette technique | Audit B-007 | Interne | **Livre** | 2026-08-26 |
| T-007 | **LIVRE `b82eccb`** — les trois appels IA suivent enfin le meme reglage. Contient la correction d'une affirmation fausse (« Filou a tourne un mois sur Opus » etait FAUX). Detail → archive 11. | Cout | Audit B-007 | Interne | Livre | 2026-08-24 |
| B-008 | **REQUALIFIE le 26/08 : n'est PLUS bloquant, et ne l'etait deja plus quand B-017 est parti en prod.** Verification avant tout code : le booleen `estAdmin` et le filtre `!adminSeulement \|\| estAdmin` existent bien, tels que decrits. **Mais la faille redoutee est fermee un cran plus haut** — `filou/actions.ts` refuse EXPLICITEMENT le secretariat (lot B-017 du 25/08), avec le commentaire qui dit pourquoi le refus ne doit pas rester un effet de bord. Une secretaire n'atteint jamais le catalogue. **Ce qui reste est de la dette latente, pas une faille** : le jour ou un 3e role doit parler a Filou, le booleen ne suffira pas. ⚠️ **La ligne de board a affiche « bloquant avant le role secretaire » pendant que le role secretaire partait en production** — le board decrivait un etat que le code avait deja depasse, comme B-010 et B-011 le meme jour. | Dette technique | Audit B-007 | Interne | Qualifie — Could, sans urgence | 2026-08-26 |
| — | Mise en service Val d'Allier : Anne-Sophie relit les regles, valide les souhaits, genere le brouillon, verifie, invite l'equipe, publication a deux avec MiKL | Feature (rollout) | Anne-Sophie | A qualifier* | En cours | 2026-08-24 |
| T-001 | **Le repli d'agenda Google devient NOMINATIF.** Il etait accorde a tout cabinet sans agenda : au 2e client, c'est ecrire dans l'agenda d'un autre. Preuve trouvee en base (valeur bidon posee a la main pour se neutraliser). Detail → archive 10. | Dette technique | Audit interne | Interne | **Livre** | 2026-08-26 |
| T-002 | **Re-validation continue aveugle aux exceptions par jour.** Verifie le 26/08 : `revaliderPlanning` reconstruit le planning depuis la table `gardes`, alors que la vue `planning_semaine` applique les exceptions (migration `20260820151000`). Un jour remplace a titre exceptionnel est donc juge sur son titulaire d'origine — la re-validation peut crier une violation qui n'existe plus, ou taire une violation reelle. ⚠️ **NON CORRIGE VOLONTAIREMENT : ce n'est pas un oubli, c'est un arbitrage produit non tranche.** Le projet a deja decide qu'une exception d'un jour ne compte PAS dans le rythme (`avertissementsReglesDuresJour` : « un seul creneau ne forme ni paire ni serie »). Appliquer betement les exceptions au validateur ferait donc apparaitre des violations fantomes — le defaut « une regle souple noyee par un faux positif » deja paye ici. **Question a trancher : que doit dire le bandeau sur un jour exceptionnellement remplace ?** | Dette technique | Audit interne | Interne | Qualifie — bloque sur arbitrage | 2026-08-26 |
| T-003 | **Les compteurs montrent les vetos a zero garde.** Le dernier recours disparaissait quand tout allait bien, et la moyenne d'equite etait calculee sans lui. Mesure avant/apres : 14 lignes contre 14, 0 divergence. Migration appliquee. Detail → archive 10. | Dette technique | Audit interne | Interne | **Livre** | 2026-08-26 |
| T-004 | Suivi des migrations non fiable (appliquees mais absentes de la liste officielle) | Dette technique | Audit interne | Interne | Qualifie | 2026-08-24 |
| T-005 | Feries en semaine absorbes par le creneau ordinaire. **Non traite le 26/08** : contrairement a T-001/T-003, ce n'est pas un defaut technique a corriger mais une regle metier a etablir (un ferie tombant un mardi doit-il etre couvert comme un dimanche, comme un soir de semaine, ou selon un reglage du cabinet ?). Aucune correction possible sans cette reponse — et c'est une question pour Anne-Sophie. | Dette technique | Audit interne | Interne | Qualifie — bloque sur arbitrage metier | 2026-08-26 |
| T-008 | **Kit realtime reporte** : les 5 abonnements signalent desormais leur echec au lieu de se taire. Restent 3 protections du kit a reporter, non urgentes. Detail → archive 10. | Dette technique | FORGE 25/08 | Interne | Partiellement livre | 2026-08-26 |

**Etat de verification de l'audit B-007** — le pave de controle du 24/08 (protocole `git log`
puis `git show HEAD:...` fichier par fichier) est **archive** : ses trois verdicts ouverts ont
tous ete rejoues et tranches le 26/08 (B-011 solde, B-012 toujours a trancher par MiKL, B-008
requalifie non bloquant). Detail integral → archive 3ter.

> Limite volontaire : pas plus de 3 items « En cours » — respectee (2/3 : B-002, rollout).

---

## 4bis. Convergence — controle de sortie avant commit

> **Rempli par OTTO avant CHAQUE commit qui contient du code.** Une ligne par element annonce.
> C'est la seule section que le harnais lit pour autoriser un commit : sans entree fraiche
> pour l'item concerne, `gate-commit` refuse. Ce n'est pas de la paperasse — c'est la reponse
> a la seule question qui compte : *a-t-on livre ce qui etait demande, en ENTIER ?*

| ID | Ce qui etait annonce | Ce qui est reellement livre | Verdict |
|---|---|---|---|
| B-093 | Donner a Filou la faisabilite des ECHANGES, pas seulement des remplacements | `engine/relecture/echanges.ts` — les permutations que `isValid` accepte, jamais une estimation. **Controle CUMULATIF** (le 2e mouvement juge sur le planning ou le 1er est deja pose), comme `arbitrer.ts` | OK |
| B-093 | 💣 **Un faux refus trouve PAR LE TEST, pas par la relecture** | Un echange de roles au sein d'un meme soir etait refuse. Cause : **R18 interdit de designer le 2e avant le 1er** — une regle sur l'ORDRE DE CONSTRUCTION du solver, pas sur le planning fini. Le refus portait sur l'ordre dans lequel on ecrivait, pas sur ce qu'on ecrivait. Corrige en essayant **les deux ordres de pose** : le planning final est identique, qu'un seul passe suffit | OK |
| B-093 | Corriger la phrase du prompt qui produisait les silences | « Si la liste est vide, personne d'autre ne peut prendre cette place » remplacee par « personne ne peut la prendre SANS QU'ON BOUGE AUTRE CHOSE », suivie de l'explication et du renvoi vers la liste des echanges | OK |
| B-093 | Ne pas noyer le dossier | Ciblage sur les 2 personnes les plus chargees et les 2 moins chargees. Sans filtre, 118 places donnent ~6 900 paires : le calcul tient, le budget de jetons non. **Le filtre est teste dans les deux sens** — il ne filtre ni tout ni rien | OK |
| B-093 | Preuves d'execution | `npx vitest run src/engine/__tests__/relecture-echanges.test.ts` → **9 passed** (dont le piege « verifier un seul cote du deplacement », celui commis a la main le 01/09) · suite complete → **1605 passed**, 1 skipped · `npx tsc --noEmit` → 0 erreur · `npx eslint` sur les 5 fichiers → 0 avertissement · `npm run build` → « Compiled successfully in 21.0s » | OK |
| B-093 | ⚠️ Ce qui n'est PAS verifie | **Aucune relecture reelle n'a ete relancee.** On ne sait donc pas encore combien des 7 constats deviennent des propositions appliquees — c'est la recette de MiKL, et c'est la seule mesure qui compte | ⚠️ a recetter |
| B-092 | Lot 1 — le week-end compte ses 3 nuits dans l'espacement | `ecartReelEntreGardes` dans les DEUX gardiens (`hard-constraints` ET `validerPlanning`, qui ne s'importent jamais l'un l'autre). Branche sur `joursCouvertsGarde`, la source qui existait deja — aucune notion creee. **Defaut REPRODUIT AVANT correction** : 3 tests ecrits, 3 tests rouges, puis verts. `npx vitest run tests/engine/espacement-min.test.ts` → **15 passed** | OK |
| B-092 | Lot 2 — l'equite passe devant les preferences au-dela d'un seuil | Etage `EQUITE_CRITIQUE` + `ORDRE_COMPARAISON`. **L'enum n'a PAS ete renumerote** : les etages 0..6 sont traduits depuis la fermete des regles (`FORCE_TEXTE_VERS_ETAGE`), les decaler aurait transforme « a eviter » en « souhait » sur des regles deja posees, sans que personne ne touche a rien | OK |
| B-092 | 💣 **Le lot 2 etait INERTE — trouve en le mesurant, pas en le relisant** | L'etage etait pose, 12 tests verts, suite complete verte. **Mesure sur une vraie generation, ecart de 6 (le double du seuil) : ZERO difference.** Cause : le LNS detruit une semaine et la REPARE avec `scorerCandidat`, reste inchange — il reconstruisait toujours la meme semaine, donc `comparerScores` n'avait jamais deux plannings a departager. **Un classement ne sert a rien sans diversite a classer.** Complete par `facteurCritique` dans `scorerCandidat`. **Effet re-mesure : ecart 6 → 3**, le veto qui decrochait passe de 6 a 8 | OK |
| B-092 | Le garde-fou contre le retour a l'inertie | `tests/engine/equite-critique.test.ts` — 13 tests dont un qui fait tourner `genererPlanningPur` en entier et compare avec/sans seuils. Il rougit si le correctif redevient inerte, ce qu'aucun test unitaire ne pouvait attraper | OK |
| B-092 | Ne rien casser en dessous du seuil | `npx vitest run` → **1596 passed**, 1 skipped. **Les golden tests du pilote sont inchanges** : il ne franchit aucun seuil, son comportement est byte-identique. C'est la moitie de la promesse qui compte le plus — un etage qui crierait sur tout serait desactive le premier jour | OK |
| B-092 | Preuves d'execution | `npx tsc --noEmit` → 0 erreur · `npx eslint` sur les 7 fichiers touches → **0 avertissement** · `npm run build` → « Compiled successfully in 22.5s » | OK |
| B-092 | ⚠️ Ce qui n'est PAS livre, et se dit | ① **Les seuils ne sont pas reglables a l'ecran** → item **B-092a** cree, pas absorbe en silence. ② **Aucune generation reelle n'a ete relancee sur Hiver P2** : l'effet est prouve sur un cas construit, pas sur les donnees du bac a sable. C'est la recette de MiKL. ③ Les lots 3 a 6 du plan (rattrapage inter-periode, exploration elargie, marche sur une regle, dossier Filou) ne sont pas commences | ⚠️ assume et trace |
| B-091 | Remapper les 13 regles du bac a sable sur les bons veterinaires | `UPDATE` execute, **13 lignes retournees** par le `RETURNING`. Verification apres coup : 14 refs valides, **0 orpheline**, **0 occurrence** d'un ancien identifiant dans tout `regles_cabinet` du cabinet demo. Les 14 refs relues une a une : Anne-Sophie x3, Victor x3, Manon x3, Antoine x2, Anne-Catherine, Fanny, Jean | OK |
| B-091 | Un garde-fou pour que la prochaine copie ne recommence pas | Trigger `trg_regle_refs_du_meme_cabinet` applique en prod (migration `20260831120000`). **Refus reellement obtenu** : l'`UPDATE` de test rejouant le defaut d'origine remonte `ERROR: 23514 … designe le veterinaire 00000000-…-0005 qui n'appartient pas au cabinet 3f1a2062…`, et la regle visee est restee intacte. Verifie AVANT de poser qu'il ne bloquerait aucune ecriture existante : les DEUX cabinets sont a 0 reference orpheline | OK |
| B-091 | Ne PAS casser le cas legitime du depart d'equipe | Le trigger compare le CABINET, jamais `actif`. Un veterinaire du bon cabinet devenu inactif passe — c'est le cas que le pre-vol sait deja nommer avec le prenom de la personne. Portee volontairement etroite aux `qui.type` qui designent des personnes, pour ne pas bloquer un futur type (cohorte, groupe) dont les refs designeraient autre chose | OK |
| B-091 | ⚠️ **ECART au plan valide par MiKL — les snapshots ne sont pas remappes** | Le plan disait « remapper les 27 snapshots ». En les ouvrant : 12 sur 27 sont rattaches a de vraies periodes du bac a sable et servent au **replay** d'une generation. Un snapshot est une archive de ce qui a ete APPLIQUE : le remapper rendrait l'archive infidele au planning qu'elle documente, et le replay ne reproduirait plus le resultat archive. **Second piege trouve au passage** : 24 snapshots contiennent un `cabinet_id` du Val d'Allier a l'interieur du JSON — un remplacement textuel l'aurait transforme en identifiant de veterinaire. Toute nouvelle generation produira desormais un snapshot correct | ⚠️ **ecart assume, non fait** |
| B-091 | ⚠️ Ce qui n'a PAS ete verifie | **Le pre-vol n'a pas ete rejoue a l'ecran** — il exige une session admin. Ce qui est prouve, c'est la condition exacte que son detecteur applique (`contraintesParVet` dont le veterinaire n'est pas dans l'equipe), reproduite en SQL : 0. Le rejeu sur Hiver P2 dans le bac a sable reste la recette de MiKL | ⚠️ a recetter |
| B-086 | Fusionner les deux encarts Google Agenda, corriger le debordement, rééquilibrer | UNE carte pleine largeur avec deux zones titrees — « Branchement » (contenu d'origine, aucune logique touchee) et « Presentation des evenements ». Selecteur raccourci en « Par defaut » + troncature en filet ; phrase d'explication sortie de la grille a deux colonnes qui la rognait | ⚠️ **JAMAIS VU A L'ECRAN** — recette MiKL |
| B-087 | « 1er » / « 2nd » au lieu de « premier » / « second », et pareil au-dela | `roleCourt` dans le module feuille, cablee dans `google-calendar.ts` ET dans l'apercu de l'ecran (meme fonction, meme chemin — sinon l'ecran aurait menti). **La base n'a pas ete touchee.** Convention non inventee : celle de `roleClair`, qui existait deja | OK (agenda) · ⚠️ apercu jamais vu a l'ecran |
| B-087 | La dette de duplication ouverte en la corrigeant | Table `ROLES_COURTS` unique, partagee ; **la fonction n'a PAS ete deleguee** parce qu'un test de non-regression a prouve 4 divergences qui auraient change un ecran existant. Aucun test existant n'a bouge | OK |
| B-079 | L'agenda affiche 20h-10h la ou la structure dit 18h-08h | Cause **prouvee par sonde** (TZ=UTC reproduit 20:00/10:00 sur le vrai creneau) puis corrigee : `dateTime` sans suffixe + `timeZone` a cote, plus aucune `Date` locale dans le fichier | OK |
| B-078 | Evenements en **journee entiere** | `mode` journee/horaire, defaut journee, lu depuis `cabinets.agenda_journee_entiere`. **Fin exclusive verrouillee par 3 tests** (29/09 → end 30/09 ; week-end ven→dim ; bascule d'annee 31/12 → 01/01) | OK |
| B-078 | **Un evenement par personne ET par jour**, samedi et dimanche separes | `planifierEvenementsGarde` (fonction pure) + table `garde_evenements` (garde, jour, place). Jours issus de `joursCouvertsParGarde`, jamais recalcules | OK |
| B-078 | La **couleur Google** choisie par l'admin, par veto | `veterinaires.couleur_google` (CHECK '1'..'11', **refus reellement teste en prod**), selecteur des 11 teintes dans la fiche veto avec le `Select` du projet | OK |
| B-078 | Les **initiales**, ACB / ASB pour les deux homonymes | `initialesVeto` + `initialesUniques`, les 7 vetos de Val d'Allier verrouilles en tests. **Calculees sur TOUT le cabinet**, pas sur les seuls vetos de la periode | OK |
| B-078 | L'intitule **composable** : base + nom + role, horaires en option | `libelleGarde` → `garde-ACB-1er` / `garde-ACB-1er-18h/08h`. Base **par creneau** (anticipation V3), apercu en direct dans Reglages sur les VRAIS horaires | OK |
| B-078 | Un **module de personnalisation** dans Equipe + Reglages | Bloc « Google Agenda » sur la fiche veto ; carte « Presentation dans Google Agenda » dans Reglages + RPC `configurer_agenda_cabinet` (sans lui le bouton n'aurait pas fonctionne) | ⚠️ **livre, JAMAIS VU A L'ECRAN** — recette MiKL |
| B-078 | La **bascule** des 20 evenements existants | supprimer → creer → effacer l'id ; **idempotence prouvee** (« relancee deux fois : toujours 8, jamais 16 »), reprise apres interruption, un evenement qui resiste garde son id | ⚠️ **teste en simulation, JAMAIS contre Google** |
| B-081 | Les lecteurs devenus aveugles aux nouveaux evenements | 4 sites cables (pas 5 : l'agent a corrige sa propre liste), grep exhaustif sans sixieme, test d'ordre **verifie par sabotage** — il rougit quand on casse l'ordre | OK |
| B-081 | `/api/calendar-sync` coupee a 10 s | `maxDuration = 60` pose, verifie ligne 34 | OK |
| — | Migrations appliquees en production | 3 migrations passees, controles EXECUTES : 5 colonnes, CHECK qui refuse pour de vrai, `anon` en « permission denied », isolation RESTRICTIVE, RPC signes et fermes a `anon` | OK |
| — | Ce qui N'EST PAS livre, et se dit | **Aucun appel reel a Google** de tout le chantier · les 2 ecrans jamais vus dans un navigateur · **B-082** (plus de description), **B-083** (l'ete frole les 60 s), **B-084** (golden test pointant vers du vide), **B-085** (8 fonctions ouvertes a `anon`) ouverts au board plutot qu'absorbes en silence | ⚠️ assume et trace |
| B-076 | Un bouton « Alterner les deux roles » entre les deux places | `GardeDetailModal.tsx` — il echange les deux SELECTIONS et **n'enregistre rien** : l'admin voit le resultat avant de valider, et l'inversion repart par le meme `PATCH /api/gardes/{id}` que n'importe quelle retouche. **Aucun chemin d'ecriture de plus**, donc les memes gardiens (lecon des « trois chemins d'ecriture, deux gardiens », 22/08) | OK |
| B-076 | ⚠️ Mon inquietude sur le bloc vendredi+week-end etait INFONDEE — verifie, pas suppose | Je craignais qu'inverser un week-end soit refuse faute de toucher au vendredi. **L'infrastructure existait deja** : le selecteur « ce seul jour » / « tout le week-end, vendredi compris » est en tete de la modale, et `performSave` envoie les roles NATIFS en perimetre bloc (le serveur propage au vendredi) ou les roles AFFICHES en perimetre jour. Le bouton n'avait donc rien de special a faire — le perimetre choisi plus haut fait le reste. Trouve en lisant le code avant d'ecrire, plutot qu'en construisant une parade a un probleme qui n'existait pas | OK |
| B-077 | Le titre porte le DEFAUT, et l'explication passe AVANT les cases | « Quelles gardes ce jour-la ? **Par defaut, toutes** », puis « Ne coche que si le veterinaire est indisponible pour *certaines* gardes de ce jour ». L'explication existait deja — mais APRES les trois cases, en petit : on rencontrait des cases a cocher sans savoir ce qu'elles font. Meme famille que B-069 | OK |
| B-077 | Les deux mots colles sont corriges | « la fiche est **régléesur** » et « Pour **larendre** disponible », vus par MiKL le 27/08 sur l'ecran d'impasse. Les espaces qui suivent une expression sont desormais ecrits `{' '}`, jamais laisses au hasard du retour a la ligne. Deux mots colles au meme endroit — apres une expression — n'etaient pas une coincidence, et **un texte abime decredibilise ce qu'il explique** | OK |
| B-076/B-077 | ⚠️ DEUX TESTS INSTABLES trouves en chemin — pre-existants, pas causes par ce lot | `compteurs-appelants` et `generation-exclut-dernier-recours` echouaient en TIMEOUT (5 s) alors qu'ils passent seuls en 4 s : ils **parcourent toute l'arborescence des sources**, donc sont lents et sensibles a la charge. **Verifie par `git stash`** : l'echec est present sur l'arbre PROPRE, sans aucune de mes modifications. Budget porte a 30 s. Un garde-fou qui crie sans raison finit par etre ignore, et le jour ou il a raison personne ne le croit | OK |
| B-076/B-077 | Preuves d'execution | `npx tsc --noEmit` → 0 erreur · `npm test` → **1478 passed**, 1 skipped · `npm run build` → « Compiled successfully in 97s » · `npx eslint` sur les 5 fichiers touches → **2 avertissements, tous deux PRE-EXISTANTS** (verifie par `git stash` : identiques avant mes modifications, sur `RegleFormDialog` ligne 490 et 496, hors de la zone touchee). **NON verifie a l'ecran** — c'est la recette de MiKL | ⚠️ a recetter |

**Regles :**

- **Une ligne par case cochee** au tableau KIT COMPLET, ou par element de l'item du board.
- **Verdict** : `OK` · `partiel` · `absent`.
- **Verifier par la preuve, jamais de memoire** — un grep, un fichier ouvert, un test passe.
  Ecrire « livre » parce qu'on se souvient l'avoir code est exactement l'erreur que ce
  controle existe pour attraper.
- **Partiel ou absent -> ca ne commite pas.** Deux issues, jamais trois : finir maintenant, ou
  creer le reste-a-faire au board (`B-012` -> `B-012a`, avec son perimetre) et le dire a MiKL.
- **Le reste-a-faire porte le numero d'origine** : la filiation doit rester lisible six mois plus tard.
- Ne pas refaire une ligne a l'identique d'un commit a l'autre : le harnais le detecte et refuse.

> Cette section s'archive avec le « Livre » — c'est du passe verifie, reconstituable.
>
> **Archivages du 2026-08-27, deux passes le meme jour** — la journee a produit huit
> lots, et le board a franchi le seuil deux fois.
> · **1re passe** (matin) : convergences anterieures a `082f04e` → archive, section 14
>   (77 lignes, 23 517 octets). Board 101 920 → 78 403.
> · **2e passe** (soir) : convergences de `082f04e` a `45978d3` → archive, section 15
>   (60 lignes, 23 265 octets) ; **et onze recits INBOX d'items DEJA LIVRES** (B-062,
>   B-063, B-064, B-070 a B-077) → archive, section 16, resumes en une ligne dans
>   « Livre ». Board 109 593 → 73 872.
>
> ⚠️ **Ce que la 2e passe a appris** : la section 4bis n'etait pas le vrai poids. L'INBOX
> l'etait — elle gardait, en texte integral, des items **livres dans la journee**. Une
> inbox qui conserve ce qui est fait n'est plus une inbox : c'est une archive qui se
> presente comme une liste de travail, et elle pesait a elle seule un tiers du board.
> Le reflexe a garder : quand un item passe a « Livre », son recit part a l'archive le
> jour meme, et le board n'en garde qu'une ligne.

---

## 4. Livre

| ID | Titre | Commit | Date | Perimetre |
|---|---|---|---|---|
| B-078 | **L'agenda Google eclate : un evenement par personne ET par jour**, en journee entiere, avec la couleur Google et le nom choisis par l'admin, et un intitule composable dont la base vit sur le CRENEAU (anticipation V3). Bascule des 20 anciens evenements idempotente. Deux ecrans de personnalisation (fiche veto + Reglages avec apercu en direct). 3 migrations appliquees en production et controlees. **A RECETTER.** Detail → archive 17. | `e51905a` | 2026-08-27 | A qualifier |
| B-079 | **L'agenda affichait 20h-10h la ou la structure dit 18h-08h** — bug de fuseau, PROUVE par sonde avant correction (sous UTC : 20:00/10:00, exactement ce que voyait la cliente). Plus aucune `Date` locale dans le fichier. Detail → archive 17. | `e51905a` | 2026-08-27 | Interne |
| B-081 | **Quatre lecteurs devenus aveugles aux nouveaux evenements** — une regeneration aurait laisse des orphelins dans l'agenda, et le compteur admin aurait annonce 20 evenements pour 56. Plus `maxDuration=60` sur la route de synchro manuelle, qui aurait ete coupee a 10 s. Detail → archive 17. | `e51905a` | 2026-08-27 | Interne |
| B-062 | **LA RELECTURE DE FILOU — la derniere couche, livree en 5 lots le 27/08.** Filou relit le planning fini avec une doctrine HUMAINE (repos, epuisement, equilibre), propose des changements, le moteur controle leur legalite, et **si c'est legal Filou a le dernier mot**. Ce que le moteur refuse remonte a l'admin avec les deux avis cote a cote. **Revise le principe fondamental** : Filou n'est plus seulement porte-parole, il est co-decisionnaire sous controle de legalite. Seule la legalite fait veto ; le score s'affiche sans refuser. **A RECETTER.** Detail integral → archive 15 et 16. | `e80dcac` | 2026-08-27 | A qualifier |
| B-070 | **La progression de Filou etait affichee et INVISIBLE**, sous plusieurs ecrans de cases a pourvoir. Le code etait juste, le placement le rendait nul. Remontee en haut, et la cause d'un echec devient lisible en un clic. Budget de sortie double (8 000 → 16 000) : ce plafond couvre la reflexion ET la reponse. Detail → archive 16. | `62a5a36` | 2026-08-27 | Interne |
| B-071 | **Filou se taisait parce que MON PROMPT le faisait taire.** Relecture entierement vide sur un planning a defauts evidents — pas une panne, une FAUSSE ASSURANCE. Quatre freins et zero exigence de couverture. Parade STRUCTURELLE, pas une consigne de plus : **le schema exige une ligne par critere**, et un critere non traite se dit « angle mort ». Detail → archive 16. | `f707468` | 2026-08-27 | Interne |
| B-072 | **Un anneau tourne pendant la relecture.** La generation avait deja sa jauge ; c'est l'attente de Filou qui restait figee. Detail → archive 16. | `f707468` | 2026-08-27 | Interne |
| B-073 | **Le rapport devient lisible** — « a vomir », disait MiKL, et le fond etait pourtant juste. **Des UUID bruts s'affichaient** (B-023 qui revient) : traduits a la frontiere moteur→ecran. Le constat passe a UNE phrase de 120 caracteres, le reste se deplie. Detail → archive 16. | `98b98e0` | 2026-08-27 | Interne |
| B-074 | **La relecture se deroule DANS le parcours**, plus apres : « la personne peut fermer avant d'avoir vu la conclusion ». L'ecran ne rend la main qu'une fois tout pret. Detail → archive 16. | `98b98e0` | 2026-08-27 | Interne |
| B-075 | **Filou sait enfin QUI PEUT ALLER OU, donc il peut proposer.** Il constatait sans agir parce qu'il n'avait aucun moyen de savoir si un echange etait legal. Le moteur calcule desormais les remplacants possibles de chaque place avec `isValid` — le meme gardien que le solver. **A RECETTER : est-ce qu'il propose enfin ?** Detail → archive 16. | `45978d3` | 2026-08-27 | A qualifier |
| B-076 | **Bouton « Alterner les deux roles »** dans la modale de garde. Il n'enregistre rien : il echange les selections, l'admin valide, et l'inversion repart par le meme chemin que toute retouche. Detail → archive 16. | `73e7b06` | 2026-08-27 | A qualifier |
| B-077 | **« Quelles gardes ce jour-la ? Par defaut, toutes »** — l'explication passe AVANT les cases. Plus deux mots colles corriges (« régléesur », « larendre »). Detail → archive 16. | `73e7b06` | 2026-08-27 | Interne |
| B-063 | **Nouvelle regle « eviter les gardes la veille d'un repos »** — moteur, catalogue, saisie, libelle, validateur, couverture Filou, tests. Detail → archive 16. | `ca855e6` | 2026-08-26 | A qualifier |
| B-064 | **Filou ne voyait AUCUNE preference restee au defaut** — une preference au defaut n'a aucune ligne en base. Il repondait « rien ne le prevoit » sur des regles actives. Detail → archive 16. | `63aa4cb` | 2026-08-26 | Interne |
| B-066 | **Horodater les demandes de conges et d'absences.** La moitie existait deja et ne se voyait nulle part : `created_at`, `saisi_par`, `valide_par` etaient ecrits depuis la migration 001 et lus par aucun composant. Ce qui manquait : la date de la DECISION (`decide_le`, nouvelle colonne) et l'affichage. Trou trouve en chemin : un REFUS etait anonyme. Les decisions anterieures au 27/08 restent MUETTES sur leur date plutot que d'afficher celle de la demande. **A RECETTER.** | `a4fe5c4` | 2026-08-27 | A qualifier |
| B-067 | **Quatre ordres de lecture pour les conges**, et ils commandent les DEUX listes de l'onglet. Ne fait pas qu'ajouter un choix : supprime une incoherence (les deux blocs suivaient des ordres opposes sans que rien ne le dise). Le dernier `<select>` natif de l'application disparait au passage. **A RECETTER.** | `a4fe5c4` | 2026-08-27 | A qualifier |
| B-068 | **« Organisation » ouvre sur Regles**, plus sur Periodes types. Une ligne. Les liens qui visent vraiment une periode type portent deja leur parametre. **A RECETTER.** | `6725a24` | 2026-08-27 | A qualifier |
| B-069 | **« Places » dit enfin que c'est un PLAFOND.** La notion avait bien sa place sur la structure — c'est le libelle qui mentait par omission. Devient « Places maximum » et annonce ce que chaque periode type en fait. La regle d'affinage, qui existait en 3 copies, est extraite en source unique (`placesEffectives`). **A RECETTER.** | `6725a24` | 2026-08-27 | A qualifier |
| B-065 | **La semaine a cheval sur deux mois s'affiche entiere — avec son contenu.** Demande de MiKL le 26/08 : « pour plus de visibilite sur les semaines ». La grille dessinait deja des semaines pleines, mais les donnees s'arretaient au mois strict : les 1 a 6 cases debordantes etaient **dessinees VIDES**, et une case vide se lit « pas de garde ce jour-la », jamais « pas chargee » (meme famille que B-005). Cause racine : deux calculs dans deux fichiers qui ne se parlaient pas — **fusionnes** dans `lib/planning/bornes-grille.ts`, source unique des deux. Garde-fou : `tests/lib/bornes-grille.test.ts` (5 cas sur 84 mois). Aucune retouche CSS necessaire — mesure faite, `.day.other` n'attenuait deja pas son contenu. **A RECETTER.** | `a venir` | 2026-08-26 | A qualifier |
| B-045 | **Le repos conditionnel n'exige plus ses deux volets.** « Quand il est de garde le week-end il se repose le jeudi ; les autres semaines, rien de particulier » etait impossible a saisir — le formulaire imposait un « sinon ». Les deux gardiens l'acceptaient depuis toujours. **A RECETTER.** | `a venir` | 2026-08-26 | A qualifier |
| B-044 | **Le produit dit « nuit » la ou il disait « jour ».** Exigence de MiKL le 26/08 : une garde de semaine commence le soir et court jusqu'au lendemain matin — « de garde le lundi » veut dire la nuit du lundi au mardi. C'est exactement ce qui pretait a confusion : la regle de Victor porte « pas de garde le lundi » parce qu'il veut ses MARDIS libres. Week-ends et vendredi soir inchanges. **A RECETTER.** | `a venir` | 2026-08-26 | A qualifier |
| B-043 | **Les demi-journees retirees du produit — elles annoncaient une portee que le moteur n'appliquait pas.** Decision de MiKL le 26/08. Conges (saisie, listes, e-mails, Filou) et libelles de regles. Un test verrouillait le defaut : il exigeait que « l'apres-midi » s'affiche. Donnees client intactes. **A RECETTER.** | `a venir` | 2026-08-26 | A qualifier |
| B-041 | **Une regle de repos porte desormais PLUSIEURS jours.** La limite « un seul jour par regle » etait une simplification posee le matin meme (B-038) ; elle est tombee au premier usage reel — Filou, qui ne propose qu'une regle par reponse, abandonnait le mardi sans le dire. Les gardiens bouclaient deja sur les entrees : aucun code moteur ecrit. Le prompt lui interdit desormais de traiter une moitie de demande en silence. **A RECETTER.** | `a venir` | 2026-08-26 | A qualifier |
| B-042 | **Filou distingue un conge ponctuel d'un repos permanent.** Une absence sans fin (« tous les lundis ») ne peut pas etre un conge : reclamer des dates etait une impasse. Il pose desormais la question qui tranche et propose la regle. **A RECETTER.** | `a venir` | 2026-08-26 | A qualifier |
| B-040 | **Filou demandait son prenom a quelqu'un de connecte.** Vu par MiKL le 26/08 : a « je veux une regle pour moi les lundis », reponse « quel est ton prenom ? ». Son contexte portait l'IDENTIFIANT de la personne — inutilisable dans une phrase — mais jamais son NOM, et le prompt disait seulement « tu parles a un membre du cabinet ». Bloc « QUI TE PARLE » ajoute, non cache pour ne pas faire manquer le cache du prompt principal. | `a venir` | 2026-08-26 | Interne |
| B-039 | **Le bloc « Apercu » disait visuellement « rien ici ».** Cadre pointille — le signal d'etat vide partout ailleurs dans le produit — autour de la phrase la plus importante de la fenetre, celle sur laquelle l'admin valide. Passe en cadre plein teinte d'accent, titre « Ce que tu vas enregistrer ». | `a venir` | 2026-08-26 | A qualifier |
| B-038a | **L'apercu ne montrait pas la parite qu'on venait de choisir.** Vu par MiKL a la recette immediate de B-038. La regle etait correctement construite et enregistree — seul l'apercu mentait, ce qui est le pire endroit : c'est la phrase sur laquelle l'admin valide. Cause : variable absente des dependances du `useMemo`, avec un `eslint-disable exhaustive-deps` qui avait desactive le controle capable de le dire. | `a venir` | 2026-08-26 | Interne |
| B-038 | **Repos fixe « une semaine sur deux » — saisissable a l'ecran et par Filou.** Demande de MiKL en seance. **Les DEUX gardiens savaient deja l'evaluer** (forme heritee du cabinet pilote) : seule la saisie manquait, donc aucun code moteur n'a ete ecrit. Piege ecarte au passage : « sauf vacances scolaires » n'etant lu que sur la forme simple, la case disparait avec une parite et le serveur refuse la combinaison — plutot que d'afficher un assouplissement que le planning n'applique pas. **A RECETTER par MiKL.** | `a347632` | 2026-08-26 | A qualifier |
| B-036 | **⚠️ Filou visait la MAUVAISE regle — vu en demonstration devant la cliente.** MiKL demande de mettre en pause le repos du mardi de Victor ; l'encadre « ce que ca changerait » annonce la mise en pause de « Anne-Catherine ne fait pas de garde le mercredi ». Autre personne, autre jour. **Cause prouvee par mesure** : Filou designe une regle par son NUMERO DE POSITION, et les deux cotes lisaient la liste avec des tris differents (`brique_id` seul contre `brique_id` puis `id`). Postgres ne garantit aucun ordre entre lignes de meme `brique_id` — **13 regles sur 22 changeaient de place sur les donnees reelles, les quatre `interdire_creneau` etant integralement inversees**, soit exactement la regle de Victor et celle d'Anne-Catherine. Corrige par UNE lecture unique, pas par le tri manquant. **Le garde-fou a tenu** : l'encadre etant calcule par le code et non redige par le modele, l'erreur etait visible a l'ecran au lieu d'etre appliquee en silence. | `f388974` | 2026-08-26 | Interne |
| B-034 | **Le lien « Je prends ce creneau » ne disait pas a QUI il avait ete envoye.** Trouve par MiKL en recettant le depannage : e-mail adresse a Jean, ouvert depuis la session d'Anne-Sophie, l'app proposait le creneau de Jean a Anne-Sophie sans un mot. Pas une faille (le serveur refuse les non-candidats) mais un trou d'IDENTITE : les verrous demandaient « as-tu le droit », jamais « ce message etait-il pour toi ». Le lien porte desormais `pour=<veterinaire_id>` ; la page ET l'endpoint refusent le decalage ; la reconnexion revient sur le creneau. Cas reels vises : poste partage du cabinet, e-mail transfere. **A RECETTER par MiKL.** | `9ab90df` | 2026-08-26 | A qualifier |
| B-017 | **Role SECRETAIRE — livre et complet le 25/08.** Table `secretaires` SEPAREE plutot que 66 filtres. Lecture stricte, ecriture refusee (403 mesure), Filou refuse cote serveur. Detail, mesures et 5 pieges payes → archive 11. | `23fd1b2`…`b1eee27` | 2026-08-25 | Interne |
| B-005 | **REGLE PERMANENTE : le tableau ne peut plus se taire sur ce qui attend quelqu'un.** 4 fiches manquantes + refus structurel (`lib/produit/attentes.ts` + test qui echoue sur le silence, dans les deux sens). Deux decouvertes en passant : l'accueil n'avait AUCUN temps reel, et un abonnement a une table non publiee echoue EN SILENCE. Detail → archive 11. | `20260825190000` | 2026-08-25 | Interne |
| B-020 | **Les 2 erreurs rouges du Security Advisor, ouvertes depuis le 4 aout.** Tables `_backup_*` lisibles par `anon`. Contenu inventorie AVANT suppression. Verifie APRES par le linter : 0 erreur restante. Cause du non-traitement : personne ne regardait ce tableau de bord. Detail → archive 11. | `20260825191000` (non joue) | 2026-08-25 | Interne |
| B-002 | Selecteur de couleur libre livre (degrade/teinte/hexa/pipette). 27 fichiers, 1223 tests verts, build vert. A recetter a l'ecran. Detail → archive 4bis. | `9aa6676` | 2026-08-24 | A qualifier* |
| B-002 | Recette MiKL du 25/08 : la fiche veto restait desequilibree — le champ couleur ouvrait une rangee a lui seul, quatre colonnes vides a sa droite. Les etiquettes d'equipe remontent a cote du selecteur de couleur. | `314638e` | 2026-08-25 | A qualifier* |
| B-016 | **Onglet SUPPORT livre `9bfaf99` + `9ec3a36` + `d385b46` — RECETTE VALIDEE** (« ca marche nickel », e-mail recu, 3 captures verifiees en base). Depot direct navigateur → Supabase Storage : le plafond Vercel de 4,5 Mo ne s'applique jamais. Deux gardiens sur le fichier. Console de traitement toujours dans le hub MPP. **Detail complet → archive 4ter.** | `9bfaf99` | 2026-08-25 | Interne |
| B-004 | Clos SANS developpement : la regle `eviter_we_avant_vacances` existe deja et tourne par defaut a l'etage « A eviter ». A durcir en « Jamais » avec Anne-Sophie + saisir les conges avant generation. Detail → archive 4bis. | — | 2026-08-24 | Interne |
| B-001 | Couleur des autres vetos : ne se reproduit plus (retestte OK par MiKL). Cause non prouvee — a rouvrir si ca revient. | — | 2026-08-24 | Interne |
| — | Gardien des regles sur les 4 chemins d'ecriture d'une garde | `c829d5a` | 2026-08-22 | LOT 1 |
| — | Dimanche juge comme le samedi | `490bb67` | 2026-08-22 | LOT 1 |
| — | Une seule voix pour les regles enfreintes (4 ecrans unifies) | `c829d5a` | 2026-08-22 | LOT 1 |
| — | Faille : les vues s'ouvraient au visiteur non connecte | `490bb67` | 2026-08-22 | Securite — LOT 1 |
| — | Banc d'essai du solver — refusait les scenarios avec conges | `e22c84c` | 2026-08-22 | — |
| — | Retrait d'un planning publie, agenda compris, 2 confirmations | `e1c7eef` | 2026-08-24 | — |
| — | Fiche veterinaire sans adresse e-mail | `f0e999e` | 2026-08-24 | — |
| — | Cabinet bac a sable clonable, isolation prouvee | `e1c7eef` | 2026-08-22 | — |
| — | Historique ete refait (7-20 septembre, soirs de semaine inclus) | base | 2026-08-22 | LOT 3 |

---

## 5. Ecarte et gele — registre des decisions

| Titre | Decision | Raison | Decide par | Date | Reouvrable ? |
|---|---|---|---|---|---|
| Console support dans le hub MonProjetPro | Reporte | Le support passe par e-mail en attendant le hub et ses API | MiKL | 2026-08-22 | Oui, des que le hub existe |
| Chat entre vetos | Ecarte (V3) | Decide avec Anne-Sophie | Anne-Sophie/MiKL | 2026-08-21 | Oui, en V3 |
| Import de planning | Eteint volontairement (code intact, flag `IMPORT_PLANNING_ACTIF`) | Devient une prestation payante — detail chiffre → archive 5bis / `decisions-produit.md` | MiKL | 2026-08-18 | Oui |
| Role secretaire (lecture seule) | Gele | Pas en meme temps qu'un chantier RLS (dette `security_invoker`) — detail → archive 5bis | MiKL | 2026-08-22 | Oui, hors chantier RLS |

---

## 6. Hors devis a chiffrer

> Section a reconfirmer par MiKL depuis la clarification du 24/08 (GuardVeto = produit MPP en
> abonnement, pas un projet au forfait) : reste-t-elle pertinente telle quelle, ou ces items
> relevent-ils du roadmap produit `Interne` inclus dans l'abonnement ?

| ID | Titre | Demande par | Date demande | Statut chiffrage |
|---|---|---|---|---|
| B-003 | Compteur de jours a recuperer (formation pendant conges) — A qualifier*, voir section 3 | Anne-Sophie | 2026-08-24 | A chiffrer |

B-004 retire de cette section le 24/08 : clos sans developpement (voir section 4), rien a
chiffrer.

---

## 7. En attente d'action externe

| Quoi | Qui doit agir | Bloque quel item | Depuis |
|---|---|---|---|
| **① RECETTE A FAIRE — les nouvelles fiches du tableau apparaissent-elles SANS rafraichir ?** Ouvrir l'accueil sur le deploiement, faire deposer une demande de conge (ou proposer un echange) depuis un AUTRE compte, et regarder l'ecran sans y toucher. **C'est le seul point de B-005 qui n'a pas pu etre verifie cote dev**, et c'est celui qui repond a la question d'origine de MiKL (« pourquoi je ne le sais que si je demande ? »). Si rien n'apparait : la cause n°1 est la publication `supabase_realtime` — mais elle a ete verifiee en base le 25/08 (9 tables), donc regarder plutot la console du navigateur. | MiKL | B-005 | 2026-08-25 |
| **② Activer la protection contre les mots de passe compromis** (Supabase → Authentication → Policies → « Leaked password protection »). Interrupteur de console, aucun code : MAX ne peut pas le faire a la place de MiKL. Verifie chaque mot de passe contre HaveIBeenPwned au moment ou il est defini. **Seul avertissement de securite qui demande une action humaine** — les 26 autres sont du code. | MiKL | B-020 | 2026-08-25 |
| ~~Decider si B-005 (page Absences, LOT 2) se lance maintenant~~ — **tranche le 25/08 : lance et livre.** | MiKL | B-005 | 2026-08-24 |
| **③ RECETTER LA REPARATION D'UNE ABSENCE (T-006, livre le 26/08).** Declarer une absence sur un planning publie, choisir un remplacant qui enfreint une regle du cabinet : une fenetre « Reparation a confirmer » doit s'ouvrir et lister ce qui est enfreint, avec « Appliquer quand meme ». **Point d'attention** : si la fenetre ne s'ouvre JAMAIS, ce n'est pas qu'il n'y a rien a signaler — c'est le symptome du piege `onClick` decrit dans T-006. Si aucune regle n'est enfreinte, le comportement d'avant est inchange (application directe). | MiKL | T-006 | 2026-08-26 |
| **④ ARBITRER T-002 — que doit dire le bandeau sur un jour exceptionnellement remplace ?** La re-validation juge aujourd'hui le titulaire d'origine. La corriger sans trancher cette question ferait apparaitre des violations fantomes. Aucune urgence, mais rien ne peut avancer sans la reponse. | MiKL | T-002 | 2026-08-26 |
| **⑥ QUESTION FORGE (T-008) — `lib/realtime/statut-abonnement.ts` doit-il remonter au catalogue ?** Ma recommandation : **NON, et la question se pose a l'envers.** Ce fichier n'est pas une brique nouvelle : c'est un EXTRAIT du kit `realtime-refresh-supabase-next`, deja inscrit au catalogue le 25/08 — le garde-fou vient de lui, il n'y a rien a lui rendre. Ce qui reste vrai, c'est que GuardVeto n'a repris **qu'une** des quatre protections du kit (la detection d'echec, la plus couteuse). Les trois autres — relecture au retour d'onglet, suffixe unique de canal, singleton du client navigateur — restent a reporter. **La question utile n'est donc pas « extraire ? » mais « finit-on le report ? »**, et elle n'est pas urgente : les 5 abonnements ne se taisent plus, ce qui etait le seul defaut a consequence silencieuse. | MiKL | T-008 | 2026-08-26 |
| **⑤ POSER LA QUESTION DES FERIES EN SEMAINE A ANNE-SOPHIE (T-005).** Un ferie qui tombe un mardi doit-il etre couvert comme un dimanche, comme un soir de semaine, ou selon un reglage ? A joindre aux 6 questions de B-003 deja en attente. | MiKL / Anne-Sophie | T-005 | 2026-08-26 |

---

## 8. Journal des decisions et changements de perimetre

| Date | Evenement | Items | Decide par |
|---|---|---|---|
| 2026-08-27 | **La recette de la relecture de Filou se fait sur les VRAIES donnees de Val d'Allier, avec l'accord d'Anne-Sophie.** MiKL : « je peux le faire directement sur ma cliente elle est ok ». Perimetre de risque nomme avant de commencer : la relecture **refuse tout planning publie ou verrouille** (409), elle ne peut donc toucher qu'un brouillon que l'equipe ne voit pas ; elle emprunte le meme chemin d'ecriture que la generation, donc les gardes **verrouillees sont preservees** et l'agenda Google n'est pas touche ; et une regeneration ecrase tout, ce qui rend l'operation reversible. **Seul point non reversible : aucun.** ⚠️ Le code n'a jamais tourne sur un appel Opus reel — c'est ce que cette recette verifie en premier. | B-062 | MiKL + Anne-Sophie |
| 2026-08-26 | **Fausse alerte sur B-036, et une lecon d'enquete.** Apres le correctif, Filou annonce « une regle n°16 : Victor ne fait pas de garde le mardi, en interdiction ferme » — MiKL ne la reconnait pas et croit le correctif rate. **Sonde executee (`phraseRegle` appele sur la ligne reelle) : le code rend bien « le lundi »**, donc il ne s'agissait pas de cette regle. Verification en base : la regle decrite existe telle quelle, mais dans le cabinet **du Val d'Allier**, en 16e position exactement. **MiKL etait sur le cabinet REEL de la cliente, pas sur le bac a sable** — et toutes les analyses de la matinee (remplacants du 9 septembre, candidats a l'appel aux volontaires, e-mails) portaient sur « Demo MonProjetPro ». Les deux cabinets ont diverge : le repos du lundi de Victor y est `si_possible` d'un cote, `sauf_crise` de l'autre. **Lecon : etablir sur QUELLES donnees un ecran tourne avant de l'expliquer.** Une analyse juste sur le mauvais jeu de donnees est indiscernable d'une analyse fausse. | B-036 | MAX |
| 2026-08-26 | **Session de soldage — et la lecon du jour n'est aucun des correctifs.**  Detail integral → archive 12. | tous | MiKL + MAX |
| 2026-08-25 | **FORGE — le mecanisme « l'ecran se met a jour tout seul » entre au catalogue des modules reutilisables.**  Detail integral → archive 12. | FORGE, catalogue | MiKL |
| 2026-08-24 | **GuardVeto est un produit MonProjetPro** ; les clients (dont Val d'Allier) y accedent par abonnement. Repo `MonprojetPro/guardveto` confirme au bon endroit, la regle projet-client→comptes-client ne s'applique pas. Corrige une question ouverte a tort par OTTO en section 7. | Gouvernance (en-tete) | MiKL |
| 2026-08-24 | « B-009 confirme reel puis corrige (`360794a`). Une verification faite pendant que le correctif etait en cours l'avait conclu a tort faux positif — verifier l'horodatage du depot, pas seulement son contenu.  Detail integral → archive 12. | B-009, B-013, B-014 | OTTO + team-lead |
| 2026-08-24 | B-004 clos sans dev — regle deja active dans le moteur, a durcir avec Anne-Sophie. Detail → archive 8bis. | B-004 | MiKL |
| 2026-08-24 | B-003 — 6 questions metier posees a Anne-Sophie avant tout code. Detail → archive 8bis. | B-003 | En attente d'Anne-Sophie |
| 2026-08-24 | Selecteur de couleur livre (27 fichiers) + 2 fix critiques publies (retrait planning publie, fiche veto sans email). Detail → archive 8bis. | Livre, B-002 | ruflo |
| 2026-08-24 | Reconciliation OTTO (2e passage) : board condense sous la limite d'injection (16,6 → archive), surplus des sections 3/4/5/8 deplace sans perte | toutes | OTTO |
| 2026-08-24 | Reconciliation OTTO (1er passage) : board aligne sur le template, dates de commit corrigees, LOT 2 (B-005) detecte comme chantier oublie | B-005, Livre | OTTO |
| 2026-08-22 | Recette 21/08 → plan en 6 lots ; lots 1, 3, 4 (reporte), 5 (gele) traites, lot 2 laisse de cote. Detail → archive 8bis. | Livre, B-005 | MAX |
| 2026-08-22 | Mise en service Val d'Allier engagee | Backlog | Anne-Sophie/MiKL |
| 2026-08-18 | Reprise d'historique retiree, devient prestation payante — detail → `decisions-produit.md` | Section 5 | MiKL |

---

## 9. Convergence — a-t-on livre ce qui etait annonce ?

> Controle de sortie, avant chaque commit. Une ligne par element annonce, verifie
> PAR LA PREUVE (un grep, un fichier ouvert, un test passe). Ecrire « livre » de
> memoire est exactement l'erreur que ce controle existe pour attraper.

| ID | Ce qui etait annonce | Ce qui est reellement livre | Verdict |
|---|---|---|---|
| B-027 | Ramener le board sous la limite d'injection | 47 842 → 35 659 octets, soit 4,3 Ko de marge sous les 40 000. Mesure par `wc -c`, pas estimee | OK |
| B-027 | Ne rien perdre en allegeant | 42 identifiants toujours presents (`grep` sur les IDs), 11 sections intactes ; l'archive passe de 55 a 67 Ko — exactement ce que le board a rendu | OK |
| B-027 | Garder lisible ce qui reste a decider | Les items OUVERTS (B-029, B-030, B-031, B-032, T-002, B-021) gardent leur detail integral ; seuls les LIVRES sont condenses. Verifie ligne par ligne | OK |
| B-027 | Deux titres devenus faux en condensant | `B-027` disait encore « suppression a trancher » alors qu'elle est faite, et une substitution avait mange un nom de fichier (backticks interpretes par le shell). Les deux relus et corriges | OK |
| B-031 | Le lien de l'appel aux volontaires mene bien a la page de depannage | `src/proxy.ts` pose `suite` (chemin + parametres) avant de rediriger vers /login ; `login/actions.ts` y revient via `suiteSure()`. Verifie a la lecture des deux fichiers, build vert | OK |
| B-031 | La destination ne peut pas devenir un tremplin vers l'exterieur | `suiteSure()` refuse tout ce qui ne commence pas par `/`, tout `//…` (URL absolue deguisee) et `/login` (boucle). Filtre cote SERVEUR ; le client ne fait que transmettre | OK |
| B-031 | Le bac a sable peut reellement envoyer | 7 vetos dotes d'alias `culus.osteo+prenom@gmail.com` (verifie en base), expediteur du cabinet pose sur `vetovaldallier@gmail.com` — la seule adresse dont `email_log` prouve qu'elle est passee | OK |
| B-031 | L'envoi lui-meme, le mail recu, le clic du veto | **ABSENT — c'est justement ce que MiKL va recetter.** Rien ne remplace l'essai reel : aucune ligne `appel_volontaires` n'existe encore dans `email_log` | absent |
| B-090 | Savoir qu'on est sur le bac a sable | Colonne `cabinets.est_bac_a_sable` (migration `cabinets_est_bac_a_sable` appliquee, `success: true`), posee a **true** sur `demo-monprojetpro` et **false** sur Val d'Allier — verifie par `select nom, slug, est_bac_a_sable from cabinets` | OK |
| B-090 | Ne pas coder un client en dur | La marque est une PROPRIETE du cabinet, reglee en base. Aucun identifiant ni slug de cabinet n'apparait dans le composant (`RubanBacASable.tsx` ne connait que la colonne) | OK |
| B-090 | Aucun ecran oublie, meme les futurs | Ruban monte dans les **deux** layouts, `(protected)` et `(v2)` — donc tous les ecrans connectes. Les groupes hors perimetre (`api`, `auth`, `login`, `set-password`) sont DECLARES avec leur raison, pas constates | OK |
| B-090 | Un garde-fou qui MORD | `tests/lib/ruban-bac-a-sable-sur-tous-les-ecrans.test.ts`, 3 tests. **Mutation testee** : ruban retire du rendu de `(v2)` → le test echoue en nommant le groupe fautif ; restaure → 3/3 | OK |
| B-090 | ⚠️ Mon premier test NE MORDAIT PAS | Il cherchait `RubanBacASable` dans le layout — la ligne d'`import` suffisait a le satisfaire, alors que le composant avait ete retire du rendu. Mutation posee : **test vert malgre le defaut**. Corrige en cherchant le MONTAGE `<RubanBacASable`, puis mutation rejouee : test rouge. *Un nom present ne prouve pas qu'un composant est affiche* | OK |
| B-090 | Le ruban se tait en cas de doute | `if (!estBacASable) return null`, et une lecture qui echoue rend `null` elle aussi (bloc `catch`). Une fausse alerte repetee finit par etre ignoree, y compris le jour ou elle est vraie | OK |
| B-090 | Visible dans les deux coquilles | Style EN LIGNE a dessein : V1 (Tailwind) et V2 (jetons propres) ne partagent aucune feuille. Dependre de l'une l'aurait rendu invisible dans l'autre — la ou on aurait cesse de le chercher. Verifie par test (`style={{` present, aucun `className`) | OK |
| B-090 | Ne rien casser | `tsc --noEmit` 0 erreur · `vitest` **1578 passes / 0 echec** (contre 1575 : +3) · `npm run build` « Compiled successfully » | OK |
| B-090 | Que le ruban s'affiche VRAIMENT a l'ecran | **NON VERIFIE — aucun rendu reel observe.** La logique et le montage sont prouves par les tests et le build ; l'apparence, la couleur et la position ne le sont pas. C'est ce que MiKL voit en ouvrant le bac a sable | absent |
| B-089 | Le banc a enfin abouti — verdict | Mesure MiKL du 31/08 sur Hiver P1 (48 places) : **`low` 64,7 s / 12,23 ¢ / 9 criteres sur 9 / 4 problemes** · `medium` 178,3 s / 29,21 ¢ / **0 critere sur 9** · `high` ECHEC (coupe a 24 000 jetons apres 247,6 s) · Opus 4.8 `medium` 115,2 s / 27,31 ¢ / 9 sur 9. **Sonnet 5 a `low` fait aussi bien qu'Opus, 2x plus vite et pour moins de la moitie du prix** | OK |
| B-089 | ⚠️ Resultat contre-intuitif, et c'est LE resultat du jour | **Plus l'application monte, PIRE c'est** sur cette tache. Explication : les compteurs, les absences et la liste des remplacants sont SERVIS TOUT CALCULES ; il reste a lire, comparer et rediger. Donner plus de place a ce travail ne l'ameliore pas, ca l'etale. Ecrit dans le code au-dessus de la constante, pour que « remonter par prudence » ne se refasse pas | OK |
| B-089 | ⚠️ La prod etait CASSEE au moment de la mesure | Le defaut etait `high` — la configuration qui echoue a 247,6 s. `EFFORT_PAR_DEFAUT` pose a **`low`** (`grep -n` ligne 92) : `effortRelecture()` ne rend plus jamais `undefined` | OK |
| B-089 | La justesse verifiee EN BASE, pas au ressenti | Requetes SQL sur `garde_placements` du bac a sable : Antoine 8 gardes dont 1 WE (8/10, 10/10, 13/10, 14/10 mi-octobre), Victor 5 gardes dont 2 WE (3/10, 5/10, 7/10), Jean 8 gardes dont 2 WE. **Les 3 configurations disent VRAI** — elles comptent en JOURS (le WE vaut 2 jours, le vendredi soir est couple au WE). Aucune invention detectee | OK |
| B-089 | ⚠️ Le « 0 sur 9 » de `medium` cachait un rejet SILENCIEUX | 3e occurrence du meme defaut sur cette chaine : les cles de critere etaient comparees a l'octet pres et tout le reste etait jete sans un mot. Une majuscule, un accent ou un tiret suffisait a effacer un constat juste. Correctif : `rattacherCritere()` compare des formes normalisees (jamais un sens), et ce qui reste inconnu est COMPTE dans `entreesNonRattachees`, affiche au banc en rouge | OK |
| B-089 | Garde-fous ajoutes | 6 tests de plus : 4 variantes d'ecriture rattachees (`Role_Avantage`, `role-avantage`, `rôle_avantage`, `ROLE AVANTAGE`), et le comptage de ce qui reste inconnu. `vitest` **1575 passes / 0 echec** (contre 1570 : +5) | OK |
| B-089 | Le banc ne repaie plus un verdict connu | `high` et l'absence de reglage retires des configurations : les deux ont echoue identiquement le 31/08. Le banc passe de 4 a 3 appels — il doit departager des candidats, pas rejouer un echec deja mesure | OK |
| B-089 | Ne rien casser (lot 3) | `tsc --noEmit` 0 erreur · `vitest` 1575 passes / 0 echec · `npm run build` « Compiled successfully in 15.7s » | OK |
| B-089 | Que `low` tienne dans la DUREE et sur une periode de 12 semaines | **NON VERIFIE.** Mesure faite sur 4 semaines et 48 places ; une periode 3x plus grande n'a pas ete essayee. Les 64,7 s laissent de la marge sous les 300 s de la route, mais ce n'est pas une preuve | absent |
| B-089 | Les 2 fonctions demandees (deblocage en cascade, regles souples) | **ABSENT — pas commence, et assume.** Lots 4 et 5, decrits dans l'item du backlog | absent |
| B-089 | ⚠️ MON ERREUR DU LOT 2, corrigee | Les 4 configurations ont echoue en **0,0 s** : « Streaming is required… ». Cause : budget releve a 24 000 SANS passer en flux. Le SDK refuse un appel non diffuse des que `max_tokens` depasse **21 333** (formule `3600 x max_tokens / 128000` > 600 s, dans `client.js:_calculateNonstreamingTimeout`). **Refus LOCAL : aucun appel parti, 0 euro facture** | OK |
| B-089 | L'appel passe en flux | `client.messages.stream(...)` + `finalMessage()` — `grep -c` donne 1 occurrence de `client.messages.stream(` et plus aucun `messages.parse(`. Memes parametres, meme `parsed_output` ; `tsc --noEmit` a 0 erreur confirme que la sortie structuree reste typee | OK |
| B-089 | Que ca ne se reproduise plus | `tests/lib/relecture-budget-impose-le-flux.test.ts`, 3 tests. **Mutation testee, la faute exacte rejouee** : `stream(` remis en `parse(` avec le budget a 24 000 → le test echoue EN NOMMANT la cause (« MAX_TOKENS_RELECTURE vaut 24000, au-dessus du seuil de 21333 ») ; mutation retiree → 3/3. Le message du SDK ne parle QUE de streaming et jamais de `max_tokens` : c'est ce test qui fait le lien | OK |
| B-089 | Ne rien casser (lot 2 bis) | `tsc --noEmit` 0 erreur · `vitest` **1570 passes / 0 echec** (contre 1567 : +3) · `npm run build` « Compiled successfully in 17.3s » | OK |
| B-089 | Que Sonnet 5 rende VRAIMENT une relecture | **TOUJOURS NON VERIFIE — aucun appel a l'API n'a encore abouti.** Le lot 2 avait ete livre sans etre executable ; le correctif est prouve par les tests et le typecheck, pas par un appel reel. C'est ce que MiKL relance au banc | absent |
| B-089 | Le temps reellement gagne | **MESURE le 31/08** — banc lance par MiKL sur Hiver P1 (48 places) : Opus non regle **152,1 s / 35,71 ¢**, Opus medium **128,3 s / 30,41 ¢**. Les deux **au-dessus du plafond de 120 s de la route** : la relecture ne tenait deja plus dans son budget de temps | OK |
| B-089 | Prendre Sonnet 5 (decision MiKL du 31/08) | `modeleRelecture()` rend `claude-sonnet-5` par defaut (`grep -n "claude-sonnet-5'"` → ligne 60). Reversible par `GUARDVETO_IA_MODELE_RELECTURE` sans deploiement | OK |
| B-089 | ⚠️ Les 2 echecs Sonnet 5 n'etaient PAS le modele | `medium` coupe a **16 000 jetons sur 16 000** (notre plafond) ; `low` avait rendu une relecture **complete et correcte**, jetee par NOTRE schema (synthese >320, quatre `detail` >400). Diagnostic pose sur les messages d'erreur du banc, pas suppose | OK |
| B-089 | Une mise en forme ne peut plus detruire un contenu juste | Contraintes de longueur et de cardinalite **retirees du schema** (`grep -c "\.max(" relecturePlanning.ts` → 1, et c'est le commentaire qui l'explique). Appliquees en COUPE dans `normaliserRelecture` : troncature au dernier espace + « … » | OK |
| B-089 | Le budget de sortie | `MAX_TOKENS_RELECTURE` 16 000 → **24 000** (`grep -n` ligne 118). Pas au maximum du modele : au-dela, c'est la connexion HTTP qui devient le risque, et il faudrait passer en flux | OK |
| B-089 | Le plafond de temps de la route | `maxDuration` 120 → **300** dans `api/planning/relecture/route.ts`. Ce n'est pas une estimation : la page du banc a laisse passer un appel de **173,5 s** sans etre coupee, donc la plateforme accepte au moins cela | OK |
| B-089 | Un garde-fou qui MORD | `tests/lib/relecture-longueur-ne-detruit-rien.test.ts`, **8 tests**. **Mutation testee** : `.max(320)` remis sur `synthese` → 1 test echoue ; retire → 8/8. Le test sur le SCHEMA est le vrai garde-fou — les tests de normalisation ne verraient pas la regression, l'echec se produisant avant qu'ils soient atteints | OK |
| B-089 | Ne rien casser | `tsc --noEmit` 0 erreur · `vitest` **1567 passes / 0 echec** (contre 1559 avant : +8) · `npm run build` « Compiled successfully in 19.0s » | OK |
| B-089 | Que Sonnet 5 rende VRAIMENT une relecture | **NON VERIFIE — aucun appel reel depuis les correctifs.** C'est ce que MiKL doit relancer au banc. Les correctifs sont prouves par les tests, pas par un appel a l'API | absent |
| B-089 | Les 2 fonctions demandees (deblocage en cascade, regles souples) | **ABSENT — pas commence, et assume.** Lots 3 et 4, decrits dans l'item du backlog | absent |
| B-089 | Auditer la lenteur de la relecture | Cause trouvee et localisee : `relecturePlanning.ts` ne passait pas `effort` dans `output_config` (verifie par `grep output_config src/` — l'appel ne portait que `format`, la ligne 430 d'origine). Volume mesure a cote : prompt systeme 5 133 caracteres (`awk` + `wc -c`), Hiver P1 = 40 places / 7 personnes (compte en base) | OK |
| B-089 | Trancher Opus ou Sonnet | **NON TRANCHE, et c'est voulu.** Le banc mesure, MiKL decide. Aucun defaut n'a ete change : `modeleRelecture()` rend toujours `claude-opus-4-8` et `effortRelecture()` rend `undefined` tant que la variable n'est pas posee | OK |
| B-089 | Un banc qui mesure temps, cout ET qualite | `src/lib/ia/bancRelecture.ts` : 4 configurations sur le MEME dossier monte une seule fois, chacune chronometree, avec jetons, cout, nombre de criteres traites, problemes, propositions **et les constats en clair**. Un banc qui n'aurait mesure que la vitesse aurait fait choisir le plus muet | OK |
| B-089 | Separer notre lenteur de celle du modele | `preparationSecondes` chronometre `resoudreContexte` + `monterValidationPeriode` + `remplacantsPossibles` + `monterDossierRelecture` AVANT le premier appel, et s'affiche a part | OK |
| B-089 | Rendre le reglage modifiable sans redeployer du code | `GUARDVETO_IA_EFFORT_RELECTURE` (5 crans valides, valeur invalide ignoree), en plus de `GUARDVETO_IA_MODELE_RELECTURE` qui existait deja. Laissee vide = comportement d'aujourd'hui, a l'octet | OK |
| B-089 | Ne rien casser | `npx tsc --noEmit` → 0 erreur · `npx vitest run` → **1559 tests passes**, 1 skip, 0 echec · `npm run build` → « Compiled successfully in 19.4s », page `/admin/banc-ia` presente. Le test `filou-couverture-produit` a **refuse** le premier jet (action serveur sans decision declaree) — garde-fou fait son travail, decision ecrite (`hors`), test vert | OK |
| B-089 | ⚠️ Le piege evite, a garder en tete | `output_config` etait deja utilise pour `format` : ecrire une seconde cle `output_config` dans le meme litteral aurait **ecrase la sortie structuree en silence**, sans erreur. `format` et `effort` sont fusionnes dans UN seul objet, commentaire pose sur place | OK |
| B-089 | Brider effectivement la relecture, tenir sous les 120 s | **ABSENT — pas commence, et assume.** C'est le lot 2 : il attend le chiffre du banc. Le defaut reste `high` en attendant | absent |
| B-089 | Les 2 fonctions demandees (deblocage en cascade, regles souples) | **ABSENT — pas commence, et assume.** Lots 3 et 4 de B-089, decrits dans l'item du backlog | absent |

> **Les 22 convergences anterieures sont archivees** (→ archive 13). Une
> convergence a rempli son role au moment du commit : la garder ici ferait grossir
> le board sans que personne ne la relise. Elle reste consultable, datee et entiere.

## Conventions

- **Identifiants** : `B-nnn` (idees et bugs, convention deja etablie sur ce projet) · `T-nnn`
  (dette technique, introduit le 24/08). Jamais reutilises, jamais effaces.
- **Aucune ligne de code sans ID** quand un ID existe ; les commits techniques sans item dedie
  restent traces par leur hash dans la section Livre.
- **Perimetre par defaut** : `A qualifier`. GuardVeto est un produit MPP en abonnement (decide
  par MiKL le 24/08) : la regle projet-client→comptes-client ne s'applique pas, la majorite du
  dev est `Interne`. `A qualifier*` marque un item ne d'un besoin specifique de Val d'Allier
  (premier client abonne) dont le perimetre commercial n'est pas encore tranche — a confirmer
  par MiKL au cas par cas, jamais suppose par OTTO.
- **Le board tient en une page.** Au-dela de la limite d'injection, le surplus part dans
  `00-product-board-archive.md` (sections `Nbis`) — jamais de perte, jamais de suppression d'ID.
- **Une decision qui n'est pas ici n'existe pas.**

---

*Template MPP applique le 2026-08-24 par OTTO. Board condense le 24/08 (2e passage) pour tenir
sous la limite d'injection du hook OTTO — voir `00-product-board-archive.md` pour le detail
integral de chaque entree condensee.*
