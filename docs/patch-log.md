# Patch Log — GuardVeto

| Date | Fix/Story | Fichiers | Leçon | Capitalisé |
|------|-----------|----------|-------|------------|
| 2026-07-27 | **Filou passe des chemins de code aux OUTILS** — socle (`types.ts` contrat lecture/écriture, `agentFilou.ts` boucle manuelle, `registre.ts` filtré par rôle, action `parlerAFilou`/`appliquerActionFilou`, fenêtre de tableau générique) puis 6 domaines écrits en parallèle par teammates = **50 outils** ; absorption des 2 assistants IA séparés de l'écran Structure ; 8 tests sur la boucle de sûreté ; routeur d'intention supprimé ; 835 tests (`bbad41a`+`8e05c9c`) | lib/ia/outils/{types,registre,equipe,regles,planning,conges,absences,echanges,compteurs,structure}.ts, lib/ia/agentFilou.ts, app/(protected)/filou/actions.ts, v2/FilouResultat+FilouChat, tests/ia/boucle-filou.test.ts | 1) Quand chaque capacité demande un chemin de code, il en manquera toujours un : passer à un catalogue d'outils rend l'ajout descriptif au lieu d'être structurel. 2) La frontière proposer/agir se tient dans la BOUCLE, à un seul endroit — la décider outil par outil finit par se contourner ; c'est aussi pour ça qu'on n'utilise pas le tool runner du SDK, qui exécute automatiquement. 3) Une revue croisée dédiée trouve ce que la relecture d'auteur laisse passer : 3 défauts, tous du même motif — **un silence (panne, droit refusé, RLS) qui se lit comme un résultat**. À chercher systématiquement dans tout code qui donne des données à un modèle. 4) Un garde-fou porté par une phrase du prompt n'est pas un garde-fou : le déplacer dans le code. | Oui → lessons-learned |
| 2026-07-27 | Filou ACTIONNEUR sur les règles existantes : routeur d'intention (appel séparé, petit schéma — celui de la création n'est pas touché), lecture des règles du cabinet nommées par la source unique `phraseRegle`, proposition « mettre en pause / supprimer / remettre en service » sur le tableau, écriture via `setRegleActif`/`deleteRegle` ; 8 tests sur la frontière numéro→id (`bbf0ec5`) | lib/ia/routerIntention.ts (nouveau), lib/regles/libelle.ts (nouveau), regles/actions.ts (`demanderAFilou`, `appliquerActionRegles`), v2/FilouResultat.tsx, v2/FilouChat.tsx, ReglesClient.tsx, v2-terrier.css, tests/ia/ciblage-regles.test.ts | 1) Quand un schéma de sortie structurée est déjà au bord des plafonds API (incident 2026-07-26), une capacité de plus se pose en APPEL SÉPARÉ — pas en champ de plus. 2) Une sortie de modèle qui va devenir un identifiant de ligne à effacer ne doit jamais être un UUID : numéroter, puis retraduire côté serveur sur la liste qu'on a soi-même envoyée, en jetant tout numéro invalide. 3) Un assistant qui « explique comment faire à la main » ce qu'il pourrait faire lui-même est un demi-produit : le test n'est pas « a-t-il compris ? » mais « a-t-il agi ? ». | Oui → lessons-learned |
| 2026-07-27 | Filou : les RÉSULTATS quittent la tablette pour le tableau du cabinet (fenêtre `filou` de plus dans le `.stage`, canal générique `ResultatFilou{genre}` prêt pour « montre-moi le planning de jeudi ») ; décision remontée dans le fil via `FilouChatHandle.dit()` ; suppression des pastilles de raccourci et des 3 exemples de règles sous le chat ; scroll doux vers le tableau sous 940 px (`afef9a4`) | v2/FilouResultat.tsx (nouveau), v2/FilouChat.tsx, v2/Epicentre.tsx, styles/v2-terrier.css | 1) La consigne d'ergonomie de MiKL (« les résultats vont à droite ») se grave dans un canal typé par `genre`, pas dans un branchement sur la règle : la 2ᵉ capacité de Filou n'aura rien à re-découper. 2) Réinitialiser l'état d'un panneau quand sa donnée change se fait par `key`, pas par `useEffect` + `setState` — ESLint refuse le second (renders en cascade) et il a raison. 3) « À droite » n'existe qu'au-dessus de 940 px : toute mise en scène en deux colonnes doit dire ce qu'elle devient en dessous. | Oui → lessons-learned |
| 2026-07-27 | Passe généralisée : toute lecture d'environnement trimmée à la source (Supabase URL/anon/service_role, Brevo, Anthropic via `cleIA()` passée explicitement au SDK, Google Calendar, `CRON_SECRET`, URLs publiques) — 819 tests, build OK (`27293a1`) | proxy.ts, supabase/client+server, brevo.ts, notifications.ts, login/actions, auth/confirm, 3 crons, absences/volontaire, echanges/actions, veterinaires/actions, google-calendar, 4 fichiers ia/ | `DEV_BYPASS_AUTH` laissé en comparaison stricte SANS trim : ce drapeau coupe l'authentification, le rendre tolérant à la saisie affaiblirait un garde-fou au lieu de corriger un bug. Un « nettoyage systématique » n'est pas systématique quand la valeur est un interrupteur de sécurité. | Oui → lessons-learned |
| 2026-07-27 | Fix 404 `not_found_error` sur l'assistant Filou : `GUARDVETO_IA_MODELE` posée sur Vercel contenait un retour à la ligne final, l'API recevait `claude-sonnet-5\n` (modèle introuvable) → `modeleIA()` trimme la valeur (`02fcf14`) | src/lib/ia/proposerRegle.ts | Toute variable d'environnement saisie à la main doit être trimmée à la LECTURE, pas seulement nettoyée dans le dashboard : la valeur invisible revient au prochain copier-coller. Déjà rencontré sur ce projet (mails Brevo) — la leçon était connue mais pas appliquée à cette lecture-ci. | Oui → lessons-learned |
| 2026-07-08 | Vague 6 « derniers items règles » (3 tranches, teammates Opus) — #21 cohortes d'équité par tag (brique `equilibrer` + `tag?` : chaque règle taguée = entrée de score indépendante dimension × cohorte, variance sur les seuls porteurs, cohortes voyagent DANS `EquityWeights` → zéro champ SolverInput, resoudreContexte/crise/replay hérités ; partition explicite « 2 cohortes + globale Ignorée », `grands_weekend` garde son défaut salariés ; UI /regles sous-section Cohortes + IA pilotant l'équité par cohorte + pré-vol `cohorte_equite_sans_porteur`) ; #15a `exclusion_dates` XOR « pas les deux » (forme `fetes` noel/nouvel_an par année via référentiel historique-fete — se reconduit seule — et forme `dates` ISO libres au sens jours couverts, WE daté samedi couvre sam+dim ; intra-période par principe) ; #15b `seulement_avec` conditionnelle ORIENTÉE (« A seulement si B sur le MÊME créneau », une ligne sans miroir contrairement au duo, pose complétante gabarit composition, gardes anti-impasse à la création : B=soi/B inactif/tous créneaux 1 place) ; 3 migrations appliquées (catalogue = 26 briques) ; 788 tests (`806e98a`+`f1c2336`+`217b244`) | engine (equity-weights, score-lexicographique, solver, hard-constraints, seulement-avec.ts nouveau, historique-fete, validerPlanning, pre-vol, catalogue, types), mapReglesCabinet, regles UI/actions/IA (ReglagesPlanningClient, RegleFormDialog, ReglesClient, AssistantIA, regleSchema, proposerRegle), route pre-vol, 3 migrations, 3 fichiers de tests (+57) | 1) Un prédicat de pose qui cherche « l'autre occurrence » dans les gardes EXISTANTES doit aussi tester si le candidat couvre les deux cibles À LUI SEUL (paire sam+dim d'un même WE) — sinon moteur permissif / validateur qui crie = violation fantôme au gate (attrapé en review tranche B). 2) Une config qui voyage DANS un objet déjà threadé (`EquityWeights.cohortes`, comme `StructureConfig.relations` en RG2) échappe à tout le champ de mines resoudreContexte. 3) Sur le chemin chaud d'isValid (LNS), aucun travail avant la violation : résolution des libellés paresseuse, jamais de Map pré-construite par appel. | Oui → lessons-learned |
| 2026-07-08 | Vague 5 « rythmes » (3 tranches, teammates Opus) — #17 lookback inter-périodes (`contexteAnterieur` ~10 j chargé best-effort, vue étendue `attributionsAvecContexte` consommée par les SEULES règles de rythme R10/R3/espacement_min/espacement_weekend/au_plus_n dans les 2 gardiens ; équité/couverture insensibles, byte-identique sans donnée) ; #13 successions/repos avancés (3 briques famille `sequence` : `succession_interdite` jour civil avec lendemain-de-WE = lundi, `serie_max` stretch borné WE = 2 jours, `repos_apres_serie` ; « repos min consécutif » non dupliqué ≡ espacement_min N+1 documenté côté IA) ; #20 `cadencement_weekend` « 1 WE sur N ancré » (ancre samedi, modulo signé, sens interdit/impose, cycle calendaire strict SANS recalage vacances, pré-vol intègre la capacité WE réduite) ; 2 migrations appliquées (catalogue = 24 briques) ; 731 tests (`9543cf9`+`8f13908`+`4ea3460`) | engine (solver, types, utils, hard-constraints, soft-constraints, score-lexicographique, validerPlanning, pre-vol, catalogue, loader, crise), resoudreContexte, revaliderPlanning, mapReglesCabinet, regles UI/actions/IA, routes absences, 2 migrations, 3 fichiers de tests (+67) | 1) Une donnée de contexte hors-période s'injecte en VUE ÉTENDUE explicite consommée par les seuls prédicats concernés — jamais fusionnée dans `planning.attributions` (sinon elle contamine couverture, équité et persistance). 2) Avant de créer une brique, chercher l'ÉQUIVALENCE sémantique dans le catalogue (repos min ≡ espacement_min) : le doublon se règle dans le prompt IA, pas par une 4e brique. 3) Une règle « après exactement N » est INVULNÉRABLE (toute pose allonge la série au-delà de N) : les seuils de séquence se formulent en « ≥ N ». | Oui → lessons-learned |
| 2026-07-07 | Vague 4 « qui peut faire quoi » (3 tranches) — #6 tags junior/senior + composition d'équipe (`veterinaires.tags` + brique globale `composition_equipe` au_moins_un/pas_seuls, jugée sur l'ÉQUIPE COMPLÈTE via « pose complétante » + nbPlaces threadé) ; #22 `role_interdit_tag` (« un junior jamais 1er », gabarit R17 place-par-place) ; #7 desiderata par-véto TOUJOURS souples (`preferer_creneau`/`preferer_avec`/`volume_gardes` — pénalité de non-satisfaction dans les 2 gardiens de score + biais signé au tri) ; UI /regles section « Composition d'équipe » + étiquettes fiche véto + formulaire guidé + IA types 8-12 ; pré-vol tag sans porteur / rôle intenable ; 3 migrations appliquées (catalogue = 20 briques) ; 664 tests (`f59de66`+`5ee9eac`+`7239da5`) | engine (types, structure-config, composition-equipe.ts, desiderata.ts, hard-constraints, solver, score-lexicographique, validerPlanning, pre-vol, catalogue), mapReglesCabinet, loader, regles UI/actions/IA, VeterinaireForm/Card, 3 migrations | 1) Une règle jugée sur l'ÉQUIPE d'un créneau ne se juge qu'à la POSE COMPLÉTANTE (bloquer avant élimine des solutions valides), et le total qui fait foi est ce que le solver VA pourvoir (effectif plafonné ≠ places déclarées). 2) JAMAIS de bonus négatif dans le vecteur lexicographique (étage « meilleur que parfait » = hiérarchie cassée) : préférence positive = pénalité de non-satisfaction, les termes négatifs restent au tri des candidats. 3) Desiderata sans gardien dur = même famille que R10/R8b : refus de « jamais » à l'écriture + clamp à l'évaluation. | Oui → lessons-learned |
| 2026-07-07 | Vague 3 (teammates Fable ×2) — #23 pré-vol de cohérence (6 détections proactives, zéro faux positif), #24 congés en attente signalés dès la génération (source unique avec le gate), #14 historique des fêtes (table + pénalité souple « pas deux Noëls de suite ») | pre-vol.ts, PreVolAlert.tsx, souhaitsCongesEnAttente.ts, historique-fete.ts, historiqueFetes.ts, publish/route.ts (`c28f691`) | Pré-vol proactif = rejouer le MÊME isValid sur planning vide (pas un 2e analyseur) → cohérence garantie avec le diagnostic réactif ; co-édition d'un fichier par 2 teammates OK si régions disjointes ET vérifiée au diff à l'intégration | Non (patterns déjà capitalisés) |
| 2026-07-07 | Vague 2 (teammates Fable ×2 + reprise) — verrou 7 étape 3 (attributions V2 synchronisée sur toute édition + détecteur de dérive V1↔V2 + FK CASCADE) ; #16 pénalités R10/R10b/R10c/R8b réglables ; #18 multi-refs dépliés ; #19 au_plus_n filtre créneaux exposé | syncAttributions.ts, attributionRows.ts, appliquer-changement.ts, revaliderPlanning.ts, mapReglesCabinet.ts, catalogue.ts, regles UI/IA, 2 migrations | Bug trouvé en chemin : les gardes VERROUILLÉES désynchronisaient V2 à chaque régénération ; un teammate interrompu à mi-course laisse un diff bâtard → un agent « repreneur » qui inspecte le diff et TERMINE vaut mieux qu'un revert ; suppression de période laissait des attributions orphelines (pas de FK) | Oui → lessons-learned |
| 2026-07-06 | Bloc 1 / P6 verrou 3 — aval générique (teammates Opus, Voie A) : vendredi/dimanche dérivés des relations RG4 au lieu de la magie R8 en dur (vue SQL + validateur + PDF + agenda) | resoudrePlanningAffichage.ts, chargerRelationsAffichage.ts, vue planning_semaine, gardesVersPlanning.ts, pdf.tsx, google-calendar.ts (`9981c95`) | La magie aval n'existait QUE parce que V1 `gardes` jette le `vendredi_soir` que le moteur produit (V2 le stocke : 43 j vs 36) → dériver par relations = byte-identique par défaut ET honnête si un cabinet coupe R8 ; `CREATE OR REPLACE VIEW` **efface** `security_invoker` → toujours le ré-appliquer | Oui → lessons-learned |
| 2026-07-06 | Bloc 1 / P6 verrou 5 (équité par type) — STOP TILT, non shippé | (revert Voie B) | Équité « pur type×place » **pas byte-compatible par défaut** : `variance(A∪B) ≠ variance(A)+variance(B)` (semaine_soir+vendredi_soir fusionnés en une variance) → décomposer bouge le planning ; verrou 5 = **décision MiKL** (accepter re-baseline goldens ou décomposition fine), pas un « rendre configurable défaut inchangé » | Oui → lessons-learned |
| 2026-07-06 | Bloc 0 audit 360° (teammates Opus ×3) — fuite inter-tenant email_log, config par cabinet (#10), journal e-mails (#9c), garde-fous édition (#12), plafond seed + fallback vacances + drop cluster dormant | email_log/cabinets migrations, geo-zone.ts, ParametresCabinet.tsx, journal-emails/, gardes/[id]/route.ts, solver.ts, structure/actions.ts (`1a2ae1f`) | 2 fuites inter-tenant réelles trouvées à l'audit (email_log RLS non bornée + soupçon cabinets levé : `cabinets_select` déjà `id=auth_cabinet_actif()`) ; parallélisme teammates sûr = **propriété de fichier disjointe** (un seul owner sur `notifications.ts`), migrations écrites par les agents mais **appliquées par l'orchestrateur** après vérif `project_ref` | Oui → lessons-learned |
| 2026-04-25 | STORY-012 — ActionBar admin (Générer, Publier, PDF) + /admin/periodes | ActionBar.tsx, /api/publish, /admin/periodes/page.tsx, planning/page.tsx | `asChild` non supporté par le Button shadcn/ui local — utiliser `<a>` stylé directement | Non (trivial) |
| 2026-04-25 | STORY-013 — Modale détail/modification garde (GardeDetailModal) | GardeDetailModal.tsx, MonthView.tsx, /api/gardes/[id], /api/gardes/[id]/disponibilites | — | Non (trivial) |
| 2026-04-25 | STORY-014 — Alertes et violations de règles | AlerteBandeau.tsx, ViolationDialog.tsx, GardeDetailModal.tsx, planning/page.tsx, GenerateurPlanning.tsx | — | Non (trivial) |
| 2026-04-25 | STORY-015 — Compteurs individuels par vétérinaire | useCompteurs.ts, CompteursClient.tsx, compteurs/page.tsx | — | Non (trivial) |
| 2026-04-25 | STORY-016 — Bonus/malus et bilan de période | bilan.ts, /api/bilan/route.ts, BonusMalusCard.tsx, useCompteurs.ts, compteurs/page.tsx | — | Non (trivial) |
| 2026-06-01 | Audit pré-livraison — outillage qualité (ESLint configuré + deps réparées) | eslint.config.mjs | `node_modules` partiellement corrompu (dossier OneDrive) → réinstall complète ; ESLint 9 = flat config native via `eslint-config-next` | Oui → lessons-learned |
| 2026-06-01 | Retrait du script dev live-reload `localhost:8400` | layout.tsx | Résidu de l'outil `impeccable` qui serait parti en prod | Oui → lessons-learned |
| 2026-06-01 | Sécurité — vues en `security_invoker` + `search_path` figé | migrations 010, 011 | Vues SECURITY DEFINER contournaient la RLS (un véto pouvait voir un brouillon) | Oui → lessons-learned |
| 2026-06-01 | Nettoyage complet du rôle « secretaire » (RLS + code + doc) | migration 012, planning/page.tsx, Header.tsx, conges/page.tsx, export-pdf/route.ts | Supprimer un rôle = traquer TOUS les résidus (base + policies + code + doc), pas seulement la contrainte | Oui → lessons-learned |
| 2026-06-01 | Export PDF ouvert aux vétos (planning publié uniquement) | export-pdf/route.ts, planning/page.tsx | Garde-fou serveur : un véto n'exporte qu'une période publiée | Non |
| 2026-06-04 | Fix recette A2 — liens emails Auth cassés en flux PKCE (reset + invitation) | docs/email-templates-supabase.md (templates dashboard Supabase) | `{{ .ConfirmationURL }}` casse en PKCE → router les liens vers `/auth/confirm?token_hash=…&type=…` | Oui → lessons-learned |
| 2026-06-04 | Fix recette D1 — emails métier (congé/planning) non envoyés | Config Vercel `BREVO_API_KEY` (clé SMTP → vraie clé API v3 `xkeysib-…`) | SMTP ≠ API Brevo : 2 circuits distincts, échec silencieux | Oui → lessons-learned |
| 2026-06-04 | Fix recette MOTEUR — binôme de semaine = même véto en 1er+2nd | src/engine/rules/hard-constraints.ts (règle R21), tests/engine/solver.test.ts | Contrainte dure « 1er ≠ 2nd » manquante ; test vérifiait « rempli » pas « distinct » | Oui → lessons-learned |
| 2026-06-06 | Fix post-démo — inversion 1er/2nd vendredi↔week-end (R8) invisible | migration 014 (vue planning_semaine), src/lib/pdf.tsx, src/lib/google-calendar.ts | Règle correctement calculée par le moteur mais **détruite par les couches aval** : l'API jette l'attribution `vendredi_soir` et la vue recopiait la paire WE à l'identique sur le vendredi. Vérifier une règle de bout en bout (moteur→base→vue→PDF→agenda), pas seulement dans le solver | Oui → lessons-learned |
| 2026-06-06 | Nouvelle règle souple R10c — pas de garde le WE avant des vacances | src/engine/types.ts, loader.ts, rules/soft-constraints.ts, tests/engine/soft-constraints.test.ts | Pénalité 45 si vacances (type `vacances`) débutent la semaine suivant le WE ; a nécessité d'ajouter le `type` du congé au moteur (non chargé avant) | Non |
| 2026-06-06 | Nouvelle règle souple R11b — équité du rôle 1er le WE (avantage financier) | rules/optimization.ts, scorer.ts, solver.ts, tests (optimization + solver) | R8 fige le rôle WE depuis le vendredi → seul levier = le vendredi soir ; biais sur 1 seul rôle insuffisant (spread restait 4) → biais symétrique vendredi-1er + vendredi-2nd (poids 25, tie-breaker). **À confirmer avec le cabinet (maintenir ou non)** | Oui → lessons-learned |
| 2026-06-06 | Feature Compteurs — filtre période/plage de dates + périmètre (tout/validé) + Total général + confirmation publication + recalcul auto bilan | useCompteurs.ts, compteurs/page.tsx, CompteursClient.tsx, ActionBar.tsx, api/gardes/[id]/route.ts | Agrégation par plage en TS (pas de migration DB) ; bilan bonus/malus = mode période seulement ; modif manuelle sur période publiée → recalcul bilan auto (plus besoin de republier) | Non |
| 2026-06-18 | F5-003 — Durcissement RLS tables V2 (attributions/snapshots/regles_version/cabinets) | migration 20260618120000, e2e/roles.spec.ts, e2e/fixtures/provision.ts | Les tables V2 reproduisaient la faille d'isolation PERMISSIVE `FOR ALL` corrigée sur V1 le 17/06 (escalade intra-cabinet : un véto pouvait réécrire le planning / falsifier les snapshots). → tout NOUVEAU lot de tables tenant doit NAÎTRE en RESTRICTIVE + policy de rôle, jamais une seule policy permissive FOR ALL. Prouvé par E2E (12/12). Appliqué via dashboard SQL (classifier auto-mode bloque l'application directe). | Oui → lessons-learned |
| 2026-06-18 | Fix script apply-f5-003.ps1 — API Supabase "query: Expected string, received object" | scripts/apply-f5-003.ps1 | PS 5.1 : `Get-Content -Raw` garde des propriétés ETS → `ConvertTo-Json` emballe le SQL dans `{ "value": … }`. Forcer `[string](...)` avant sérialisation (+ `-Compress`), comme le script générique. Vaut pour tout futur helper de migration sur Windows. | Oui → lessons-learned |
| 2026-06-25 | Palier 1 — formulaire /regles : briques au_plus_n + espacement_min réglables | regles/actions.ts, RegleFormDialog.tsx, ReglesClient.tsx, regles-p1a007-contrat.test.ts | Trou form↔moteur : une capacité moteur (briques branchées en `f13eba4`/`912d695`) reste une coquille vide tant qu'aucun consumer UI ne l'expose. Aligner les 3 allowlists (BRIQUES_EVALUABLES action + BRIQUES form + BRIQUES_EDITABLES liste) en un commit. | Oui → lessons-learned |
| 2026-06-25 | Palier 1 — validité par période (règles permanentes ou limitées à une période) | lib/periodes.ts (+test), regles/actions.ts, regles/page.tsx, RegleFormDialog.tsx, ReglesClient.tsx | Mécanisme moteur déjà présent (loader filtre `periode_id.is.null OR eq`) mais inatteignable : le form écrivait toujours `null`. Câblage applicatif pur, colonnes `periode_id`+`validite_json` déjà en base → aucune migration, zéro risque moteur. validite_json = miroir cohérent de periode_id. | Oui → lessons-learned |
| 2026-06-25 | Assainissement + cadrage Palier 2 (catalogue interne + diagnostic à jour) | catalogue.ts, mapReglesCabinet.ts, diagnostic.ts, solver.ts (+tests) | `motif_grand_weekend` = brique INTERNE (déjà couverte par repos_conditionnel) → marquée + garde mapper anti-coquille-vide. DÉCOUVERTE : Palier 2 (diagnostic intelligent + suggestions vérifiées) DÉJÀ construit/câblé/testé (livré avec le système de crise) → entêtes périmés corrigés, pas de layer à rebâtir. 3e dette `RegleResolue[]` documentée comme reportée au Palier 3. | Oui → lessons-learned |
| 2026-06-25 | Palier 3 slice 1 — assistant IA basique (créer une règle en langage naturel) | lib/ia/regleSchema.ts (+test 9/9), lib/ia/proposerRegle.ts, regles/actions.ts, AssistantIA.tsx, ReglesClient.tsx | 1er morceau IA, basique exprès (refonte design plus tard). claude-opus-4-8 + sortie structurée Zod (parse). L'IA PROPOSE en termes humains (prénoms/jours) ; conversion pure prénom→id ; création via upsertRegle existant (zéro écriture IA, RLS inchangée). Dégrade proprement sans ANTHROPIC_API_KEY. ⏳ Clé à définir dans Vercel + local pour activer. | Oui → lessons-learned |
| 2026-06-29 | Assistant IA — recette MiKL : clé activée + 3 ajouts UX (`1bcd6c5`, `82900bb`) | AssistantIA.tsx | Puissance réglable (étage dur/mou pré-rempli IA, éditable avant création) + bouton Reformuler (garde la phrase) + Tout effacer (page blanche). L'IA propose, l'humain décide jusqu'au bout. | Non |
| 2026-06-29 | Assistant IA — durcissement (`7522808`) : anti-doublon, prénoms ambigus, anti-coquille-vide, duo 1 ligne, aide | lib/ia/regleSchema.ts (+6 tests), regles/actions.ts, ReglesClient.tsx, AssistantIA.tsx, proposerRegle.ts | Doublon refusé (règle équivalente / paire duo). Prénom en double → REFUS de choisir au hasard (risque mauvais véto). Plafond hors borne/sans effet → plus de bouton Créer. Duo affiché 1 ligne (base garde 2 sens ; toggle agit sur les 2). Fenêtre d'aide langage clair + renvoi MPP. Prompt dé-jargonné. | Oui → lessons-learned |
| 2026-06-29 | Assistant IA — erreurs dans le panneau + message de refus franc (`f055d53`, `427f46a`) | AssistantIA.tsx, regles/actions.ts, lib/ia/regleSchema.ts | Erreur de création (doublon) affichée DANS le panneau, pas en toast lointain. Quand notre couche refuse une proposition que l'IA croyait faisable → on force NOTRE message (sinon message optimiste de l'IA = illusion « rien n'a changé »). Plafond « trop élevé » SANS chiffre magique (ni 14 ni 7 en dur) : le réaliste dépend du nb de gardes/jour du cabinet → borne config-cabinet reportée V2. | Oui → lessons-learned |
| 2026-06-29 | Nouvelle règle « un week-end sur N » (brique espacement_weekend) (`2a2d317`) | engine (types, hard-constraints, validerPlanning, briques/catalogue), mapReglesCabinet, regles/actions, lib/ia (regleSchema+proposerRegle), RegleFormDialog, ReglesClient, migration 20260629181000 (+tests) | Brique de fréquence WE « au plus 1 WE toutes les N sem » (interprétation A = espacement, préférence par défaut). Kit complet : moteur + validateur indépendant (FREQ_WE) + IA + form + migration catalogue (FK). Migration appliquée sur base live mpvrok (vérifiée). 2 échecs benchmark solver = pré-existants (stash). | Non (story tracée commit+mémoire) |
| 2026-07-03 | Sprint blindage post-audit 360° — sécurité : assertAdmin sur veterinaires (dont inviterVeterinaire service_role, faille C1) + periodes ; gitignore **/_backups/ | admin/veterinaires/actions.ts, admin/periodes/actions.ts, .gitignore | Une Server Action qui bascule en service_role DOIT porter sa propre garde admin+tenant : la RLS ne peut plus la rattraper. Trouvé indépendamment par 2 agents d'audit. | Oui → lessons-learned |
| 2026-07-03 | Sprint blindage — intégrité génération/publication : verrou CAS `generation_lock_at` (migration 20260703100000 appliquée mpvrok), dépublication AVANT destruction, purge agenda APRÈS succès (ids pré-capturés), gate de publication (revalidation + souhaits en attente → confirmation) | api/generate/route.ts, api/publish/route.ts, lib/sync-calendrier.ts, ActionBar.tsx, migration | Le pipeline détruisait avant de reconstruire (période « publiée » vide + agenda purgé si échec à mi-course) et rien n'empêchait 2 générations simultanées. Ordre sûr : verrou → dépublier → capturer ids agenda → delete/insert → purger agenda. Le code de revalidation existait, il n'était juste pas appelé au bon endroit. | Oui → lessons-learned |
| 2026-07-03 | Sprint blindage — moteur : R17 conditionné à l'effectif résolu (violations fantômes été=2), fériés zone-aware threadés (compterParVet/scorerPlanning/mapTypeGardeEnDb) + férié dominical d'un WE enfin compté (bug n°3 catalogue blindé) | validerPlanning.ts, rules/optimization.ts, score-lexicographique.ts, solver.ts, api/generate/route.ts, r17-effectif-resolu.test.ts | Deux gardiens désalignés (again) : le validateur jugeait sur `saison` là où le moteur juge sur l'effectif résolu. Et tout lecteur d'`estJourFerie` SANS param calendrier juge sur les fériés France en dur. Banc 450/450. | Oui → lessons-learned |
| 2026-07-03 | Sprint blindage — perf LNS : compteurs mémoïsés (1×/step au lieu de 1×/comparaison de tri) + index Map étage 0 ; 12 sem 15-22s → <5s, banc 23s → 11,5s, les 2 tests perf repassent (450/450) | solver.ts, score-lexicographique.ts | Un comparateur de tri qui recalcule un agrégat O(n) = O(n·log n) recalculs identiques. Mémoïser par step est byte-identique (mêmes valeurs, tri stable) — prouvé par les goldens. | Oui → lessons-learned |
| 2026-07-03 | Sprint blindage — UX : suppression de congé confirmée + refus avec motif unifié (RefuserCongeDialog), statut visible mobile, effectif RÉSOLU affiché sur /admin/periodes (précédence période>profil>saison + provenance), bottom-nav mobile réparée (5 icônes manquantes + onglet « Plus ») | CongesList.tsx, admin/periodes/page.tsx, types/index.ts, MobileNav.tsx | La seule suppression sans confirmation de l'app était sur la donnée la plus sensible (congé validé). Un écran qui affiche une valeur SANS appliquer la précédence du moteur ment à l'admin. | Non (audit tracé docs/v2/10) |
| 2026-07-03 | Monitoring interne — signalerIncidentTechnique : les échecs agenda/Brevo/placements notifient les admins via la cloche (anti-spam 24h, cabinet borné) | notifications-inapp.ts, api/publish, api/generate, appliquer-changement.ts | Un `console.error` sur Vercel = personne ne le lit. La brique cloche existante (C2) suffit pour un monitoring interne sans service externe ni clé API. | Non (backlog n°9 traité) |
| 2026-07-03 | Échange de gardes self-service slice 1 (backlog n°2) — table echanges_gardes (RLS F5-003, migration appliquée mpvrok), cycle proposer→accepter→valider admin→application, UI /echanges + nav, notifs in-app | migration 20260703150000, echanges/actions.ts, echanges/page.tsx, EchangesClient.tsx, types, Sidebar, MobileNav, notifications-inapp | L'application passe par appliquerChangementGarde EXISTANT → agenda + placements + bilan + email hérités sans duplication (règle CONSUMERS appliquée en conception). Policy INSERT notifications = admin-only → notifier depuis une session véto exige un service_role borné cabinet, APRÈS validations. Re-vérifier l'échange AU MOMENT de la validation admin (le planning a pu bouger) + rollback best-effort sur la 2e jambe d'un swap. | Oui → lessons-learned |
| 2026-07-03 | Échanges slice 2 — proposition ouverte « à tous » (premier arrivé = claim CAS en base), badge nav compteur, « Proposer un échange » depuis le modal de garde du planning (`/echanges?proposer=ID`) | migration 20260703180000, echanges/actions.ts, EchangesClient.tsx, layout.tsx, Sidebar, MobileNav, planning/page, MonthView, GardeDetailModal, notifications-inapp | Une course « premier arrivé » se tranche PAR LA BASE (UPDATE conditionnel cible_id IS NULL), jamais par une lecture puis écriture. Les garde-fous métier (congé, déjà sur le créneau) doivent se rejouer AU MOMENT d'accepter, pas seulement à la proposition. | Oui → lessons-learned |
| 2026-07-03 | Batch backlog — fuite multi-tenant rappels corrigée (filtre cabinet, avant cabinet n°2) + cron phase 2 « période suivante manquante » (≤21 j, anti-spam hebdo, le retour anticipé sautait le cas le plus grave) + recette échanges notée | notifications.ts, cron/rappels/route.ts, notifications-inapp.ts, docs/recette-echanges-gardes.md | Tout code service_role qui sélectionne des destinataires DOIT filtrer explicitement par cabinet (la RLS est contournée). Un early-return « rien à faire » peut court-circuiter la détection du cas le plus grave. | Oui → lessons-learned |
| 2026-07-03 | R11b de bout en bout — rôle à avantage financier réglable (migration cabinets + RPC auto-gardée + loader + UI /regles + crise threadée aux 4 routes absences) ; banc 454/454 | migration 20260703200000, equity-weights, loader, types, resoudreContexte, crise (contexte+reparer+4 routes), regles (actions+page+ReglagesPlanningClient), test r11b-mapping-db | resoudreContexte reconstruit l'objet champ par champ : TOUT nouveau champ moteur doit y être propagé EXPLICITEMENT sinon il est détruit en silence (attrapé par tsc grâce au typage). Écriture cabinets sans policy UPDATE large = RPC SECURITY DEFINER auto-gardée. La question « avantage financier » au cabinet est maintenant traduisible en réglage. | Oui → lessons-learned |
| 2026-07-03 | Backlog n°4 tranche 1 — fin du silence sur les créneaux ignorés : `detecterCreneauxIgnores` (miroir de stepsForDay) + `creneauxIgnores[]` dans /api/generate + alerte ambre dans ActionBar et GenerateurPlanning (lien /admin/structure) ; 9 tests dédiés, banc 463/463 | engine/creneau-modele.ts, api/generate/route.ts, CreneauxIgnoresAlert.tsx, ActionBar.tsx, GenerateurPlanning.tsx, creneaux-ignores.test.ts | Quand une couche aval ne sait pas consommer une donnée de config, l'ignorer EN SILENCE est une dette UX grave : détecter et avertir coûte une fonction pure miroir + un champ de réponse. Zéro faux positif garanti par test sur le catalogue par défaut. | Non (tranches 2+3 = vrai chantier moteur) |
| 2026-07-06 | P3b backlog n°4 tranches 2+3 — créneaux SUR-MESURE réellement planifiables de bout en bout : moteur (stepsForDay multi-créneaux/jour + codes libres, R22 une garde/jour, équité étalement par code), validateur miroir, migration CHECK gardes.type/attributions.role + slugs sm_, persistance par place réelle, horaires par code, MonthView/PDF multi-gardes, agenda honnête, gate publication sans fantômes, UI création/activation/suppression sur /admin/structure | 40 fichiers (engine, validation, data, api/generate, MonthView/DayCell/PDF, agenda, crise, dispo, compteurs, libelles-gardes.ts, CatalogueCreneauxAdmin.tsx, migration 20260706120000) | 1) Le byte-identique se PROUVE : les goldens n'exerçaient que le chemin legacy — le banc d'équivalence catalogue-défaut manquait, il existe maintenant. 2) « Pas deux gardes le même jour » n'existait PAS (R21 = intra-créneau, espacement_min saute le même jour) → R22 créée. 3) Tout mapping réducteur (mapDbTypeToEngine, typeEngineVersCodeCreneau, CHECK SQL) est un point de silence à lever ENSEMBLE, sinon violations fantômes au gate de publication. | Oui → lessons-learned |
| 2026-07-06 | RG1 tranche 1 (verrou n°4 doc 09) — relations entre créneaux EN DONNÉE : `relation_creneau` profil-aware (profil_id + trigger d'intégrité même cabinet/même profil), seed des 2 relations historiques vendredi→WE (meme_binome/inversion_role) par profil, `prendre_snapshot` v4 (fige les relations), `dupliquer_profil` v2 (copie les relations, ids remappés par code+nom), loader profil-aware ; zéro effet moteur (branchement = tranche 2) ; 482 tests (476+6), migrations appliquées mpvrok | migrations 20260706150000/160000/170000, chargerCreneauModele.ts, relations-creneau-loader.test.ts | La table `relation_creneau` dormait depuis P1 SANS seed ni profil : une donnée posée « pour plus tard » dérive du schéma courant si personne ne la fait vivre — l'inspection consumers (snapshot, duplication, loader) devait se faire AU MOMENT où la donnée prend vie, pas au branchement moteur. | Non (tranche 2 = branchement moteur, sous TILT) |
| 2026-07-06 | RG2 (verrou n°4, tranche 2) — le MOTEUR consomme les relations : checkR8/checkR9 génériques (plus de couple vendredi↔WE câblé, messages legacy conservés), pénalités souples génériques, appariement des occurrences par ADJACENCE (même jour inclus, fenêtre 7 j, byte-identique à vendrediDeSemaine), loader attache les relations du profil, replay v4 rejoue les relations figées (≤v3 → repli couple historique, fidèle) ; 502 tests (20 nouveaux RG2 dont solve bout-en-bout mar→mer même équipe), tsc baseline (3) | structure-config.ts, relations-structure.ts (nouveau), hard-constraints.ts, score-lexicographique.ts, loader.ts, api/generate/replay/route.ts, rg2-relations-generiques.test.ts | Faire voyager une nouvelle config DANS un objet déjà threadé partout (`StructureConfig.relations`) évite TOUT le champ de mines resoudreContexte/crise/replay (la reconstruction champ-par-champ n'a rien à apprendre). Distinguer `undefined` (pas de donnée → repli historique) de `[]` (donnée = zéro relation → découplage voulu) est ce qui rend le repli sûr ET la donnée souveraine. | Oui → lessons-learned |
| 2026-07-06 | RG3 (verrou n°4, tranche 3) — le VALIDATEUR INDÉPENDANT applique les relations en donnée : bloc R8/R9 générique, appariement ré-implémenté localement (`sourceLiee`, jamais le module moteur), messages legacy conservés pour le couple historique ; ferme la fenêtre « moteur générique / validateur câblé » de RG2 ; 509 tests (+7 : deux gardiens d'accord sur solve custom, R9/R8 détectées sur types sur-mesure, anti-fantôme), tsc baseline (3) | validerPlanning.ts, rg2-relations-generiques.test.ts | Les deux gardiens doivent être généralisés dans la MÊME session : entre RG2 et RG3, toute donnée custom aurait produit des violations fantômes (moteur permissif là où le validateur criait). Reste un couplage assumé hors-périmètre : `aGardeWeekendCetteSemaine` (helper R3) suppose toujours la liaison vendredi↔WE — c'est la sémantique de R3, pas des relations. | Non (suite = tranche 4, UI + IA + config par relation) |
| 2026-07-06 | RG4 (verrou n°4, tranche 4 — VERROU LEVÉ de bout en bout) — section « Créneaux liés » sur /admin/structure : liste par profil, création entre n'importe quels créneaux (sur-mesure compris), désactivation/suppression (découplage réel possible) ; garde anti-impasse « même équipe + même jour » refusée avant création (incompatible R22, alternative proposée) ; assistant IA « décris une liaison » (miroir P5s5 : proposition zod → aperçu → l'humain crée) ; niveau ferme/souple reste PAR GENRE via /regles (par relation = backlog) ; 518 tests (+9), tsc baseline, build OK | admin/structure/actions.ts + page.tsx, RelationsCreneauxAdmin.tsx, AssistantRelationIA.tsx, lib/ia/relationSchema.ts + proposerRelation.ts + test | Quand deux règles configurables peuvent se contredire (meme_binome ↔ R22), le bon endroit pour l'attraper est LA CRÉATION de la config (message + alternative), pas la génération (impasse cryptique des semaines plus tard). La garde vit en double : conversion IA (feedback immédiat) ET server action (frontière de confiance). | Non (backlog : config par relation, repos_apres, généraliser R3) |
| 2026-07-10 | Audit Bloc 3 — D1 : `seulement_avec` enfin éditable (ajout à BRIQUES_EDITABLES, oubli Vague 6C) | ReglesClient.tsx | Un set d'autorisation UI (BRIQUES_EDITABLES) doit être testé en cohérence avec ses jumeaux (BRIQUES_EVALUABLES, formulaire) — un test de contrat aurait attrapé l'oubli. | Non (trivial) |
| 2026-07-10 | Audit Bloc 3 — D4 : expéditeur Brevo de repli `vetovaldallier@gmail.com` retiré (précédence cabinet > BREVO_FROM_EMAIL, sinon refus d'envoi) | brevo.ts | Ne jamais usurper une adresse réelle en repli : refuser d'envoyer vaut mieux qu'envoyer au nom du cabinet pilote. | Non |
| 2026-07-10 | Audit Bloc 3 — D3 : « Anne-Sophie » en dur dans les e-mails congés → signature dynamique (brevo_from_name du cabinet, repli « l'équipe du cabinet ») | brevo.ts, conges/actions.ts | Tout texte nominatif dans un template = bombe multi-cabinet ; les templates doivent recevoir l'identité en paramètre. | Oui → lessons-learned |
| 2026-07-10 | Audit Bloc 3 — D5 : e-mails congés journalisés dans email_log (helper journaliserEmailConge + migration CHECK types conge_valide/conge_refuse, appliquée mpvrok) | brevo.ts, conges/actions.ts, admin/journal-emails, migration 20260710120000 | Deux chemins d'envoi d'e-mails = un seul journalisé ; harmoniser les canaux AVANT d'ajouter un 3e type. | Non |
| 2026-07-10 | Audit Bloc 3 — D8 : /api/gardes/[id]/disponibilites ne sert plus les raisons d'indispo de toute l'équipe aux non-admins (vets:[] par rôle, pas de 403 — la modale véto lit les métadonnées de garde) | api/gardes/[id]/disponibilites/route.ts | Avant de gater une route « admin-only », grep ses consumers : ici la modale était PARTAGÉE admin/véto — un 403 sec aurait cassé le parcours véto. Dégrader la réponse par rôle > bloquer. | Oui → lessons-learned |
| 2026-07-26 | **Assistant IA cassé en PROD** — schéma de sortie hors limites API (30 params : 16 unions max, 24 optionnels max). Refonte : `SortieIaSchema` à 7 champs, params du type choisi dans `params_json` (chaîne JSON), dépliés par `normaliserProposition` | regleSchema.ts, proposerRegle.ts | Une fonctionnalité recettée puis laissée sans usage casse en SILENCE quand l'API évolue. Et une correction qui atteint la limite « pile » est un piège quand le compte est condamné à grandir — il faut découpler la forme externe de la forme interne. | Oui → lessons-learned |
| 2026-07-26 | Horaires de l'accueil V2 écrits en dur et FAUX (19 h → 8 h affiché, 18 h 30 → 8 h 30 réel) — nouvelle source `horairesCreneaux.ts` lisant `creneau_modele` par profil | data/v2/horairesCreneaux.ts, accueilEpicentre.ts, Epicentre.tsx | Deux vocabulaires de créneaux (vue vs catalogue) + le vendredi rangé sous « weekend » : vérifier le modèle réel en base avant de coder un libellé, jamais le supposer. | Oui → lessons-learned |
| 2026-07-26 | Accords en dur dans l'accueil V2 (« Jean prennent le relais », « 1 jours », deux coquilles vides affirmatives) — helpers `prenomsDe` + `accord` | Epicentre.tsx | Un pluriel écrit en dur dans un gabarit ne peut jamais s'accorder ; accorder sur le NOMBRE RÉEL d'éléments, pas sur la présence d'un champ. | Non |
| 2026-07-26 | Aucun retour vers l'accueil depuis les écrans V2 (dock sans entrée, marque non cliquable) — binette de Filou cliquable + page courante marquée dans le dock | BarreV2.tsx, v2-terrier.css | Un menu qui ne liste pas l'écran d'accueil doit rendre la marque cliquable : la maquette le prévoyait, le portage l'avait perdu. | Non |
| 2026-07-26 | Cout de l'assistant IA divise par ~4 : mise en cache du prompt + catalogue condense de 39 % (13 535 → 9 192 car., 6 179 → 4 268 tokens). Mesure avant/apres sur les 19 types : 21/21 conserve | proposerRegle.ts, bancEssai.ts, banc-ia/ | Avant d'optimiser un prompt, se doter d'un banc qui couvre TOUS les cas — le jeu court n'en exercait que 3 sur 19. Et mesurer d'ou vient le cout : ici 80 % venait du prompt, pas du modele. | Oui → lessons-learned |
| 2026-07-28 | Remise à zéro du chat Filou (bouton ↺ dans la saisie, confirmation en deux temps) — efface le fil, sa trace `sessionStorage` ET le résultat posé sur le tableau | FilouChat.tsx, Epicentre.tsx, v2-terrier.css | Effacer une conversation, c'est effacer les DEUX moitiés de l'écran : sinon la réponse reste à droite, orpheline de la question. | Non |
| 2026-07-28 | **Filou répondait à côté** dès qu'on rebondissait sur sa réponse — il repartait d'UN SEUL message (la dernière phrase), sans aucune mémoire, pas même de la question qu'il venait de poser. Fil des 10 derniers tours envoyé avec la demande + `assemblerMessages()` (6 tests) | agentFilou.ts, filou/actions.ts, FilouChat.tsx | Ce qu'on renvoie au modèle doit être sa VRAIE réponse (le tableau), pas le renvoi affiché dans le fil (« je te réponds sur le tableau ») : sinon il se souvient de la vitrine sans le contenu. Et un fil mal formé = 400 = assistant entièrement à terre, donc à tester. | Oui → lessons-learned |
| 2026-07-29 | **Filou ne proposait jamais d'action** (3 tours de boucle) — la boucle sortait au PREMIER outil du tour, or la consigne dit de finir par l'affichage : une réponse détaillée ne pouvait donc pas porter de bouton. Puis **second gardien** : quand le tour principal ne propose rien, un appel isolé avec `tool_choice: any` force le choix entre une action et « rien à proposer » | agentFilou.ts, FilouResultat.tsx, filou/actions.ts | Quand un modèle « oublie » de faire quelque chose 3 fois malgré 3 versions du prompt, arrêter de réécrire le prompt : poser la question dans un appel isolé, avec obligation de répondre. Le chronomètre (durée + nb de tours) a été l'outil de diagnostic décisif — 2 allers-retours prouvaient que l'outil n'avait jamais été TENTÉ. | Oui → lessons-learned |
| 2026-07-29 | Budget de réflexion IA en dur → **400, Filou muet en prod** : `thinking.budget_tokens` n'existe plus sur Sonnet 5. Remplacé par `thinking: adaptive` + `output_config.effort` (défaut `medium`, réglable par `GUARDVETO_IA_EFFORT`) | agentFilou.ts | Une forme d'API supprimée ne dégrade pas : elle tue la fonctionnalité entière. Vérifier la doc du modèle AVANT de régler un paramètre de perf, jamais de mémoire. | Oui → lessons-learned |
| 2026-07-29 | Réponses de Filou en pavé indistinct — 4 natures séparées dans les DONNÉES (`action.changements` ≠ `lignes`) : réponse / ce qui est établi / ce qui changerait (encadré) / l'action | FilouResultat.tsx, agentFilou.ts, v2-terrier.css | Réserver la couleur d'accent à ce qui attend une décision : deux blocs de même teinte, l'un informatif l'autre en attente, se lisent comme un seul. | Non |
| 2026-07-29 | Remise à zéro du chat Filou + mémoire de conversation (10 derniers tours) | FilouChat.tsx, Epicentre.tsx | Ce qu'on renvoie au modèle doit être sa VRAIE réponse (le tableau), pas le renvoi affiché dans le fil. | Oui → lessons-learned |
| 2026-07-31 | **Règles & structure fusionnées en un écran V2 à 4 onglets** (`/regles` absorbe `/admin/structure`) : Profils · Types de garde · Enchaînements · Règles du moteur. Sélecteur de profil unique en tête, créneaux enfin modifiables après création (`modifierCreneau`), ordre réordonnable (`reordonnerCreneaux`), 3 assistants IA inline remplacés par Filou au rebord, 5 `window.confirm()` convertis en modales | (v2)/regles/page.tsx, ReglesStructureV2.tsx, regles/Onglet{Profils,Creneaux,Enchainements,Moteur}.tsx, data/v2/reglesStructure.ts, v2-regles.css, admin/structure/actions.ts | Deux écrans pour un seul sujet se trahissent par leurs renvois croisés en dur : on créait la liaison ici, on réglait sa fermeté là-bas. Et un contexte (le profil) dupliqué en 3 sélecteurs finit par mentir — le catalogue était codé sur le profil par défaut pendant que les autres sections laissaient choisir. | Oui → lessons-learned |
| 2026-08-01 | **Recette Règles & structure** : 25 `<select>` natifs → composant du projet (le menu qui s'ouvrait était celui du navigateur), case « jours fériés » illisible, textes de développeur réécrits, identité colorée par type de garde, `.consequence` pour ce qui dépend d'un choix, glisser-déposer retiré (il ne changeait rien), étiquettes en menu, invite « Demander à Filou » | v2-regles.css, regles/Onglet*.tsx, ReglesStructureV2.tsx | Un `<select>` natif ne s'habille pas : la liste qui s'ouvre appartient au navigateur. Chercher ce que le projet fait déjà AVANT d'écrire un composant — le modèle correct était dans les fichiers déjà lus. | Oui → lessons-learned |
| 2026-08-01 | **3 trous moteur au-delà de 2 vétos** : (1) le soir en semaine plafonné à 2 en silence — l'effectif transitait en booléen, tout réglage ≥ 2 retombait à 2 ; (2) l'équité ignorait les places 3/4 en semaine — mesuré `[10,0,6,0,0,0]` avant, `[3,3,3,2,3,2]` après ; (3) violations R17 fantômes dans le scoreur en été à 2 vétos | solver.ts, equity-weights.ts, rules/optimization.ts, score-lexicographique.ts, placesAttendues.ts, migration 20260801090000 | Un nombre qui transite par un booléen perd sa valeur en silence. Et un correctif posé sur UN site quand il y en a deux ne change rien au symptôme : c'est la sonde qui l'a montré, pas le raisonnement. | Oui → lessons-learned |
| 2026-08-01 | **Refonte « Organisation / Période type » — étape 1 : ménage.** Suppression de `/admin/veterinaires` (V1 : lisait ET écrivait dans `contraintes_veto`, table que le moteur n'ouvre plus — un admin y réglait une contrainte sans effet sur le planning) et de `/admin/periodes` (doublon complet de la section périodes de `/historique`, vers lequel le planning V2 renvoyait à tort). 11 composants V1 orphelins supprimés. Liens, entrées de nav, `revalidatePath` et 3 specs e2e rebranchés sur `/equipe` et `/historique` | admin/{veterinaires,periodes}/page.tsx, contraintes-actions.ts, components/admin/* (11), regles/ReglesFocus.tsx, lib/formatContrainte.ts, types/index.ts, PlanningV2.tsx, ActionBar.tsx, notifications-inapp.ts, e2e/{roles,auth,rls-isolation}.spec.ts | Un écran mort n'est pas neutre : tant qu'il est atteignable par URL, il ment activement à celui qui le trouve. Et les commentaires qui décrivent un doublon comme « en place jusqu'à la recette » deviennent des fausses pistes le jour où il disparaît — les mettre à jour dans le même commit. | Oui → lessons-learned |
| 2026-08-01 | **Refonte — étape 2 : le vocabulaire.** L'écran `/regles` devient « Organisation du cabinet » ; l'onglet « Profils » devient « **Périodes types** » (Hiver, Été… un MODÈLE réutilisable, plusieurs plannings peuvent partager la même). Le mot « profil » disparaît de l'écran, de la barre V2 et des messages de Filou. Unification : *créneau* → **type de garde**, *liaison/relation* → **enchaînement**, « Cohortes d'équité » → « **Équilibrer entre certains seulement** ». Filou apprend les nouveaux mots dans ses descriptions d'outils | ReglesStructureV2.tsx, regles/Onglet{Profils,Creneaux,Enchainements,Moteur}.tsx, regles/types.ts, BarreV2.tsx, (v2)/regles/page.tsx, types/index.ts, lib/ia/outils/structure.ts | Frontière assumée : on renomme ce que l'utilisateur LIT, pas ce que Postgres stocke (`profils_planning`, `creerProfil` restent). Renommer les deux à la fois aurait mêlé un chantier de vocabulaire à une migration — et un `<label>` faux ne casse pas un test, il casse la confiance. La preuve que les anciens noms étaient mauvais : MiKL a dû inventer « période structurelle » pour se faire comprendre. | Oui → lessons-learned |
| 2026-08-01 | **Recette étape 2 (retour MiKL).** (1) « Types de garde » → « **Structure des gardes** » ; « Créer un type sur-mesure » → « Ajouter une garde ». (2) Cartes de la structure : jours **en toutes lettres** (« Lundi, mardi, mercredi et jeudi » au lieu de « Lun, Mar, Mer, Jeu »), les 3 faits passent de `flex-wrap` à une **grille à colonnes fixes** (les en-têtes JOURS/PLACES/HORAIRES dansaient d'une carte à l'autre) et chacun prend un **encart** (fond, bordure, coins). (3) Nouveau composant `AideFilou` : bouton « Demander à Filou » **en haut à droite** des cartes sans « + Ajouter » (Équilibrage des charges, Préférences du planning, Ce que les enchaînements imposent) ; l'encart en pied de carte est réduit à sa phrase | reglesStructure.ts, v2-regles.css, regles/AideFilou.tsx (nouveau), Onglet{Creneaux,Moteur,Enchainements,Profils}.tsx, ReglesStructureV2.tsx | On abrège quand la place manque — elle ne manquait pas. Et un `flex-wrap` dimensionne chaque carte sur SON contenu : des faits qu'on compare verticalement doivent être en grille, pas en flex. L'action d'une section se cherche en haut à droite : une porte de sortie posée en pied de carte n'est lue qu'après avoir renoncé. | Oui → lessons-learned |
| 2026-08-01 | **Régression V1→V2 réparée : les vétérinaires retrouvent l'accès à l'Organisation, en lecture seule.** Les deux pages V1 fusionnées (`/regles`, `/admin/structure`) étaient ouvertes à toute l'équipe — leur `isAdmin` grisait les boutons sans fermer la porte (« Lecture seule — seul l'administrateur peut modifier »). La bascule V2 (`ccc71a1`) avait posé un `redirect('/accueil')` : un véto ne pouvait plus voir les horaires ni les règles qui produisent SON planning. Trouvé en auditant les actions serveur sans appelant V2 | (v2)/regles/page.tsx, ReglesStructureV2.tsx, BarreV2.tsx, Onglet{Moteur,Enchainements}.tsx, v2-regles.css, e2e/roles.spec.ts | Une vitrine ne se fabrique pas en conditionnant vingt boutons en JSX : il suffit d'en oublier un — ou d'en ajouter un six mois plus tard — pour qu'un lecteur reçoive une erreur serveur. `<fieldset disabled>` + une règle CSS qui masque `.btn`/`.icon-btn` rendent la garantie STRUCTURELLE. Et l'audit des actions serveur sans appelant est la bonne sonde pour retrouver ce qu'une migration d'écran a laissé tomber. | Oui → lessons-learned |
| 2026-08-01 | **Refonte — étape 3 : 5 cartes → 3.** L'onglet « Règles » posait cinq entrées dont deux redisaient une question déjà posée. (1) « Composition d'équipe » rejoint « Règles du cabinet » : nommer quelqu'un ou nommer une étiquette, c'est la même question (*qui peut faire quoi ?*), même échelle de fermeté, même moteur — les deux familles sont désormais rangées ENSEMBLE dans les groupes de fermeté (🔴 fermes / 🟠 sauf crise / 🟡 confort / Désactivées), deux boutons de saisie en tête (« + Nouvelle règle » · « + Par étiquette »). (2) « Équilibrer entre certains seulement » devient une SOUS-SECTION de « Équilibrage des charges » (`.card-head.sous-section` : trait en haut, titre plus petit, fond en retrait) — un équilibrage par étiquette s'ajoute à la ligne générale, il ne se comprend qu'en la voyant. Le mot « cohorte » disparaît des libellés restants (bouton, aria-label, title) | regles/OngletMoteur.tsx, v2-regles.css | Deux cartes séparées se contredisent à distance : on réglait « Week-ends » sur Essentielle en haut sans voir la cohorte qui la rejouait plus bas. Et un composant défini dans le corps d'un composant est un TYPE NEUF à chaque rendu — React démonte son sous-arbre : le panneau de saisie extrait est une **fonction** de rendu (`panneauEquipe()`), pas un `<PanneauEquipe/>`, sinon le champ « étiquette » perdrait le focus à chaque frappe. | Oui → lessons-learned |
| 2026-08-01 | **« + Une nouvelle étiquette… » n'était qu'une impasse** : le serveur refuse toute règle dont l'étiquette n'a aucun porteur, et rien dans le panneau ne permettait de la poser. Le panneau demande maintenant « Qui porte cette étiquette ? » (vétos ACTIFS en pastilles) et pose l'étiquette sur leurs fiches avant de créer la règle (`poserEtiquetteSurVetos`) — idem pour l'équilibrage par étiquette. Et les 31 refus des 4 onglets passent de la vignette `sonner` à une **vraie modale** : message serveur en tête, explication en français, porte de sortie (« Ouvrir la page Équipe », « Se reconnecter », « Recharger ») | regles/actions.ts, ErreurBloquante.tsx (nouveau), lib/regles/refus.ts (nouveau), Onglet{Moteur,Profils,Creneaux,Enchainements}.tsx, regles/types.ts, (v2)/regles/page.tsx, v2-regles.css, tests/lib/refus.test.ts (16) | Un garde-fou serveur légitime devient un piège quand l'écran propose le geste qu'il interdit : soit on retire la porte, soit on la rend franchissable. Et une vignette de 3 secondes en bas d'écran est le bon format pour « enregistré », le pire pour « refusé » — l'information arrive au moment où l'on vient de remplir un panneau entier. Le décodeur de messages est testé contre les textes RÉELS du serveur : reformuler un message côté serveur le ferait retomber en silence sur le cas générique, sans rien casser et sans que personne ne le voie. | Oui → lessons-learned |
| 2026-08-02 | **Étiquette hors « junior/senior » effacée au décochage** : la fiche véto proposait `TAGS_PRESETS` + les étiquettes cochées sur CETTE fiche — toute étiquette hors des deux presets disparaissait du choix dès qu'on la décochait, sans moyen de la recocher. Le choix se déduit désormais de ce que l'équipe porte réellement (+ celles tapées dans la session), et toute comparaison passe par la forme minuscule | EquipeV2.tsx | Deux valeurs codées en dur qui « amorcent » un choix ne sont pas un référentiel : le jour où on ajoute une troisième valeur par un autre chemin, elle n'a pas le même statut que les deux premières — et le bug ne se voit que sur ce chemin-là. | Oui → lessons-learned |
| 2026-08-02 | **Filou gardien : une règle est vérifiée avec les autres AVANT d'être écrite** (objectif V1 jamais tenu). `engine/pre-vol.ts` (11 familles d'incohérences) ne tournait qu'à la génération ; il est rejoué DEUX FOIS à la validation — sans puis avec la règle candidate — et on n'affiche que le DELTA. Trois issues : corriger (assouplir / aller à la page Équipe), revenir à sa règle, ou enregistrer quand même. Branché sur les 3 portes (formulaire nominatif donc aussi /equipe, panneau par étiquette, équilibrage par étiquette). Aucun appel d'IA : le moteur décide, Filou est le porte-parole | lib/regles/paramsRegle.ts (extraction), data/verifierRegleCandidate.ts, lib/regles/corrections.ts, GardienFilou.tsx, regles/actions.ts, OngletMoteur.tsx, RegleFormDialog.tsx, v2-terrier.css, tests/lib/corrections.test.ts (19) | Le DELTA est ce qui rend l'avertissement crédible : sans lui, un cabinet qui traîne déjà trois avertissements les reverrait à chaque enregistrement, et on apprendrait à cliquer « quand même » sans lire. Un contrôle qui se déclenche toujours n'avertit plus de rien. Et un gardien doit bâtir la règle candidate avec EXACTEMENT la fonction qui l'écrira — d'où l'extraction de `construireParams` hors du fichier 'use server' (qui n'exporte que des fonctions async). | Oui → lessons-learned |
| 2026-08-02 | **Page blanche à la validation d'une règle** — cause racine PROUVÉE par la trace runtime Vercel : `ReferenceError: BriqueEvaluable is not defined`. `regles/actions.ts` (fichier `'use server'`) réexportait trois types (`export type { … }`) ; le transformateur recense les exports pour les enregistrer comme actions AVANT l'effacement des types, et émet un export runtime d'un symbole inexistant. Types réimportés depuis leur module + filet `try/catch` total sur `verifierRegle` (qui remonte désormais son `diagnostic` à l'écran) | regles/actions.ts, RegleFormDialog.tsx, OngletMoteur.tsx, creerRegleProposee.ts, CompositionEquipeClient.tsx, ia/outils/regles.ts, ia/regleSchema.ts, data/verifierRegleCandidate.ts, tests/lib/use-server-exports.test.ts (2) | `tsc --noEmit` passe, `next build` passe, 930 tests passent — et la page tombe en blanc en production. Un raisonnement juste en général (« un type est effacé à la compilation ») peut être faux dans un contexte précis : la seule parade est un test qui lit le SOURCE. Et une server action FACULTATIVE (un contrôle, une suggestion) doit avoir un filet total : rien de ce qui casse dedans ne doit faire perdre la saisie de l'utilisateur. | Oui → lessons-learned |
| 2026-08-02 | **Filou gardien était MUET** — `verifierRegleCandidate` lisait `periodes.nom`, colonne qui n'existe pas (c'est `libelle`). Supabase ne lève pas d'exception là-dessus : il renvoie `data: null`, et le `?? []` concluait « aucune période » → verdict silencieux sans même le message de panne. 6 règles absurdes acceptées sans un mot. Correctifs : bonne colonne, TOUTE `error` de requête levée, période de référence = planning pas encore publié, + les 21 refus de `RegleFormDialog` passent en modale et ses styles rejoignent `v2-terrier.css` | data/verifierRegleCandidate.ts, RegleFormDialog.tsx, v2-regles.css → v2-terrier.css, tests/lib/colonnes-lues.test.ts (2) | Le filet `try/catch` posé une heure plus tôt n'a rien vu : il n'y avait pas d'exception. `?? []` sur une requête en échec transforme une panne en résultat vide — le silence le plus coûteux qui soit, parce qu'il ressemble exactement au cas nominal. Distinguer « zéro ligne » (métier) de « la requête a échoué » (panne) doit se faire À LA SOURCE : à l'arrivée, les deux sont identiques. | Oui → lessons-learned |
| 2026-08-02 | **Finitions écran Règles (recette MiKL)** : (1) « 24 XOR 31 déc » → « L'une ou l'autre, jamais les deux » ; (2) les 4 refus de doublon renvoient l'id de la règle fautive → bouton « Voir la règle existante » (halo `?focus=`) au lieu de « retrouve-la dans la liste » ; (3) l'anti-doublon compare l'EMPREINTE MOTEUR (`lib/regles/identiteRegle.ts`) et non le JSON stocké — le mercredi d'Anne-Catherine passait parce que `periode`/`description` différaient alors que `violeReposFixe` ne lit que `jour` ; (4) « En parler avec moi » emmène Filou AVEC le message du moteur (`lienAccueilAvecSujet`) au lieu d'un bonjour générique | RegleFormDialog.tsx, regles/actions.ts, ErreurBloquante.tsx, GardienFilou.tsx, lib/regles/identiteRegle.ts, lib/v2/filou-origine.ts, Epicentre.tsx, v2-terrier.css, tests/lib/{identiteRegle,filou-sujet}.test.ts (19) | Un message qui dit « retrouve-la dans la liste » sans y emmener est une impasse polie : le serveur CONNAÎT l'id, il suffisait de le renvoyer. Et comparer deux règles sur leur JSON, c'est comparer leur rédaction, pas leur effet — l'identité d'une règle est ce que le MOTEUR en lit. Prudence asymétrique sur les champs décoratifs : en oublier un laisse passer un doublon, en ajouter un à tort fait refuser deux règles différentes. | Oui → lessons-learned |
| 2026-08-02 | **Le repos fixe peut viser certaines gardes du jour** (`interdire_creneau` accepte `creneaux`). Bombe désamorcée : la règle ne savait viser qu'un JOUR ; le jour où un cabinet déclare deux gardes le même jour (la structure le permet), elle en interdirait deux en n'en annonçant qu'une. Le `periode: 'apres_midi'` des données d'origine, JAMAIS évalué par le moteur, disparaît du libellé. Ciblage threadé au solver ET au validateur, validé contre le référentiel du cabinet, champ non écrit si rien n'est coché (empreinte inchangée pour les 18 règles en base) | hard-constraints.ts, validerPlanning.ts, catalogue.ts, lib/regles/paramsRegle.ts, regles/actions.ts, RegleFormDialog.tsx, tests/engine/repos-fixe-cible.test.ts (9) | Une règle qui affiche une portée plus fine que celle qu'elle applique est une bombe à retardement : elle dort tant que les deux se confondent (une garde par jour), et se réveille en production le jour où la structure change — sans que rien ne signale l'écart. Corollaire : ne jamais AFFICHER un paramètre que le moteur n'évalue pas. Et tout ciblage nouveau se thread aux DEUX gardiens, sinon le validateur crie des violations fantômes. | Oui → lessons-learned |
| 2026-08-02 | **Créer un planning est devenu la première étape de « Générer »** (décision MiKL). Le bouton agissait en aveugle sur la période du mois affiché, et restait grisé sans un mot quand le mois n'en avait aucune — la création vivait dans « Historique », personne ne pouvait le deviner. Il pose désormais la question : nouveau planning (nom · lundi de départ · durée en semaines — 12 l'hiver, 17 l'été, avec un repli « date de fin précise » · période type) créé PUIS rempli dans la foulée, ou reprise d'un existant (celui du mois affiché pré-coché). `/historique` perd la création, garde la consultation, et laisse un lien vers Planning. Deux trous comblés au passage : `creerPeriode` acceptait une date de fin ANTÉRIEURE au début (le test de chevauchement ne peut pas voir une fenêtre vide), et les écrans lisant `periodes` n'étaient pas revalidés (`/planning`, `/accueil`, `/regles` restaient figés) | AssistantGeneration.tsx (nouveau), outils-planning.tsx, PlanningV2.tsx, HistoriqueV2.tsx, planning/page.tsx, historique/page.tsx, admin/periodes/actions.ts, lib/planning/duree.ts (nouveau), CreerPeriodeDialog.tsx (supprimé), v2-planning.css, v2-historique.css, tests/lib/duree-planning.test.ts (12) | Un bouton grisé est le pire des refus : il ne dit ni pourquoi, ni où aller. Le trou n'était pas fonctionnel (tout existait) mais de PARCOURS — la création vivait sur un écran de consultation. Corollaire retenu : quand on déplace un geste d'un écran à l'autre, laisser le chemin (un lien) et pas un vide. Côté technique, deux réflexes : un contrôle de cohérence qui compare des bornes (ici le chevauchement) ne voit RIEN d'un intervalle vide — il faut valider l'ordre des bornes séparément ; et une action qui crée une ligne doit revalider TOUS les écrans qui la lisent, pas seulement celui d'où l'on vient (inventaire refait : 4 routes, plus le dock rendu à l'intérieur). | Oui → lessons-learned |
| 2026-08-02 | **Le verrou de génération cassait TOUTE génération** — `POST /api/generate` répondait 500 « column periodes.generation_lock_at does not exist » alors que la colonne existe. Cause racine PROUVÉE par sonde curl : PostgREST accepte un filtre `or=` en LECTURE mais le qualifie `periodes.colonne` en ÉCRITURE, où la requête utilise un alias ; un `.eq()` seul passe. Le compare-and-swap descend en RPC SQL (`acquerir_verrou_generation`, SECURITY INVOKER pour garder la RLS), où il est vraiment atomique | api/generate/route.ts, migration 20260802200000_verrou_generation_rpc.sql | Le message d'erreur désignait une colonne absente ; elle était là. Sans la sonde qui isole lecture/écriture puis payload/filtre, on partait sur le cache PostgREST ou une migration non jouée — deux fausses pistes qui « expliquent » aussi bien le symptôme. Corollaire durable : ne jamais mettre `.or()` sur un `.update()` Supabase ; si le filtre est composite, il descend en SQL. | Oui → lessons-learned |
| 2026-08-02 | **Les deux bandeaux d'alerte du planning repoussaient le calendrier hors de l'écran** (retour MiKL : « c'est quoi tout ce bazar »). Repliés par défaut ; le bandeau rouge groupe désormais par CAUSE (« 64 incohérences » = 2 règles répétées sur 64 dates) et traduit les codes machine (`ROLE_TAG`, `COMPOSITION`) en français ; le pré-vol passe en cartes, règles en cause en pastilles sous le constat au lieu d'une liste à puces qui s'y confondait ; habillage V2 au lieu des classes `amber-*`/`red-*` V1. Plus : « Gérer les périodes » du menu ouvre l'assistant au lieu de mener à `/historique` qui ne sait plus créer, et le refus de chevauchement donne des dates en français + la date à partir de laquelle c'est libre | RevalidationRealtime.tsx, PreVolAlert.tsx, lib/regles/libelleViolation.ts, PlanningV2.tsx, outils-planning.tsx, AssistantGeneration.tsx, admin/periodes/actions.ts, v2-planning.css, tests/lib/libelle-violation.test.ts (8) | Un compteur d'incidents n'est pas une liste d'incidents : afficher 64 lignes là où il y a 2 causes fait passer un problème réglable en 2 gestes pour une catastrophe illisible. Règle retenue : un bandeau est un SIGNAL (une ligne, un ton, un bouton), le détail vit derrière un dépli et se groupe par cause, jamais par occurrence. Et un code interne qui fuit à l'écran (`ROLE_TAG`) est toujours un défaut, même quand la phrase à côté est correcte. | Oui → lessons-learned |
| 2026-08-02 | **Le parcours de génération, du départ au planning publié** (demande MiKL : « une succession de pop-ups qui accompagnent tout au long du processus », « on touche le cœur de l'application »). Cinq temps : ① quel planning · ② **avant de lancer** — ce qui coince, réglable SUR PLACE, le bloquant barre la route et le « à surveiller » laisse passer · ③ ça tourne · ④ le résultat, avec retour à ② pour corriger · ⑤ publier — contrôle final, ce que ça déclenche (équipe, e-mails, agendas), puis ce qui vient de se passer. **Le pré-vol remonte désormais la GRAVITÉ et les IDENTIFIANTS** (`regleIds`, `tag`) : il ne renvoyait que des libellés, on ne pouvait donc rien faire de ce qu'il disait. Chaque point porte ses gestes — assouplir, mettre en pause, poser/retirer l'étiquette avec choix des vétos sur place. Barre d'outils : « Générer » prend l'accent (dégradé + étincelle), et un planning publié cesse d'afficher un faux bouton vert « Publiée » — c'est un état | ParcoursGeneration.tsx, DialogPublication.tsx, PointPreVol.tsx (nouveaux), PreVolAlert.tsx, outils-planning.tsx (réécrit), engine/pre-vol.ts, regles/actions.ts (+assouplirRegle, mettreEnPauseRegle, retirerEtiquetteDeVetos), v2-planning.css, tests/engine/pre-vol-gravite.test.ts (13) | Un diagnostic qu'on ne peut pas actionner coûte plus qu'il ne rapporte : il fait porter à l'utilisateur le travail de retrouver l'objet dont on vient de lui parler. Le pré-vol connaissait les ids depuis toujours (`regleId`, commenté « ciblage UI » dans structure-config) — il ne les publiait simplement pas. Deuxième leçon : la gravité se dérive du CODE, en un seul endroit, jamais au cas par cas dans les douze points de production — sinon deux avertissements identiques finissent classés différemment. Et un état ne doit JAMAIS avoir la forme d'un bouton : « Publiée » en vert plein promettait un geste qui n'existait pas. | Oui → lessons-learned |
| 2026-08-03 | **Recette du parcours de génération — 6 retours MiKL.** (1) « Générer » au survol : `.head-btn:hover` repeignait le texte en `--accent-ink`, brun sur brun ; le bouton accentué réaffirme son blanc sur tous les états. (2) « indisponible impairess » : un `s` collé à une valeur portant déjà sa marque du pluriel, corrigé aux DEUX sources (validateur + rendu catalogue) et la phrase nomme enfin son sujet. (3) « D'où vient la règle du lundi ? » → du moteur (semaines pleines : rythmes, séries, équité s'ancrent sur le lundi ; `lundisDePeriode` remonte au lundi précédent, donc hors période). On ne refuse plus : on **CALE** sur le lundi de la semaine choisie, en le disant. (4) Un planning créé alors qu'on n'allait pas au bout restait orphelin → l'étape ① propose « Reprendre le planning en cours » dès qu'un brouillon n'a jamais été rempli. (5) « Relever les limites » sortait du parcours et n'avait pas la forme d'un bouton → bouton, **nouvel onglet**, + « J'ai corrigé — revérifier » à l'étape de contrôle. (6) Cartes sans aucun bouton : `charge_globale_insuffisante` et `weekends_insuffisants` ne remontaient pas leurs `regleIds` | hard-constraints.ts, briques/catalogue.ts, engine/pre-vol.ts, lib/planning/duree.ts, ParcoursGeneration.tsx, PointPreVol.tsx, outils-planning.tsx, PlanningV2.tsx, v2-planning.css, tests/lib/duree-planning.test.ts (+6) | Trois familles de leçons. **Contraintes techniques** : quand le moteur impose quelque chose (le lundi), l'interface doit CALER et expliquer, jamais refuser — l'utilisateur n'a pas à connaître la mécanique interne. **Parcours** : un lien qui quitte un parcours en cours le DÉTRUIT ; s'il faut sortir, c'est dans un nouvel onglet, avec un moyen de revérifier au retour. Et tout objet créé au milieu d'un parcours doit être repêchable, sinon il s'accumule en doublons invisibles. **Libellés** : un `${valeur}s` sur une chaîne métier est une bombe à retardement — elle explose le jour où la valeur porte déjà son pluriel, et ces libellés remontent jusqu'aux écrans clients. | Oui → lessons-learned |
| 2026-08-03 | **Chaque point de pré-vol emmène droit sur ses règles (halo `?focus=`)**. Le renvoi menait à l'écran Règles nu — à l'admin de retrouver dans une longue liste celle dont on venait de lui parler. Il porte désormais les ids des règles en cause, l'écran défile jusqu'à elles et les entoure du halo (même mécanisme que « Voir la règle existante » des refus de doublon). **TOUTES les règles, pas la première** : un point en met souvent plusieurs en cause (« limites cumulées de week-end » en désigne six) — le halo accepte plusieurs ancres séparées par des virgules, un id seul reste traité comme avant. Le bouton annonce le compte (« Voir les 6 règles en cause »). « Créneau impossible » garde le renvoi générique : ses raisons viennent du rejeu du moteur, pas d'une ligne identifiée | lib/regles/focusRegles.ts (nouveau), PointPreVol.tsx, OngletMoteur.tsx, tests/lib/focus-regles.test.ts (10) | Un halo qui n'éclaire qu'une cible sur six ne fait que déplacer la recherche : il faut couvrir tout ce que le diagnostic a désigné, sinon l'utilisateur repart chercher à la main et le raccourci n'a rien économisé. Deuxième point : le contrat « fabriquer le lien » / « le relire » vit dans UN module testé — écrits chacun de leur côté, ils divergent au premier changement de format, et l'échec est silencieux (une ancre qui ne correspond à rien ne fait simplement rien). Enfin, ne jamais haloter une cible DEVINÉE : mieux vaut un renvoi générique honnête qu'un halo sur la mauvaise règle, qui ferait modifier un réglage innocent. | Oui → lessons-learned |
| 2026-08-03 | **Recette du parcours (2ᵉ passe) — 5 retours.** (1) « J'ai corrigé — revérifier » restait actif sous « Rien ne coince » ; masqué tant qu'aucun point ne subsiste. (2) Générer un planning **existant** ne renvoyait pas sur son mois — le ciblage navigue désormais dès le choix, comme le faisait déjà la création. (3) « Publier » grisé n'expliquait rien : il reste **cliquable** sur un brouillon, et le contrôle de publication répond « ce planning n'a aucune garde, génère-le d'abord » puis interdit la publication à vide. (4) « Reprendre » n'en proposait qu'un sans dire lequel → c'est **le dernier créé** (tri sur `created_at`, pas sur la date de début), la carte le dit et annonce combien d'autres attendent dans la liste. (5) Aucun moyen de supprimer un brouillon, les essais s'empilaient → la liste marque « Jamais rempli » les plannings sans gardes et porte une corbeille avec confirmation en ligne, bornée aux brouillons vides (garde-fou serveur : ni un planning rempli, ni un que l'équipe a vu) | ParcoursGeneration.tsx, outils-planning.tsx, v2-planning.css | Un bouton **grisé** est un refus muet : il faut soit expliquer à côté, soit le laisser cliquable pour que le clic explique. Deuxième piège, structurel : dès qu'un parcours CRÉE des objets, il doit fournir dans le même écran de quoi les **reprendre ET les supprimer** — sinon chaque essai abandonné laisse un déchet, et l'utilisateur finit par se heurter à ses propres résidus (ici, un chevauchement de dates avec un planning qu'il avait lui-même créé sans le savoir). Enfin : « le plus récent » ne se déduit pas d'une liste triée pour un autre usage — trier sur `date_debut` pour l'affichage et lire `[0]` comme « le dernier créé » est un faux ami. | Oui → lessons-learned |
| 2026-08-03 | **Supprimer un brouillon même généré, et « Selon la saison » nomme sa période type.** (1) `supprimerPeriode` refusait tout planning ayant des gardes ; la règle est levée pour les BROUILLONS (jamais vus par l'équipe, aucun e-mail parti, aucun agenda rempli — tout cela se déclenche à la publication). Publiés et verrouillés restent intouchables, double garde-fou. Inventaire des dépendances refait **sur les FK réelles** : `gardes`/`attributions`/`bonus_malus` cascadent, `email_log`/`historique_fete` passent à NULL, `compteurs_gardes` et `planning_semaine` sont des VUES, et seule `regles_cabinet.periode_id` (NO ACTION) peut bloquer — détecté avant, expliqué en français. (2) La confirmation « Supprimer ? Non/Oui » se chevauchait avec le nom du planning → la rangée bascule entièrement et annonce ce qu'on perd. (3) « Selon la saison » n'indiquait pas sur quoi le moteur se base → il affiche la période type réellement retenue. (4) La corbeille de l'écran Historique prévient elle aussi quand des gardes seront effacées | admin/periodes/actions.ts, ParcoursGeneration.tsx, SupprimerPeriodeButton.tsx, historique/page.tsx, v2-planning.css | Un garde-fou trop large est un bug lent : interdire la suppression d'un brouillon rempli semblait prudent, mais l'utilisateur accumulait des essais sans issue jusqu'à se heurter à ses propres résidus. La bonne question n'est pas « y a-t-il des données ? » mais « quelqu'un les a-t-il vues ? » — ici la publication, pas la génération, est la ligne rouge. Deuxième leçon, méthode : avant d'autoriser une suppression, lire les **contraintes FK réelles** (`information_schema`) plutôt que de raisonner sur le code — ça a révélé que trois tables cascadaient déjà, que deux « tables » étaient des vues, et qu'une seule pouvait bloquer. Troisième : une option nommée « automatique » doit dire ce qu'elle va choisir, sinon elle demande un acte de foi. | Oui → lessons-learned |
| 2026-08-03 | **Corbeille dans le menu de période + encart compteurs à colonnes réglables.** (1) La suppression rejoint le menu déroulant des plannings — le geste doit exister là où on les choisit. (2) L'encart compteurs gagne une colonne **« Écart à la juste part »** : les trois d'origine donnaient des nombres bruts, il fallait lire la phrase du bas (« écart de 10 week-ends ») pour savoir qu'il y avait un problème, sans savoir chez qui. Même calcul que le bilan officiel (`calculerBilans`). (3) **Colonnes au choix, 4 maximum** (catalogue de six : WE, nuits, 1ᵉʳ WE, écart, fériés, total), engrenage dans l'encart, réglage stocké par personne dans une nouvelle table `preferences_affichage`. La grille CSS était figée à 4 colonnes chiffrées alors que 3 étaient rendues — c'était le vide repéré par MiKL | PlanningV2.tsx, CompteursPanel.tsx (nouveau), lib/planning/colonnesCompteurs.ts (nouveau), preferences/actions.ts (nouveau), planning/page.tsx, v2-planning.css, migration 20260803140000_preferences_affichage.sql, tests/lib/colonnes-compteurs.test.ts (10) | **Ne jamais ouvrir l'écriture d'une table métier pour y loger une préférence.** Mettre `colonnes_compteurs` sur `veterinaires` aurait exigé une policy UPDATE pour chaque vétérinaire sur SA fiche — donc aussi sur `role_app`, `dernier_recours`, `tags` et `actif` : une policy Postgres ne se restreint pas à une colonne. Une table dédiée donne exactement le droit nécessaire. Deuxième leçon : un tableau de chiffres **bruts** ne répond pas à la question qu'on se pose (« est-ce juste ? ») — c'est la valeur DÉRIVÉE (l'écart à la référence) qui informe, et elle doit venir du même calcul que le bilan officiel, sinon deux écrans annoncent deux vérités. Troisième : une limite d'affichage (4 colonnes) doit être expliquée à l'utilisateur par sa CAUSE (« l'encart est étroit ») et non subie comme un refus. | Oui → lessons-learned |
| 2026-08-03 | **AUDIT du principe fondamental + paliers 1, 2 et 4.** Constat : « chaque règle vérifiée avec l'existant » n'était appliqué que sur **1 porte d'entrée sur 10** — le gardien vivait dans le composant React de l'écran Règles, donc Filou écrivait sans contrôle et les 17 actions de l'écran Organisation n'en avaient jamais eu. **Palier 1** : `data/controleImpact.ts` — le gardien généralisé côté SERVEUR, avec un type `Mutation` (le compilateur force à dire comment chaque nouvelle porte s'applique), le monde « après » fabriqué en mémoire, le delta dans les deux sens (ce qui casse ET ce qui se répare), et `refusSiBloquant` comme passage obligé. `verifierRegleCandidate` devient un adaptateur. **Palier 2** : congés validés, départs de vétérinaires, effectif de nuit, pose d'étiquette. **Palier 4** : `signalerLimite` + `SignalerLimite.tsx` — quand le moteur cale sans cause identifiable, l'utilisateur prévient l'éditeur en un clic (contexte technique, aucun planning nominatif) | data/controleImpact.ts (nouveau), verifierRegleCandidate.ts (adaptateur), regles/actions.ts, conges/actions.ts, admin/veterinaires/actions.ts, admin/periodes/actions.ts, assistance/actions.ts (nouveau), SignalerLimite.tsx (nouveau), ParcoursGeneration.tsx, RegleFormDialog.tsx, paramsRegle.ts, v2-planning.css, tests/lib/controle-impact-couverture.test.ts (13) | **Un principe tenu par discipline dans chaque écran finit toujours par être oublié dans le suivant.** Personne n'avait « oublié » le gardien ailleurs : il n'était simplement pas sur le chemin. La parade n'est pas de mieux se souvenir, c'est de déplacer le contrôle à l'étage où toutes les portes passent — puis d'écrire un test qui LIT LE SOURCE et refuse une écriture non contrôlée. Ce test a immédiatement trouvé un trou réel dans le code que je venais d'écrire (le duo interdit écrit ses deux lignes miroir par un chemin à part). Deuxième leçon : un contrôle en panne doit LAISSER PASSER — bloquer un geste légitime parce que le gardien est cassé est pire que l'absence de gardien. Troisième : un assistant (Filou) ne doit jamais pouvoir poser lui-même le drapeau « passer outre » — un test le vérifie. | Oui → lessons-learned |
| 2026-08-03 | **PALIER 3 — Filou porte-parole, quelle que soit la porte.** Les paliers 1-2 avaient mis le contrôle sur le chemin de toutes les écritures, mais leurs refus ressortaient en **toast d'erreur** : correct, et muet sur la suite à donner — l'utilisateur repartait chercher lui-même la règle en cause, exactement ce qu'on voulait supprimer. `GardienImpact.tsx` est leur voix commune : il sert congés / retrait d'équipe / effectif / étiquettes et embarque les **vrais gestes** (`PointPreVol`) — assouplir, mettre en pause, poser ou retirer une étiquette — sans quitter la fenêtre ; corriger **relance l'action**, la liste se vide et le geste passe tout seul. Le ton suit la gravité : **aucun « quand même » sur un blocage** (ce serait promettre un planning qui ne peut pas exister). Ce que la modification **répare** s'affiche aussi. Les actions renvoient l'`Impact` complet, plus seulement une phrase ; un refus SANS impact (droits, forme) reste un toast — il n'a rien à proposer | components/v2/GardienImpact.tsx (nouveau), ValiderCongeDialog.tsx, EquipeV2.tsx, conges/actions.ts, admin/veterinaires/actions.ts, v2-terrier.css, tests/lib/controle-impact-couverture.test.ts (18) | Un contrôle qui trouve mais ne propose rien reporte le travail sur l'utilisateur : il apprend qu'il y a un problème, pas comment en sortir. La différence entre un avertissement et une AIDE tient en quatre points, tous vérifiés par le test : l'impact remonte jusqu'à l'écran (et pas seulement un message), la fenêtre porte les gestes, corriger relance l'action, et rien ne propose de passer outre l'impossible. Corollaire de ton : ce qui se RÉPARE doit s'afficher aussi — sans ça, le système ne renvoie jamais que des reproches, et on finit par ne plus le lire. | Oui → lessons-learned |
| 2026-08-03 | **Les deux dernières portes parlent aussi comme Filou** : l'effectif de nuit et la pose d'étiquette bloquaient bien côté serveur, mais leur refus retombait en toast. L'effectif ouvre désormais la fenêtre (le sélecteur revient à sa valeur d'avant, corriger le rejoue) et l'écran Historique lui passe l'équipe active pour que les gestes sur étiquette soient utilisables ; la pose d'étiquette aussi — son refus vient du moteur, pas d'une faute de saisie, une modale d'erreur sèche n'avait rien à en dire | EffectifPeriodeSelect.tsx, OngletMoteur.tsx, admin/periodes/actions.ts, regles/actions.ts, historique/page.tsx, tests/lib/controle-impact-couverture.test.ts (19) | Brancher un contrôle côté serveur ne suffit pas : tant qu'un seul écran laisse le refus en toast, l'utilisateur qui tombe sur CE cas-là reste sans issue — et on ne s'en aperçoit qu'en recette, précisément sur ce chemin. D'où la forme du test : il énumère les quatre écrans ET les quatre actions, pour que la question « est-ce que toutes les portes sont couvertes ? » ait une réponse mécanique et non une revue à l'œil. | Oui → lessons-learned |
| 2026-08-04 | **La période type devient un choix obligatoire — fin de « Selon la saison ».** (1) `creerPeriode` refusait déjà bien des choses, mais **pas l'absence de période type** : sans `profil_id`, il cherchait le premier profil de la saison détectée (mai→août = été), et à défaut insérait NULL, ce qui fait retomber le moteur sur le profil `est_defaut` du cabinet. **Deux replis successifs, tous les deux silencieux.** MiKL : « je ne veux pas qu'il y ait une période par défaut ». Le serveur exige désormais une période type existante, active et du cabinet ; `setProfilPeriode` refuse de la RETIRER (on ne peut plus revenir à « aucune »). (2) Écran : le champ démarre **vide** (« Choisis une période type »), la saison ne fait plus que suggérer à voix haute. Cabinet sans aucune période type → la route est barrée avec la porte de sortie (« Créer une période type »), au lieu de masquer le champ comme avant — ce qui laissait croire qu'il n'y avait rien à décider. (3) **Filou demande confirmation avant de lancer**, en tête du contrôle et donc AVANT les points à régler : « je vais remplir X avec la période type Y — on y va ? » ; « Générer » reste éteint tant qu'on n'a pas répondu. (4) Un planning d'avant la règle (`profil_id` NULL) devient un **point bloquant réglable sur place**. (5) Consumers alignés : outil Filou `creer_periode` (paramètre devenu obligatoire, il DEMANDE au lieu de deviner), `regler_periode` (« remets le défaut » n'existe plus), sélecteur de l'Historique, pilule de période | admin/periodes/actions.ts, ParcoursGeneration.tsx, ProfilPeriodeSelect.tsx, PlanningV2.tsx, historique/page.tsx, lib/ia/outils/planning.ts, v2-planning.css | **Un repli silencieux est une décision prise à la place de l'utilisateur, sans trace.** Ici deux s'enchaînaient — la saison, puis le profil « par défaut » — pour choisir la structure sur laquelle tout un trimestre de gardes serait calculé. Rien n'était faux techniquement : simplement, personne ne pouvait dire APRÈS COUP pourquoi le planning contenait ce qu'il contenait. Corollaire de méthode : supprimer un défaut ne se fait pas dans l'écran seul — il faut suivre la donnée jusqu'à **tous** ses points d'écriture (ici l'assistant IA écrivait par la même action serveur) et jusqu'à ses points d'AFFICHAGE, sinon un écran continue d'annoncer « par défaut » un état devenu impossible. Enfin, une confirmation qui porte sur le SOCLE se demande AVANT les détails : régler dix points sur la mauvaise structure, c'est dix réglages perdus. | Oui → lessons-learned |
| 2026-08-04 | **Trois retours de recette.** (1) Compteurs : « assoc. / sal. » collé au prénom chevauchait la colonne WE — retiré (n'explique aucun écart, l'équité ne se calcule pas dessus ; l'info reste sur la fiche véto), et un prénom composé se coupe désormais dans sa colonne. (2) « Je n'ai plus accès aux boutons du bas » : le panneau scrollait bien, mais **tout** scrollait — le pied sortait de vue et rien ne disait qu'il fallait faire défiler. Tête et pied fixes, seul le corps défile. (3) La corbeille du menu de période n'apparaissait qu'**au survol** ; elle est visible en permanence, en discret | CompteursPanel.tsx, v2-planning.css | Une action qu'on ne découvre qu'en promenant la souris n'existe pas — et sur écran tactile elle n'existe littéralement jamais. Deuxième leçon, plus large : `overflow: auto` sur une modale entière n'est pas une mise en page, c'est un filet. Dès qu'un contenu peut grandir (ici l'ajout d'un message de refus), les commandes doivent être **ancrées** hors de la zone qui défile, sinon le bouton d'action devient introuvable au moment précis où l'utilisateur en a besoin. | Oui → lessons-learned |
| 2026-08-04 | **Recette « période type » — 3 retours.** (1) **Refus périmé** : le champ affichait « hiver periode 1 » et le bandeau rouge disait toujours « Choisis la période type ». Le message datait du clic précédent, avant correction, et rien ne l'effaçait — tous les champs du formulaire nettoient désormais le refus dès qu'on y touche. (2) **Phrase de Filou éclatée en colonnes** : `.gp-type-phrase` était en `display: flex`, donc chaque nœud de texte et chaque `<b>` devenait un item flex (« Je vais / remplir | hiver / 2027 | avec la / période / type… »). Remise en flux normal. (3) **« Pourquoi ce n'est pas juste indiqué : période type = hiver = telles caractéristiques ? »** — la confirmation portait sur un NOM que personne ne connaît par cœur. Elle porte maintenant sur une **fiche** en deux colonnes alignées : période type, saison, gardes à couvrir (lues dans `creneau_modele`, descendues du serveur — jamais redécrites côté écran), effectif de nuit. La même fiche apparaît dès le formulaire de création, au choix de la période type. (4) Porte de sortie ajoutée quand AUCUNE période type ne convient : « en créer une ↗ » (nouvel onglet, le parcours survit) + « j'en ai créé une — rafraîchir la liste » | ParcoursGeneration.tsx, planning/page.tsx, PlanningV2.tsx, outils-planning.tsx, v2-planning.css | **Un message de refus qui survit à sa correction ment.** Il envoie chercher un problème déjà réglé — pire qu'un silence, parce qu'on ne sait plus quoi croire de l'écran : tout formulaire qui refuse doit effacer son refus dès la première frappe. Deuxième leçon, CSS : `display: flex` sur un paragraphe **casse la phrase** — les nœuds de texte deviennent des items flex anonymes et se répartissent en colonnes. Le flex sert à poser des BLOCS côte à côte, jamais à mettre en page du texte contenant du gras. Troisième, produit : faire confirmer un identifiant (un nom, une référence) n'est pas une confirmation — il faut montrer le CONTENU sur lequel on s'engage, sinon on récolte un « oui » qui ne vaut rien. | Oui → lessons-learned |
| 2026-08-04 | **La saison et le « profil par défaut » sortent du moteur — 3 étapes.** Suite directe de la période type obligatoire ; MiKL : « à quoi servent les périodes types si on n'y configure plus rien ? » — réponse : elles contiennent tout, ce sont les deux champs de leur CARTE qui ne servaient à rien. **① « Saison suggérée » supprimée** : son unique rôle était de proposer la période type d'office à la création d'un planning, supprimé le matin même. Un réglage affiché que rien n'évalue est pire qu'un réglage absent. **② « Le soir en semaine » supprimé du profil** : doublon PARTIEL de `creneau_modele.nb_places`, qui règle déjà l'effectif de TOUTES les gardes (« pourquoi on ne définit que le nb de véto pour les soirs de la semaine et pas les week-ends ? » — parce que les week-ends, eux, suivaient déjà leur créneau). La chaîne passe de `période > période type > saison` à `surcharge du planning > créneau`. Le repli en dur `hiver = 2 / été = 1` ne survit QUE sur le chemin sans catalogue (legacy). Source unique `plafondNuitSemaine()` créée : la règle était recopiée dans CINQ modules — solver, validateur, pré-vol, contexte de crise, places attendues — dont un (`controleCoherence`) que l'inventaire manuel avait manqué et que le compilateur a trouvé. **③ « Configuration standard » masquée** dès que le cabinet a ses propres périodes types ET qu'aucun planning ne s'appuie encore dessus ; sinon elle reste, en disant combien de plannings la retiennent | OngletProfils.tsx, ReglesStructureV2.tsx, regles/page.tsx, engine/loader.ts, engine/solver.ts, engine/structure-creneaux.ts, engine/pre-vol.ts, validation/validerPlanning.ts, lib/crise/contexte.ts, lib/planning/placesAttendues.ts, lib/ia/controleCoherence.ts, ParcoursGeneration.tsx, planning/page.tsx, + 4 routes absences, tests (5 fichiers, +5 tests) | **Un défaut en dur est une décision prise à la place de l'utilisateur, sans trace ni moyen de la changer.** « Hiver = 2, été = 1 » vivait dans le moteur et pouvait contredire la structure que le cabinet avait lui-même écrite — sur le seul créneau du soir, et en le rabotant silencieusement (`Math.min`). Deuxième leçon, de méthode : quand une règle est recopiée dans N modules, la changer sans source unique laisse forcément un oubli, et l'oubli est INVISIBLE (le moteur construit, le validateur déclare faux — on ne le voit qu'après génération). Le compilateur a trouvé le 5ᵉ site que la lecture n'avait pas vu : rendre le type plus strict (`number` → `number | null`) transforme une divergence silencieuse en erreur de build. Troisième : quand des tests tombent après un changement voulu, la question n'est pas « comment les faire passer » mais « qu'affirmaient-ils exactement » — deux tests d'équivalence catalogue reposaient en SILENCE sur ce repli ; l'hypothèse est devenue explicite au lieu d'être supprimée avec eux. | Oui → lessons-learned |
| 2026-08-04 | **LE SOCLE DE STRUCTURE + L'AFFINAGE PAR PÉRIODE TYPE** (refonte du modèle). MiKL : « la structure donne l'ensemble des possibilités, les périodes types les affinent par période ». **Le malentendu de départ** : `creneau_modele` portait `profil_id` — chaque période type possédait sa PROPRE COPIE complète des types de garde (d'où « Copier la structure des gardes de » à la création). Changer un horaire obligeait à le refaire partout, « Structure des gardes » ne montrait jamais le cabinet mais une période type, et il n'existait nulle part de notion de « le vendredi, on peut aller jusqu'à 2 ». **Le modèle posé** : le SOCLE (`profil_id IS NULL`, un par cabinet) décrit ce qui est possible (types, jours, horaires, `nb_places` = maximum) ; `periode_type_creneau (profil_id, creneau_id, nb_vetos)` dit ce que chaque période type en retient. **`nb_vetos = 0` = pas de garde de ce type sur cette période**, et `appliquerAffinage` RETIRE le créneau au lieu de le laisser à 0 place. Migration des données : créneaux du profil défaut → socle, créneaux dupliqués → affinages, liaisons rebranchées et dédupliquées (8 créneaux → 4, 5 relations → 3). Onglets réordonnés (Structure, Enchaînements, **puis** Périodes types). `dupliquer_profil` ne duplique plus que les CHOIX ; `prendre_snapshot` fige le socle DÉJÀ affiné (schema 5) | migrations 20260804140000 + 20260804141000, chargerCreneauModele.ts, data/v2/reglesStructure.ts, admin/structure/actions.ts, OngletProfils.tsx, ReglesStructureV2.tsx, regles/page.tsx, planning/page.tsx, regles/types.ts, v2-regles.css, tests (5 fichiers, +16 tests) | **Quand l'utilisateur ne comprend pas un écran, c'est parfois le MODÈLE qu'il faut regarder, pas l'écran.** Trois allers-retours ont porté sur des libellés et des réglages avant qu'apparaisse la vraie cause : la structure était dupliquée par période type. Ma réponse « le nombre de vétos du week-end se règle dans Structure des gardes » était exacte ET à côté — vraie dans le modèle existant, fausse par rapport à ce que l'utilisateur croyait manipuler. Deuxième leçon, technique : dans une migration, **dédupliquer AVANT de rebrancher**. Rebrancher d'abord fait violer la contrainte d'unicité PENDANT l'UPDATE, bien avant que le DELETE de nettoyage ait la moindre chance de tourner — l'ordre naturel de la pensée est l'inverse de l'ordre que la base impose. Troisième : une table qui en affine une autre doit **copier ses politiques RLS à l'identique** ; inventer une variante ouvre un trou par la porte de côté. | Oui → lessons-learned |
| 2026-08-04 | **Recette du socle — la période type se paramètre AVANT d'exister, et la carte redevient lisible.** (1) MiKL : « pourquoi quand je crée une période type je ne peux pas la paramétrer ? ». Le panneau de création ne demandait qu'un nom et une source ; il fallait créer, PUIS régler sur la carte — deux gestes pour une intention, et entre les deux une période type qui ne correspondait à rien de voulu. Les menus « combien de vétérinaires » sont désormais DANS le panneau, pré-remplis depuis la source (changer de source les recharge), et `creerProfil` les écrit juste après la RPC, chaque valeur bornée par le socle relu en base. Si l'écriture des réglages échoue, on ne supprime pas la période type créée : on dit exactement ce qui n'a pas été appliqué. (2) « La présentation est claquée, c'est anarchique » : `.ptc-ligne` était en `flex` + `flex-wrap`, donc chaque ligne décidait seule — nom court, menu à côté ; nom long, menu à la ligne. Quatre lignes, quatre mises en page. Passage en `grid` 2 colonnes (une colonne de menus commune), séparateurs pointillés, et cartes élargies de 300 à 380 px — une carte de période type n'est plus une vignette, c'est un tableau de bord. (3) Coquille « tournentencore » corrigée | OngletProfils.tsx, admin/structure/actions.ts, v2-regles.css | **Un formulaire de création doit produire l'objet VOULU, pas un objet à corriger.** Séparer « créer » de « paramétrer » semble anodin côté code (deux actions au lieu d'une) et coûte cher côté usage : l'utilisateur voit apparaître quelque chose qu'il n'a pas demandé, et doit réparer. Deuxième leçon, CSS : `flex-wrap` sur des lignes de formulaire donne une mise en page qui DÉPEND DU CONTENU — chaque ligne se replie à un endroit différent selon la longueur de son libellé. Pour aligner des paires « libellé / contrôle », c'est `grid` : la colonne est décidée une fois pour toutes les lignes, et l'œil descend droit. | Oui → lessons-learned |
| 2026-08-04 | **Filou rattrape le nouveau modèle — il était CASSÉ, pas seulement en retard.** MiKL : « évidemment tu donnes les nouvelles instructions à Filou au fur et à mesure ? ». Non — et le constat était pire que prévu : ses outils structure filtraient les créneaux sur `profil_id === <celui de la période type>`, un filtre qui ne rend plus RIEN depuis que les créneaux vivent sur le socle. Filou aurait répondu « aucun type de garde dans cette période type » à un cabinet parfaitement configuré, et `resoudreCreneau` aurait refusé toute modification d'horaire. Corrigé : les trois chargeurs lisent le socle (`.is('profil_id', null)`), `resoudreCreneau` perd son paramètre de profil (une garde appartient au cabinet, plus à une période type), `lire_creneaux_profil` annonce le maximum du socle ET ce que la période type retient (`absente_de_cette_periode` quand c'est 0), `lire_relations_creneaux` signale les liaisons sans effet, `lire_profils_planning` cesse d'annoncer saison suggérée et effectif (supprimés le matin). NOUVEL OUTIL `regler_vetos_sur_periode_type` — le geste central du modèle, qui manquait complètement : « en hiver 2 vétos le week-end », « pas de garde le vendredi soir l'été » (0). Le prompt système de Filou décrit maintenant les deux niveaux (socle / période type) et l'avertissement qui va avec : un horaire touche TOUTES les périodes types, un nombre de vétos une seule | lib/ia/outils/structure.ts, lib/ia/outils/registre.ts, lib/ia/agentFilou.ts, lib/ia/proposerProfil.ts | **Un assistant qui lit la base est un CONSUMER comme un autre — l'inspection des consumers doit l'inclure.** J'avais mis à jour les écrans, le moteur, le validateur, le pré-vol, la crise… et laissé l'IA en arrière, alors qu'elle interroge exactement les mêmes tables. Pire : son échec est SILENCIEUX et poli — pas d'erreur, pas de test rouge, juste une phrase fausse dite avec assurance à un client. Deuxième leçon : quand un modèle de données gagne un geste (ici « combien de vétos sur cette garde pour cette période »), l'assistant doit gagner l'OUTIL correspondant, sinon il devient le seul endroit du produit où l'utilisateur ne peut pas faire ce que l'écran permet. Troisième : le prompt système doit porter le MODÈLE, pas seulement le ton — sans les deux niveaux écrits noir sur blanc, Filou aurait continué à parler de « la structure de l'hiver » comme si elle lui appartenait. | Oui → lessons-learned |
